<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- 暴露自愈测试探针（ShouldStartSelfHeal / 防重入位）给测试程序集，与 S7/Modbus 同范式 -->
    <InternalsVisibleTo Include="NitroGateway.UnitTests" />
    <InternalsVisibleTo Include="NitroGateway.IntegrationTests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Abstraction\NitroGateway.Protocol.Abstractions.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- ADR 固定：Version="*" 每次还原取最新（已还原 1.5.378.156，net10.0），固定避免漂移 -->
    <PackageReference Include="OPCFoundation.NetStandard.Opc.Ua.Client" Version="1.5.378.156" />
  </ItemGroup>

</Project>
