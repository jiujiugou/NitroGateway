<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\NitroGateway.Shared\NitroGateway.Shared.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- ADR-020 P2-3：实现直接 new HttpClient + Polly，未用 IHttpClientFactory；
         移除 Microsoft.Extensions.Http / Http.Resilience 死依赖（也避免把 Logging 拉到 10.0.10 与测试项目冲突） -->
    <PackageReference Include="Polly.Core" Version="8.7.0" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="NitroGateway.IntegrationTests" />
  </ItemGroup>

</Project>
