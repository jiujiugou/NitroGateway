<UserControl x:Class="NitroGateway.Desktop.Views.AlarmsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- 合并共享样式（Themes/Styles.xaml）：无 Application 实例（单元测试冒烟）时也能解析 StaticResource -->
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/NitroGateway.Desktop;component/Themes/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 页头 -->
        <StackPanel Grid.Row="0" Margin="26,22,26,14">
            <TextBlock Text="告警" Style="{StaticResource PageTitleStyle}" />
            <TextBlock Text="告警规则触发与恢复记录" Style="{StaticResource PageSubtitleStyle}" Margin="0,3,0,0" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="26,0,26,22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 工具栏（卡片） -->
            <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}" Padding="14,10" Margin="0,0,0,12">
                <DockPanel>
                    <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center"
                               Foreground="{StaticResource TextSecondary}" />
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <!-- ADR-037 S3：加载中禁用刷新并显示进度条 -->
                        <Button Content="刷新" Command="{Binding RefreshCommand}" IsEnabled="{Binding IsIdle}" />
                        <ProgressBar Width="90" Height="6" IsIndeterminate="True" VerticalAlignment="Center" Margin="10,0,0,0"
                                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <Grid Grid.Row="1">
                <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True"
                          HeadersVisibility="Column">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource DataGridRowStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Severity}" Value="Emergency">
                                    <Setter Property="Background" Value="{StaticResource SeverityEmergencyBrush}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Severity}" Value="Critical">
                                    <Setter Property="Background" Value="{StaticResource SeverityCriticalBrush}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Severity}" Value="Warning">
                                    <Setter Property="Background" Value="{StaticResource SeverityWarningBrush}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn ElementStyle="{StaticResource DataGridCellTextStyle}" Header="发生时间" Binding="{Binding OccurredText}" Width="160" />
                        <DataGridTextColumn ElementStyle="{StaticResource DataGridCellTextStyle}" Header="严重性" Binding="{Binding SeverityText}" Width="90" />
                        <DataGridTextColumn ElementStyle="{StaticResource DataGridCellTextStyle}" Header="状态" Binding="{Binding StateText}" Width="80" />
                        <DataGridTextColumn ElementStyle="{StaticResource DataGridCellTextStyle}" Header="设备" Binding="{Binding DeviceName}" Width="*" />
                        <DataGridTextColumn ElementStyle="{StaticResource DataGridCellTextStyle}" Header="消息" Binding="{Binding Message}" Width="*" />
                        <DataGridTextColumn ElementStyle="{StaticResource DataGridCellTextStyle}" Header="触发值" Binding="{Binding TriggerValue, StringFormat=F2}" Width="90" />
                        <DataGridTextColumn ElementStyle="{StaticResource DataGridCellTextStyle}" Header="阈值" Binding="{Binding Threshold, StringFormat=F2}" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>
                <!-- ADR-037 S3：空态提示，有数据时折叠 -->
                <TextBlock Text="暂无数据" HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondary}" FontSize="13">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Items.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
