# NitroGateway 疑点清单(阶段 2)

> 生成时间: 2026-08-04 | 验证回填: 2026-08-05(D-01) | 全部疑点已验证, 剩余待用户确认项见 [Q-04]

## [Q-01] README 与代码项目数不一致

- 状态: ✅ 已验证
- 结论: slnx 实际收录 21 个项目; 磁盘共 24 个 csproj(src 22 + tests 2)。未收录项目情况:
  - `Protocol.Mitsubishi`(3 cs)、`Protocol.OpcUa`(9 cs): 有源码但无人引用、未入 slnx、Dockerfile 不构建
  - `Infrastructure.Sqlite`、`Scheduler`: **幽灵目录**(git 未跟踪, 仅 obj 残留, 连 csproj 都不存在); tests/NitroGateway.Verification.csproj 引用它们 → **Verification 构建失败(6 错误)**
- 处置: README 已改为"21 个项目"; 幽灵目录清理 + Verification 修复已入 backlog [D-07]; Mitsubishi/OpcUa 处置决策已入 [D-08]

## [Q-02] README 测试数与实际不一致

- 状态: ✅ 已验证
- 结论: 实测 [Fact]+[Theory]=115, 与 dotnet test 115 通过一致; README 写 128 是过时数字
- 处置: README 已改为 115

## [Q-03] 无实际 CI 工作流

- 状态: ✅ 已验证
- 结论: `.github/workflows/` 为空目录; 当前 README 无 CI 描述(非"声称有但缺失")
- 处置: 补 CI 已入 backlog [D-02](高, 机器闸门)

## [Q-04] git 工作区有未提交的文档删除

- 状态: ⏳ 待用户确认
- 依据: 7 个文件删除未提交: ARCHITECTURE.md/html、DEMO-GUIDE.md、PRODUCTION-GUIDE.html、PROJECT-WALKTHROUGH.html、TESTING-CHECKLIST.md、.claude/skills/qa-check/SKILL.md
- 处置: 内容是否已被 docs/00~04 覆盖, 删除是否有意——待用户拍板后提交

## [Q-05] 根目录 SQLite 文件来源

- 状态: ✅ 已验证
- 结论: 根目录与 src/NitroGateway.Webapi/ 下各有一个 `nitrogateway.db-shm`(SQLite WAL 共享内存, 6/24 生成), **均已被 git 跟踪提交**(历史残留, db 本体已不存在); .gitignore 只忽略 `*.db`, 未覆盖 `*.db-shm`/`*.db-wal`
- 处置: 建议删除两个 db-shm + .gitignore 补充 `*.db-shm`/`*.db-wal`(待用户确认)

## [Q-06] 健康检查 Degraded 语义

- 状态: ✅ 已验证
- 结论: **设计如此, 非 bug**。MqttHealthCheck: Connected→Healthy; Connecting/Reconnecting→Degraded; 其他→Unhealthy。/healthz 全检查,/readyz 只看 ready 标签(sqlite+mqtt)。MQTT 未启动时 Degraded 表示"重连中, 可用但降级"; FACTORY-TEST 1.1 要求启动顺序正确时 Healthy, 与实现一致
- 处置: 无需修改; 可在 README 健康检查处补充 Degraded 语义说明(可选)

## [Q-07] 数据表结构与字段待确认

- 状态: ✅ 已验证
- 结论: FluentMigrator 迁移 6 个(M001~M006), 表结构确认:
  - measurements(id, device_id, point_id, point_name, raw_value, value, data_type, timestamp, quality, error_msg) + 复合索引(device_id, point_id, timestamp)
  - devices(Id, Name, Description, ProtocolName, ProtocolDialect, Endpoint, ConnectTimeoutMs, RequestTimeoutMs, RetryCount, Status, ConnectionParams)
  - points(Id, DeviceId FK, Name, Address, Description, DataType, Access, Enabled, ScanIntervalMs, Deadband, ScaleFactor, ScaleOffset) + IX_points_DeviceId
  - 另有 forward_buffer(M002)、dead_letters(M004)、alarms(M005)、app_meta(M006)
- 处置: 结论已回填, docs/02 数据模型节可按此核对(如有出入再修)

## [Q-08] 死信 API 前端对接

- 状态: ✅ 已验证
- 结论: DeadLettersView.vue 直接 import `../../api/client`(通用 API 客户端), **无独立死信 API 客户端文件**(api/ 下无 deadletters.ts); 03 清单"走 api/status.ts(死信接口)"描述不实
- 处置: 03 清单 F-39 已修正为"api/client.ts(通用客户端)"; **2026-08-22 死信整条链路已删除**(见 F-15), 本条作废
