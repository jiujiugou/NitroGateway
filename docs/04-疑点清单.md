# NitroGateway 疑点清单(阶段 2)

> 生成时间: 2026-08-04 | 全部为"待验证", 验证后回填结论并同步 02/03

## [Q-01] README 与代码项目数不一致

- 状态: 待验证
- 依据: README 说"14 个项目";slnx 收录 22 个;磁盘另有 `Infrastructure.Sqlite`、`Scheduler`、`Protocol.Mitsubishi`、`Protocol.OpcUa` 4 个 csproj 未入 slnx(Dockerfile 也未 COPY)
- 验证方法: 检查这 4 个项目是否被引用、是否可编译、用途;确认是遗留空壳还是规划中协议

## [Q-02] README 测试数与实际不一致

- 状态: 待验证
- 依据: README 说 128 个单元测试, `dotnet test` 实际 115 通过
- 验证方法: git log 检查测试数量变化;或 README 数字过时, 更新 README

## [Q-03] 无实际 CI 工作流

- 状态: 待验证
- 依据: `.github/workflows/` 为空目录, README 声称有 CI 配置
- 验证方法: 确认是否故意移除;若需要, 补 CI(属开发阶段需求, 入 backlog)

## [Q-04] git 工作区有未提交的文档删除

- 状态: 待验证(对仓库历史)
- 依据: `git status` 显示 7 个文件删除未提交(ARCHITECTURE.md/html、DEMO-GUIDE.md、PRODUCTION-GUIDE.html、PROJECT-WALKTHROUGH.html、TESTING-CHECKLIST.md、.claude/skills/qa-check/SKILL.md)
- 验证方法: 与用户确认这是有意清理还是误删;确认 README 是否已覆盖这些文档内容

## [Q-05] 根目录 SQLite 文件来源

- 状态: 待验证
- 依据: 根目录存在 `nitrogateway.db-shm`;而连接串 `Data Source=nitrogateway.db` 为相对路径, 应生成在 Webapi 工作目录
- 验证方法: 确认是否曾在根目录启动过;检查 .gitignore 是否覆盖 *.db-shm(当前 `*.db` 不匹配 db-shm, 但 git status 未显示, 待查 .git/info/exclude)

## [Q-06] 健康检查 Degraded 语义

- 状态: 待验证
- 依据: MQTT 未启动时 `/healthz` `/readyz` 返回 200 Degraded(非 Healthy 也非 503);FACTORY-TEST.md 要求依赖就绪后 /healthz 为 Healthy
- 验证方法: 启动 MQTT(docker compose up -d)后确认状态变为 Healthy;确认 Degraded 是否符合预期语义

## [Q-07] 数据表结构与字段待确认

- 状态: 待验证
- 依据: 03-功能清单"数据模型"节基于 README 推断, 未逐表核对迁移文件
- 验证方法: 阅读 Persistence/Migrations 下的 FluentMigrator 迁移, 回填表结构与字段

## [Q-08] 死信 API 前端对接

- 状态: 待验证
- 依据: 03-功能清单标注死信管理前端走 `api/status.ts`, 未确认是否有独立死信 API 客户端文件
- 验证方法: 检查 DeadLettersView.vue 的 import 来源, 修正 03 清单