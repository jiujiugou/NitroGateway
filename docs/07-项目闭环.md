# NitroGateway 项目闭环（缺口清单）

> 生成时间: 2026-08-10 | 用途: 说明项目距「闭环」还差什么 | 数据基准: 当日实测 build 0 错误 + UnitTests 375 全绿 + IntegrationTests 40 全绿

## 1. 闭环口径

- 功能闭环: 阶段 2 网关全链路 + B 方案（多现场 → 一中心）路线落地
- 验证闭环: 构建 0 错误、单测/集成全绿、FACTORY-TEST P0 场景实测
- CI 闭环: 提交即自动验证，红绿可控、可追溯
- 交付闭环: 代码入库、文档同步、依赖风险有明确决策
- 演进闭环: ADR 待办清零，或逐条显式「接受不修」

## 2. 已闭合（当日实测）

- 构建: `dotnet build NitroGateway.slnx` 0 错误（20 警告）
- 测试: UnitTests 375/375 + IntegrationTests 40/40 全绿（含 Desktop 新增用例）
- 功能: 采集→SQLite→MQTT→Web/Vue 全链路、安全、可观测、告警、死信、桌面端 P0/P1 均已实现
- 文档: docs/01-06 + FACTORY-TEST(.md/.html) + README + 28 条 ADR + 10 天 worklog 在案
- 已修复: ADR-001~009、013、016~019、022~025、027、028（P1-1/P2-1）

## 3. 未闭环清单

### A. 阻塞级（不做则闭环不成立）

| 缺口 | 现状与位置 | 修复方向 |
| --- | --- | --- |
| A1 CI 必红 | 已修复（2026-08-10，ADR-028 P1-1）: ci.yml 拆双 job——build-server(ubuntu) 构建 Webapi/Ingest/IntegrationTests + 集成测试；build-windows(windows-latest) 全量构建 slnx + 顺序跑两个测试项目。连带修复 ForwarderEngineTests 停机排空 flaky（.NET 10 Task.Run 取消竞态，根因与修复见 ADR-028） | 已完成 |
| A2 工作区未提交 | Desktop 模块、slnx、测试工程 TFM、docs/03 全部未入库（git 提交由用户执行，规则 6） | 用户提交，或授权 AI 代执行并在 worklog 记录哈希 |

### B. 路线级 P0（当前目标）

| 缺口 | 现状与位置 | 状态 |
| --- | --- | --- |
| B1 中心 Ingest | `src/NitroGateway.Ingest` 已实施：订阅 `nitrogateway/+/measurements` → 批量 INSERT OR IGNORE（记录级幂等）→ ingest_* 指标；compose 新增 ingest 服务（5200，Dockerfile build args 切换） | 已修复（2026-08-10，ADR-025 P0） |
| B2 告警上行契约 | 中心订阅 `nitrogateway/+/alarms` → 按 AlarmId UPSERT 中心 alarms（状态迁移覆盖）；现场 MqttAlarmNotifier 契约不变 | 已修复（2026-08-10，ADR-028 P2-1） |
| B3 桌面 P2 待办 | 串口 Modbus RTU、配置界面、元数据同步（ADR-026 P2） | 待办（P2 演进，非 P0） |

### C. 健壮性 P1（设计完成，未实施）

- C1 ADR-011 HTTP 上报通道（设计完成待实现）
- C2 ADR-012 磁盘保护（设计完成待实现）
- C3 EventBridge 帧循环异常后永久停摆，无自愈（ADR-028 P3-1）

### D. 收尾 P2/P3（低危，可显式接受）

- D1 ADR-020 P3×7、ADR-021 P3×6 待处理；ADR-022 P3-6 metrics 决策保留
- D2 WriteGuard 预留未接线（F-28: Webapi 无写端点，Modbus/S7 WriteAsync 无生产调用方）

### E. 验证缺口（事实性）

- E1 未接真实 Modbus/S7 从站: 冒烟中设备 Offline，协议驱动仅模拟/单测覆盖
- E2 FACTORY-TEST T3 8h 长稳未跑（P0 场景）；T4 故障恢复、T5 安全未按手册执行
- E3 桌面端仅 Windows 本机冒烟验证，无自动化 UI 验收

### F. 决策项（G1，需用户拍板）

- F1 NU1903: SQLitePCLRaw.lib.e_sqlite3 2.1.11 高危漏洞 —— 升级依赖违反雷区「不升级依赖包」，需用户明确授权或显式接受
- F2 NU1701: LiveCharts2/SkiaSharp/OpenTK 按 .NET Framework 兼容还原 —— 确认可接受或替换图表库
- F3 docs/interview/** 28 文件（个人面试题库）是否移出仓库（今日清理时已提出待确认）

## 4. 建议闭环顺序

1. ~~修 A1 CI~~ 已修复（2026-08-10）→ 提交 A2 当前工作区
2. 跑 E2: T1/T2 模拟从站端到端（桌面 → broker → Ingest → 中心库）+ T3 8h 长稳，按 FACTORY-TEST 判定表留档
3. 拍板 F1/F2/F3 三项决策
4. 收尾 C/D: 实施或逐条「接受不修」归档，ADR 状态清零

## 5. 最短闭环判定

A（CI 绿：A1 已修复，待 A2 提交入库）+ E（长稳实测留档）+ F（三项决策）完成即闭环；
B 已修复（2026-08-10），C/D 可显式「接受不修」归档，不算缺口。
