# ADR-059: MQTT 转发总开关（运行期启停上云转发）
- 日期: 2026-08-19 | 状态: 已实现（2026-08-19，全量测试 663 通过；未提交，git 提交默认由用户执行）
- 背景: 边缘网关形态下，采集数据经 `DataDispatcher` 双写（本地时序库 + 转发缓冲），`Forwarder` 每 5s 出队经 MQTT 上云。当前无运行期开关——转发只有配置项 `Forwarder:Channels`（mqtt/http/both），想停转发只能改配置重启容器。用户需要 web 面板一个开关，运行期控制是否 MQTT 上云。
- 方案（决策点 ①=B、②=只控 MQTT）:
  - 语义: **关闭 = 照常采集 + 照常写本地 SQLite + 告警/web/SignalR 不受影响；仅跳过 MQTT 通道入转发缓冲** → 无缓冲堆积、不触发死信；恢复后从关闭时刻起续传，不补发关闭期数据。
  - 只控 MQTT: 若 `Forwarder:Channels` 含 http，http 通道不受开关影响（开关仅作用于 mqtt 通道）。
  - 存储: 复用 `app_meta` 键值表（M006），`key='forwarder_mqtt_enabled'`、`value='true|false'`；缺省视为 true（启用）。**不改库结构、不加迁移**。
  - 接口: 新增 `ForwarderController`（复用现有 RBAC）:
    - `GET /api/forwarder/enabled` → `200 { enabled: bool }`
    - `PUT /api/forwarder/enabled`，body `{ enabled: bool }` → `200`
  - 实现位置: `DataDispatcher.DispatchAsync` 入队循环处，对 mqtt 通道检查统一开关接口 `IForwardMqttToggle`（IsEnabled/SetEnabled），关闭则跳过 mqtt `EnqueueAsync`（http 照常）。检查逻辑在 Dispatcher 层共用，Webapi 与 Desktop 两个宿主共享。
  - 双宿主实现（同一接口、两套存储）:
    - **Webapi（Linux 边缘网关/容器形态）**: `IForwardMqttToggle` 实现走 `app_meta`（SQLite，重启保持）；web System 页加 `el-switch`（`web/src/views/System/SystemStatus.vue`），绑定 `GET/PUT /api/forwarder/enabled`；文案提示"关闭后本机采集/本地存储/告警不受影响，仅暂停 MQTT 上云"。
    - **Desktop（WPF 桌面端）**: `IForwardMqttToggle` 实现走 `DesktopSettings`（新增 `ForwarderMqttEnabled` 字段，存 `%LocalAppData%\NitroGateway\desktop-settings.json`）；设置页（`SettingsView.xaml` + `SettingsViewModel`）加开关，复用现有 `IDesktopSettingsStore` 持久化，重启保持。
- 改动文件（预计）:
  - `src/NitroGateway.Persistence/`: app_meta 读写 helper（新，含接口），复用现有 SQLite 连接
  - `src/NitroGateway.Collection/Dispatcher/DataDispatcher.cs`: mqtt 入队前检查 `IForwardMqttToggle`
  - `src/NitroGateway.Desktop/Services/Settings/DesktopSettingsStore.cs`: `DesktopSettings` 加 `ForwarderMqttEnabled`
  - `src/NitroGateway.Desktop/ViewModels/SettingsViewModel.cs` + `Views/SettingsView.xaml`: 桌面端开关
  - `src/NitroGateway.Webapi/Controllers/ForwarderController.cs`（新）+ 服务注册
  - `web/src/api/forwarder.ts`（新）+ `web/src/views/System/SystemStatus.vue`（开关）
  - `tests/`: 单测（开关语义、关闭时 mqtt 不入队且 http 不受影响、app_meta 持久化、DesktopSettings 字段读写）
- 验证:
  - `dotnet build NitroGateway.slnx` 0 错误
  - 全量测试基线 645 + 新增（关键逻辑红绿对照）
  - 冒烟: Webapi 起 → PUT 关闭 → 采集/本地入库正常、MQTT 不发布 → 重启后仍为关闭 → PUT 开启恢复发布
- 后续: 文档确认后实现代码；git 提交默认由用户执行。
