# [002] 补充 CI 工作流（spec — 做什么/为什么）

- 对应待办: [D-02] 补充 CI 工作流(优化, 高) — notes/backlog.md

## 背景（为什么做）

- 来源: 工程优化（开发发现）
- 需求三问:
  - 为什么做: .github/workflows 为空, 机器闸门缺失, 任何改动无法自动构建+测试, 工程基线闸门无法生效
  - 验收标准是什么: 见下
  - 不做会怎样: 回归风险无人把关, 工程基线闸门无法生效

## 现状（2026-08-05）

- .github/workflows/ci.yml 已建立（构建+测试, 测试改为跑 slnx）, 尚未推送触发验证

## 目标

- push 触发 CI, 自动执行 dotnet build + 全量测试, 结果可见, 红即拦截

## 边界

- 做: 收尾 .github/workflows/ci.yml（触发条件/步骤/报告）, 推送验证
- 不做（Non-Goals）: 不接部署/发布流水线; 不新增代码覆盖率门禁

## 验收标准（可测试）

- [ ] AC-1: push 触发 CI, 自动执行 dotnet build + 全量测试 — 验证: GitHub Actions 运行记录
- [ ] AC-2: CI 失败（红）即拦截, 对应待办不得标完成 — 验证: 红/绿状态可见
- [ ] AC-3: 验证结果与 CI 报告地址记录到 worklog — 验证: worklog 有链接

## 决策点（需用户拍板）

- [ ] 决策点 1: 现在是否推送触发 CI 验证?（推送由用户执行; 回复通过即视为确认）

## 工程基线（固定节, 勿删; 不适用项标 N/A）

- [ ] 错误处理: N/A（CI 工作流, 无运行时代码）
- [ ] 健康检查/可观测: N/A
- [ ] 测试: CI 执行全量测试（dotnet test, 跑 slnx）
- [ ] 文档: 验证结果回填 worklog 与 backlog
