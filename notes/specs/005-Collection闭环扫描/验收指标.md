# [005] Collection 闭环补全（验收指标 — 收尾对照）

> 覆盖: P0 批次已交付; P1/P2/P3 批次完成后按本文件续勾, 全勾后提交用户最终验收。

## 自动验收（P0 批次, 已完成）

- [x] 构建: `dotnet build NitroGateway.slnx` → 0 错（原始输出: 验证记录/2026-08-05-build.txt）
- [x] 测试: `dotnet test NitroGateway.slnx` → 全绿（基线 117 → 现 120: 单测 117 + 集成 3; 原始输出: 验证记录/2026-08-05-test.txt）
- [x] 测试锚定: MeasurementWriteHostTests / CollectionEngineTests / PointName_PropagatedToSnapshot / PipelineDispatchIntegrationTests 点名透传断言
- [x] 执行记录回填: 005/tasks.md + worklog 2026-08-05

## 行为指标 + 断言大白话（每条修复在抓什么）

- [x] P0-1 存储写入异常隔离: 存储抛错 → 跳过本批 + 日志「时序库写入失败」+ 网关继续运行
  - 大白话: 写数据时存储突然出错, 网关不能死——要跳过这批、记条日志、继续收下一批。测试: 第一批故意让存储抛错, 第二批必须还能写进去。
- [x] P0-2 云端点名非空: 转发报文 MeasurementRecord.PointName = 点位名称
  - 大白话: 从采集到上报, 点位名字（如「炉温」）必须一路带过去, 不能到云端变成空名。测试: 造一个叫 T1 的点位, 断言快照和转发报文里名字还是 T1。
- [x] P0-3 引擎异常恢复: 采集轮次抛错 → 日志「采集轮次发生异常，5 秒后重试」+ 下一轮继续采集
  - 大白话: 某一轮采集炸了, 引擎不能就此罢工——要报错、歇一下、继续下一轮。测试: 第一轮故意抛错, 断言第二轮仍然发生。

## 红绿对照（证明测试有意义, 非恒真/非走过场）

- [x] 红侧: 临时撤掉三处修复 → 4 条对应测试全红（MeasurementWriteHost / PointName 单测 / CollectionEngine / PipelineDispatch 集成）, 证明每条测试都抓得住它对应的 bug（原始输出: 验证记录/2026-08-05-redgreen-red.txt）
- [x] 绿侧: 恢复修复 + 强制全量重建（--no-incremental, 因中断进程污染增量缓存）→ 120 全绿（原始输出: 验证记录/2026-08-05-redgreen-green.txt）
- [x] 结论: 4/4 测试均通过红绿对照, 验证有可证伪性

## CI 绿勾（独立机器验证）

- [ ] CI 已配置（.github/workflows/ci.yml, push main / PR 触发）; 待用户提交推送后, GitHub Actions 自动重跑 build+test 出示绿勾（回填链接/结果于此）

## 运行时验证（可选, 用户现场验收）

- [ ] 1. 停库/灌满存储 → 观察网关日志与进程存活（P0-1）
- [ ] 2. 订阅 MQTT 主题 → 报文 PointName 为点位名称（P0-2）
- [ ] 3. 断网/制造采集异常 → 日志重试提示且服务持续运行（P0-3）

## 工程基线对照（spec.md 基线逐条勾选）

- [x] 错误处理: P0-1/P0-3 失败路径有日志与降级（P0 批次完成）
- [ ] 健康检查/可观测: P1-2 修复后采集耗时/丢弃计数可观测（待 P1 批次）
- [ ] 测试: P3 缺口按修复项补单测/集成测试（待 P3 批次）
- [ ] 文档: P2 修正后与代码一致（待 P2 批次）

## 结论

- AI 自评: P0 批次通过（2026-08-05, 红绿对照 4/4 + 全量 120 绿）
- 用户最终验收: [ ] 通过 / [ ] 不行（理由: …）