# 2026-08-22

## 当前目标

- 写功能（下发写值到 PLC）已接线落地：数据模型（M013 MinLimit/MaxLimit）→ WriteService（Access/Enabled 校验 + WriteGuard 三级门控 + 工程值反向缩放 + 驱动池长连接写）→ Web 写端点 POST /api/devices/{id}/points/{pid}/write → Web MonitoringView 行内气泡写值（借鉴 ThingsGateway 行内就地输入）→ 桌面 RealtimeView 曲线卡片头行内就地编辑器（确认/取消命令）。工作记录只写本 worklog，未新增 ADR。

## 结论：写功能落地方案（2026-08-22，仅分析未实施）

### 现状盘点（均已核实代码）
- 已就绪：
  - 协议层物理写：`IProtocolDriver.WriteAsync/WriteBatchAsync`（Modbus TCP/RTU、S7、OPC UA 全覆盖，含全部 DataType）；`ReliableProtocolDriver` 重试管线、`ProtocolDriverPool` 按设备长连接复用。
  - 写守卫：`Security/Guard/WriteGuard`（Mode 设备状态 + Range 范围 + RateLimit 变化率三级校验）+ `WriteCommand` DTO + `WriteGuardTests`；`AddNitroSecurity` 已注册（注释明示"预留能力，未接线：Webapi 无写端点、驱动 WriteAsync 无生产调用方"）。
  - RBAC：`Roles.Admin/Operator` 注释已含"写操作"，Viewer 只读。
  - 点位权限：`DevicePoint.Access`（`PointAccess`：ReadOnly/WriteOnly/ReadWrite）已建模，前端 `types.ts` 已有、`PointList.vue` 已有下拉。
  - 审计：`AuditMiddleware` 记录所有 `/api/*`，写操作天然覆盖。
  - 实时回推：SignalR `LiveDataHub` + `OutboxConsumer` + `DeviceStatusDispatcher`（`Measurement`/`DeviceStatusChanged`/`MqttStateChanged`）。
  - 可借鉴链路：`TestConnection`（factory→Connect→Ping）、Desktop `DeviceConnectionTester`、Forwarder `SqliteForwardOutbox` 两阶段提交、Desktop `ConfigSyncOutboxStore`（意图队列可参考）。
- 缺口：无"写执行服务"（未把 校验→取驱动→连接→`WriteAsync`→结果 串起来）、Web 无写端点（docs F-28 待办）、双端无写 UI。

### 落地方案
- 后台（核心，建议放 Collection 模块）：新增 `IWriteService`/`WriteService`：取设备/点位（`IDeviceSnapshotCache`）→ 校验 `Access ∈ {WriteOnly, ReadWrite}` → `WriteGuard.Evaluate` → `driverPool.GetOrCreate(device)` → 确保连接 → `driver.WriteAsync` → `OperationResult` → 审计 + SignalR 回推 `WriteResult`（复用 Outbox Channel）。
- Web：新增 `POST /api/devices/{deviceId}/points/{pointId}/write`（`[Authorize(Roles=AdminOperator)]`，body `{value}`，与 test-connection 同风格）；前端 `devices.ts` 加 `writePoint`，`PointList.vue` / 实时页对非 ReadOnly 点位加"写值"按钮 + 对话框 + `ElMessage` 结果。
- 桌面：进程内注入 `IWriteService`（桌面内嵌整套引擎，无 HTTP、本地会话无鉴权但仍走 WriteGuard）；`PointsViewModel`/`RealtimeViewModel` 加"写值"命令 + 对话框（仿 `PointEditor` 模式），结果提示。
- 二期（可选项，对应 ADR-044 B 阶段预留）：写意图持久化队列 + 回执（设备离线排队、上线补发、回执审计），参照 Forwarder Outbox / Desktop ConfigSyncOutboxStore 模式。

### 三问（一句话）
- 为什么做：设备控制是边缘网关核心能力，当前只有采集无下发，写功能是上位机/管理面标配；验收：Web 与桌面都能对可写点位写值并看到成功/失败回执，WriteGuard 拦截生效、写操作进审计；不做会怎样：只能读不能控，功能闭环缺一环。

### 交付（2026-08-22 下午）
- 已将上述方案整理为可对照实现的指南文档：`docs/14-写功能实现指南.md`（现状盘点 / 后台接线 / Web / 桌面 / 可复用清单 / 测试 / 实现顺序）。用户据此自行实现，未改代码。
- UI 呈现位置结论（已并入 docs/14 §4.0 / §5.0）：主入口 Web=`MonitoringView`（卡片 point-row 行尾“写值”按钮+弹窗）、桌面=`RealtimeView`（工具栏“写值”按钮或 DataGrid 操作列+写值窗口）；次入口 Web=`PointList`、桌面=`PointsWindow`（操作列/工具栏按钮）；可写按 `Access∈{WriteOnly,ReadWrite}` 显隐，控件随 DataType，写后靠 SignalR/EventBridge 自动刷新，写前二次确认。
- 桌面写 UI 骨架已落地（仅 UI 无功能，`dotnet build Desktop` 0 错误、过滤测试 34 通过），位置按用户要求调整为**行业惯例（SCADA/HMI）**：监控/管理界面不堆控制按钮，控制动作“选中对象→操作对话框”。最终形态：`RealtimeView.xaml` 工具栏与点位表保持纯净只读，选中可写点位（`SelectedPoint.CanWrite=True`）时曲线卡片头出现“✎ 写值”按钮（DataTrigger 显隐）→ 弹 `WriteValueWindow.xaml(+.cs)` 写值输入窗（信息只读+新值输入+二次确认）；`PointsWindow.xaml` 不加写值入口。见 docs/14 §5.4。
- 入口可见所需的 VM 接线已补齐（解决用户“没看见”）：`RealtimePointItem.Access + CanWrite`、`RealtimeViewModel.Write` 命令（构造可选注入 `IDeviceDialogService? dialogs=null`，`EditWrite` 弹窗，确认后留 TODO）、`WriteValueEditor`、`IDeviceDialogService.EditWrite` + `DeviceDialogService` 实现 + 测试替身。真正写值业务（解析 InputValue → WriteGuard → WriteAsync → 结果）仍未接（用户只要求 UI）。见 docs/14 §5.4“已补齐的接线成员”。

## 结论：ThingsGateway 参照清单（2026-08-22，本地 clone 对照，未动代码）

对照源：`D:\Code\ThingsGateway`（v12 / .NET 10 / Blazor，已 docker 跑通）。

### 第一优先级（贴合现有缺口，直接可参照）
1. **RPC 反写架构** —— 对应我规划的写功能。参照 `src/ThingsGateway.Gateway.Application/Services/Rpc/RpcService.cs` + `Driver/IRpcDriver.cs`：按"设备→变量→值"批量分发、按驱动分组并发写、逐点位返回 `OperResult`、RpcLog 审计队列。关键设计：`ProtectTypeEnum(ReadOnly/ReadWrite)` + `RpcWriteEnable` 点位级写开关，等价于我已有 `PointAccess`+`WriteGuard`，可对照补"按驱动批量聚合"一层。
2. **内存变量 + C# 表达式** —— 无 PLC 即可联调/造数。参照 `MemoryVariable/`（`MemoryDriver` + `IMemoryVariableRpc` + `MemoryExpressionEvaluatorExtension`，用 Westwind.Scripting 动态编译表达式）。我目前无虚拟设备，调试采集链路只能接真设备，这块收益大、成本低。

### 第二优先级（参考价值高，工作量中等）
3. **GlobalData 事件总线** —— `GlobalData/GlobalData.cs`：进程内静态事件（DeviceStatusChangeEvent / VariableValueChangeEvent / AlarmChangedEvent），任何模块可订阅。我已有 EventBridge/SignalR，可对照是否够用。
4. **VariableSourceRead 连读模型** —— `Model/VariableSourceRead.cs`：把地址连续的变量合并成一次报文读。我已有 `ModbusBatchPlanner`，可对照其通用化程度。

### 第三优先级（阶段二，先不碰）
5. **网关冗余（主备心跳+变量同步）** `Services/Management/Redundant/`
6. **规则引擎（可视化流程节点）** `Services/RulesEngine/`
7. **插件热更新 / OTA** `Services/Management/Update/`

### 一句话
最大直接收益 = ①反写按驱动批量分发（写功能落地时抄） + ②内存变量表达式（造数调试）。①②均与现有架构不冲突，可独立小步落地。

### 修订（同日会话）
- 批量缓存上报已从 ADR-064 第一优先级移除：项目已有对应批量处理链路（`MeasurementWriteHost` 有界 Channel 攒批 + `SqliteMeasurementStore` 单事务批量 INSERT + `Forwarder` 5s 间隔转发），与 TG `BusinessBaseWithCache` 的"内存队列攒批→按间隔批量写/上报"能力等价，已具备无需参照实施；ADR 留删除线痕迹，条目编号顺移（内存变量补为第 2 条）。

## 结论：写值 UI 借鉴 ThingsGateway（2026-08-22，仅记录不实施）

- 借鉴点（已核对源码）：TG 前端"写入变量"入口 = 变量表行内按钮（`VariableRuntimeInfo.razor` 的 `RowButtonTemplate` 内 `PopConfirmButton`）→ 就地气泡内嵌 `Textarea` 输入 `WriteValue` → 确认后 `OnWriteVariableAsync(id, WriteValue)` → toast 结果；无独立写值对话框、无二次确认；权限走 `AuthorizeButton("写入变量")`；写后订阅 `VariableRuntimeDispatchService` 1s 节流刷新回显。
- 决定：我方 Web 端（`MonitoringView` point-row 行尾"写值"按钮）借鉴其**行内就地输入**交互（比弹窗更轻）；WPF 桌面端（`RealtimeView` 曲线卡片头"写值"入口）同样借鉴就地输入形态，套到 WPF 控件（Input 内联 + 确认按钮），不拷贝 TG 实现。
- 差异保留：我方仍保留写前二次确认 + `WriteGuard` 三级校验 + 权限 `Admin/Operator`（TG 无二次确认、仅有按钮级权限）；点位写开关用既有 `PointAccess`（ReadOnly/WriteOnly/ReadWrite）而非 TG 的 `RpcWriteEnable`+`ProtectType`（等价）。
- 备注：TG 桌面端为 Photino（内嵌 Chromium 跑同一套 Blazor UI，非 WPF），故"桌面借鉴"是借鉴其交互模式，非直接复用代码。
- 落地时不新增 ADR（本次只记 worklog）；真正接线写值业务时按 docs/14 执行，实现后从 ADR-064 ① 删条并记 worklog。

## 交付：写功能落地（2026-08-22 晚，已实施）

- 数据模型：`DevicePoint` + `PointEntity` + `DomainMapper` 双向映射新增 `MinLimit`/`MaxLimit`（double?，null=不限）；迁移 `M013_AddPointWriteRange`（PascalCase 列）；`DeviceDto`/PointDto + Web `types.ts` 同步。
- 后台写服务：Device 模块新增 `Writing/IWriteService`（`WriteRequest`）+`Writing/WriteService`——设备/点位解析（IDeviceSnapshotCache）→ Access/Enabled 校验 → 值类型转换（JsonElement/字符串/number/bool 按 DataType）→ WriteGuard 三级门控（Range+RateLimit+Mode，状态以健康监控实时快照为准、无快照回退配置更保守）→ 工程值反向缩放 → 驱动池取长连接（未连接先建连）→ WriteAsync。String 只做在线校验；Bool 按 0/1 过门控、写驱动时转回 bool。`AddNitroDevice` 注册 `RangeValidator/RateLimitValidator/ModeValidator/WriteGuard/IWriteService`（桌面不启用 Security 扩展，故在 Device 模块补齐；Web 与桌面共用同一链路）。Device.csproj 新增 Security + Storage.TimeSeries 引用。
- Web 端点：`WriteController`（`POST api/devices/{deviceId}/points/{pointId}/write`，`[Authorize(Roles=AdminOperator)]`，body `{value}`，200/400 封装 `ApiResponse`）。
- Web 前端：`devices.ts` 新增 `writePoint`（失败从 response.data.error.message 取原因）；`MonitoringView.vue` point-row 行尾对非 ReadOnly 点位加 `el-popover` 行内气泡（Bool→el-switch、数值→el-input-number、String→el-input）+ 确认/取消；默认值取当前实时值。
- WPF 桌面：`RealtimeViewModel` 不再走 `_dialogs.EditWrite` 独立窗口，改为构造可选注入 `IWriteService?` + 行内编辑器 `CurrentWriteEditor/IsWriteEditorOpen/IsWriting` + `ConfirmWriteAsync/CancelWrite` 命令；`RealtimeView.xaml` 曲线卡片头"✎ 写值"按钮旁就地展开输入区（Bool→CheckBox、其余→TextBox + 下发/取消，IsWriting 禁用防重复）。`WriteValueEditor` 重建为 `DeviceId/PointId + BoolValue + IsBool`；旧 `WriteValueWindow` 保留不删（DeviceDialogService.EditWrite 兼容旧路径，RealtimeViewModel 已不再使用）。
- 测试：新增 `WriteServiceTests` 14 例（成功写/缩放反算/Bool/String/只读/禁用/超范围/离线/变化率/设备点位不存在/类型转换失败/驱动建连/驱动写失败）。全量：UnitTests 730 通过（726 基线 + 新 14，工作区另有与本次无关的删除项）、IntegrationTests 51 通过、slnx 构建 0 错误、`vue-tsc -b` 通过。
- 构建修复（G1 已确认）：Device→Security 使 Identity.Core 10.0.11 进入 IntegrationTests 依赖图，直接引用 Logging/DI 10.0.8 触发 NU1605 降级，对齐到 10.0.11（仍满足 OPC Server 的 >= 10.0.8）；`SqliteDeviceRepositoryTests` 的 `TempDeviceDb` 手工建表补 `MinLimit/MaxLimit` 列（与 M013 对齐）。
- 备注：Telemetry 文件轮转测试在并行全量下偶发临时目录清理失败（文件句柄竞争），单跑通过，为既有 flaky、与本次无关。
- 写值编辑器修复（WPF）：①RealtimeView.xaml 写值 TextBox 样式默认 Visibility 误设为 Collapsed（复制 CheckBox 样式漏改），导致数值/字符串点位输入框**永远不可见**、无法写非 Bool 值——已改默认 Visible、仅 IsBool=True 时折叠；②RealtimeViewModel.OnSelectedPointChanged 切换点位/设备时立即关闭写值编辑器，防止把新值误下发给旧点位（控制动作安全）；③Write() 打开编辑器时按当前值预填 BoolValue（与 Web 端预填一致）。桌面工程编译验证 0 错误（用 -o 临时输出避开运行中实例占用的 bin）；Web MonitoringView.vue 已核对用 -if/v-else-if/v-else 正确切换 switch/input-number/input，无同类问题。
- 收尾清理：RealtimeViewModel 移除已死字段 _dialogs/构造参数 IDeviceDialogService? dialogs=null 及其 using（Write 命令已改行内编辑器不再走 EditWrite；IDeviceDialogService/DeviceDialogService.EditWrite 保留兼容旧路径）。清理后重新全量验证：slnx 构建 0 错误、UnitTests 730 通过、IntegrationTests 51 通过、ue-tsc -b 通过。
