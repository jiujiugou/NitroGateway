# 2026-08-27

## 当前目标
- 回写闭环 b 部分：NitroGateway 命令处理器（云→网关→PLC，带回执）已落地（ADR-069）：新 `src/NitroGateway.Command` 模块（8 文件）+ Telemetry（1 Activity + 2 指标）+ 接线（slnx / Webapi / 单测）+ 20 个单测；构建 0 错误、单测 780/780 全绿。剩余跨项目前置：`tools/mqtt-simulator` 命令回执模拟 + 端到端联调；已提交推送（04ac3d0），见文末「提交与推送」节。

## 回写闭环 b 部分：NitroGateway 命令处理器（ADR-069，已落地）
- 三问：为什么做——DoD 5 反向写值闭环的网关侧（云发布 `commands` → 网关写 PLC → 回 `commands/ack`）；验收标准——订阅 commands（QoS1）→ 校验解析 → WriteService 写值 → 幂等回执；不做会怎样——闭环断在网关侧，云侧命令永远等不到回执。
- G1：行为变更（新增下行命令订阅/写值/回执发布）；命令契约以 NitroGateway 侧为准，新增 `commands`/`commands/ack` topic 属 ADR-010 D8 明示的可接受跨项目小改动；接口只增不删（未动 Storage/Abstraction）；依赖包未升级。
- 新模块 `src/NitroGateway.Command`（8 文件）：
  - `GatewayCommand` / `CommandAck`（result 字符串枚举 Success/Failure、error 恒存在、at UTC）/ `CommandAckSerializer`（camelCase：commandId/result/error/at）。
  - `CommandRequestParser`（topic 4 段 + siteId 一致性 + WritePoint + value 非空；value 解包为 CLR 原始类型）。
  - `CommandProcessor`（幂等：commandId 去重，重复投递重发缓存回执不重写值；写值异常隔离为 Failure 回执；回执发布失败仅记日志 + 指标）。
  - `CommandHostedService`（订阅 `nitrogateway/+/+/commands` QoS1；wrapper 重连自动重放订阅，首次 Connected 订阅一次；Site:Id 每条命令惰性解析——Program 建表后才写回配置）。
  - `CommandServiceCollectionExtensions.AddNitroCommand()`。
- Telemetry：`GatewayActivities.CommandProcess` + `NitroMetrics.CommandProcessedTotal(result)` / `CommandAckPublishFailuresTotal`。
- 接线：slnx 注册 Command 项目；Webapi.csproj 引用 + Program.cs `AddNitroCommand()`（AddNitroMqtt 之后）；UnitTests.csproj 引用。
- 修一处实现 bug：`CommandRequestParser.Unwrap` 三目 `l : GetDouble()` 公共类型是 double，整数被装箱成 double 42.0 而非 long 42——两侧显式 object 化修正。
- 单测 4 文件 20 用例：`CommandRequestParserTests`（14）/ `CommandProcessorTests`（4）/ `CommandHostedServiceTests`（2）+ `CommandTestFakes`（RecordingFakeMqttClient / FakeWriteService）。
- 验证：`dotnet build NitroGateway.slnx` 0 错误（12 个既有警告）；`dotnet test tests/NitroGateway.UnitTests` **780/780 通过**（含新增 20 个 Command 用例）。
- 已提交并推送（见文末「提交与推送」节）。

## 提交与推送（AGENTS.md 规则 6）
- 用户已授权提交+推送：git add -A → git commit 04ac3d0（feat(edge): 命令回写处理器（ADR-069）+ web 纯边缘化/站点身份/配置同步（ADR-068）+ 桌面 MQTT 连接设置（ADR-067））→ git push origin master 成功（fe01404..04ac3d0）。
- 覆盖范围：今日 Command 命令处理器（ADR-069）+ 08-25 web 纯边缘化/站点身份/配置同步（ADR-068）+ 08-24 桌面 MQTT broker 可编辑（ADR-067）三批未提交工作，一次内聚提交；74 文件，+4013/-2396。
- 分支 master、远程 origin（https://github.com/jiujiugou/NitroGateway.git）；git status 干净。