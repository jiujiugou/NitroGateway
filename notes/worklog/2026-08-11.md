# 2026-08-11

## 当前目标
职业方向已定：社招上位机/工业软件岗位（门槛低、与 .NET + 工业协议背景匹配）。NitroGateway 演进路线：B 方案（多现场 → 一中心 MQTT 上报）为主。当前进度：桌面端 P0/P1 已实施（ADR-026，2026-08-10）；中心 Ingest P0 已实施（ADR-025，2026-08-10：订阅 → INSERT OR IGNORE → 指标 + compose 服务）；告警上行契约已对齐（ADR-028 P2-1）；CI 双 job 拆分 + 停机排空 flaky 修复完成（ADR-028 P1-1，2026-08-10，并行 slnx 12 轮全绿）；HTTP 上报通道 + 磁盘保护 + payload v 字段落地（ADR-011/012/025 P1，2026-08-10）；桌面端本地设备/点位配置界面已实施（ADR-029 阶段 1，2026-08-10：设备 CRUD + 点位管理 + Modbus TCP/RTU/S7 表单）。下一步：E 验证（T1/T2 模拟从站端到端 + T3 8h 长稳）、F 决策拍板（NU1903 依赖升级 / NU1701 图表库 / docs/interview 移库）、B3 桌面剩余（串口 Modbus RTU 现场实测、阶段 2 中心配置导入）。git 提交/推送默认由用户执行，明确指示时 AI 可代执行

## 提交与推送（2026-08-11，用户指示 AI 代执行）
- 提交 b12757e: feat: HTTP 上报通道 + 磁盘保护 + payload v 字段 + Transport/Storage 修复（ADR-011/012/020/021/025 P1/028 P3-1）——61 文件（+1871/-182），含 docker-compose.center.yml 中心形态部署
- 提交 db172e0: feat: 桌面端本地设备/点位配置界面（ADR-029 阶段 1）——22 文件（+1638/-6）
- 分支/远程: master → origin（https://github.com/jiujiugou/NitroGateway.git）
- 结果: 推送成功（fe5d017..db172e0），工作区已清空；推送前已验证 build 0 错误 + Unit 414 + Integration 43 全绿

## 扫描: Collection 模块可维护性（2026-08-11）
- 范围: src/NitroGateway.Collection 全部源码 + 子模块 DESIGN/README + 相关测试（ChannelDrain/DataDispatcher/CircuitBreaker/Pipeline/DeviceCollector）+ 依赖方向（Collection→Host）
- 结论: 无 P1 级新问题（ADR-016 的 10 条已闭环，本轮未复现）；P2×3 + P3×6 新条目
- 产出: notes/ADR/ADR-030-collection-maintainability-review.md
  - P2: DispatchAsync 返回值形同虚设且与 DESIGN 矛盾（入队失败被掩盖）；Dispatcher/DeviceReader 子 DESIGN 文档漂移漏标；熔断注册表/死区缓存无清理随删除增长
  - P3: 墙钟做熔断超时、CollectionEngine 跨线程字段竞态、Collection→Host 依赖倒置、通道解析两处重复、Closed 态 RecordFailure no-op 语义隐晦、零碎一致性问题（含无 .editorconfig）
- 亮点: Options 校验 + ValidateOnStart、每轮独立 scope、drain 协调（ADR-016 P1-1）、热路径日志已降 Debug、关键路径均有测试
- 验证: 纯 review 无代码改动，未跑测试

## 修复: 日志刷屏 + 连接池长短连接（ADR-030 L1/P1/P2/P3，2026-08-11）
- 问题: ① 1s 轮询下 Info 级刷屏——"采集轮次"每秒 1 条、"设备 N 没有有效点位数据"WRN×9 台×每秒、"Modbus 连接成功"故障设备每轮重连各 1 条、"HealthListener 数量"每次健康变更 1 条；② 连接池——DeviceConnection.RetryCount/RetryIntervalMs 配置不生效（ReliableProtocolDriver 硬编码 3 次/500ms），健康 Offline（熔断 Trip）后池中 Faulted 驱动/socket 不释放；故障设备熔断前每轮重建 TCP 连接属"超时后 socket 状态不可信必须重连"的正确设计，但缺注释且连接日志 INF 刷屏
- 改动: ① ModbusTcpDriver 连接成功、DeviceCollector 空点位跳过、DeviceHealthMonitor 监听器数量三处降 Debug（失败路径 WRN 保留）；② ProtocolDriverFactory 注入 connection.RetryCount/RetryIntervalMs（RetryCount=0 支持不重试）；③ CircuitBreakerHealthListener 注入 IProtocolDriverPool，Offline→Trip 时一并 Evict 释放连接（HalfOpen 探测时重建）；④ ModbusDriverBase 全失败复位 Faulted 处加设计意图注释（重连频率由熔断器兜底）
- 测试: 新增 CircuitBreakerHealthListenerTests（Trip+Evict / Online 不 Evict）+ ProtocolDriverFactoryTests（重试次数配置生效 / RetryCount=0 不重试）共 4 例；单测 418 全绿（DesktopViewSmokeTests 2 例偶发 WPF 渲染 flaky，重跑全绿）、集成 43 全绿、build 0 错误
- 未提交: git 提交由用户执行（工作区另含此前未提交的 DeviceCollector "采集轮次"降 Debug 改动，已保留）

## 修复: 空点位设备连接判定（ADR-030 L2，用户决策，2026-08-11）
- 决策: "重连机制连接不上后统一判离线；空设备也重试，连接不上就是离线"
- 改动: DeviceReader 移除"无启用点位直接返回空列表"短路——空点位设备仍从连接池取驱动并尝试连接/复用长连接（Modbus/S7 驱动对空点位列表返回空成功）；连接失败经 ReliableProtocolDriver 重试（RetryCount）上报失败 → DeviceCollector ReportFailure → 连续 3 次判 Offline → 熔断 Trip + Evict（上一轮修复）
- 语义: 连接成功+空列表 = 在线（无数据可读但可达）；连接失败 = 离线，与有点位设备统一
- 测试: 新增 DeviceReaderTests 3 例（空点位仍走连接池返回空成功 / 空点位连接失败返回 Failure / 有点位正常读取）；单测 421 全绿、集成 43 全绿、build 0 错误
- 未提交: git 提交由用户执行

## 修复: 健康检查日志带设备名字与 ID（2026-08-11）
- 问题: 健康变更（离线/恢复）与 SignalR 推送日志只打 {DeviceId}，排查时需另查设备表
- 改动: 设备名从采集器（DeviceCollector 已有 Device 对象）沿 IHealthReporter → IDeviceHealthMonitor 上报链传入 DeviceHealthMonitor，日志统一为 "设备 {DeviceName} [{DeviceId}] …"；DeviceHealthChanged 事件新增 DeviceName 字段，DeviceStatusDispatcher / PersistenceListener / DeviceManager 状态变更日志同步带名字
- 接口: IHealthReporter.Report / IDeviceHealthMonitor.ReportSuccess/ReportFailure 增加 string? deviceName 参数（模块内接口，增量变更；Storage/Protocol 纯接口未动）
- 测试: DeviceHealthMonitorTests 断言事件 DeviceName 透传；单测 421 全绿、build 0 错误
- 未提交: git 提交由用户执行
