# 2026-08-14

## 当前目标

- **（当前，2026-08-14）ModbusSlaveSim 程序化从站模拟器落地，替代 GUI 手动搭 40+ 窗口。** 已在 `D:\resource\Modbus.project` 新增 `ModbusSlaveSim`（.NET 8、零依赖）并加入 `Modbus.Project.sln`：CSV 驱动（`points-device-NN.csv`，NN=UnitId）、范围内随机值（CSV 行级 > `--range` > 类型默认）、多 IP（`--ip` 可重复）、断链（`d`）；功能码 01/02/03/04/05/06/0F/10 + ABCD 大端 + 异常回填。`dotnet build Modbus.Project.sln` 0 错误；hsl-probe 10 从站全通 `seedFail=0 readFail=0 SEED-OK`；docs/09 已重写为主方案（GUI 手动法降为附录 A）、FACTORY-TEST.md §14 已更新。下一步：用户注册 10 设备（127.0.0.1:502，UnitId 1..10，DataFormat=ABCD）导入 CSV 跑 T1/T2。
- **（当前，2026-08-14）FACTORY-TEST 验证启动：按用户现有 Modbus Slave（127.0.0.1:502，UnitId 1~10）跑，不重造模拟器。** 产出 `tools/factory_test_points.py` + `tools/factory-test/points-device-01~10.csv`（每台 50 点、9 种数据类型、全功能区），FACTORY-TEST.md 追加第 14 节现场模拟方案；T0 复跑全绿（build 0 错 / 单测 586 / 集成 43 / 前端 build 通过）。下一步：用户在 Modbus Slave 每从站配 4 块（40001×90 / 30001×6 / 00001×2 / 10001×2）→ 注册 10 设备（UnitId 1..10, DataFormat=ABCD）→ 逐台导入 CSV → 跑 T1/T2。
- **（当前，2026-08-14）A 阶段实施完成（ADR-044）：中心 Web 功能裁剪 + 测试连接迁移桌面端。** 用户确认「先按照 a 改动，测试连接写到桌面端，a 的改动要为 b 以后的改动顺应」。A 全部完成：后端暴露部署形态 + center 模式兼容、前端按 mode 裁剪、桌面 DeviceEditor 测试连接；build 0 错误 + 单元 586 全绿 + 集成 43 全绿 + 前端 build 通过（见下）。为 B 阶段（中心写=意图+下发+回执）预留 `DeploymentMode` + `GET /api/status/info` 地基。
- （承接 2026-08-13 冻结态）项目冻结：只修 bug；用户明确指示做 A 阶段，**不再扩范围**。B 阶段（中心写=意图下发）与 C 阶段（权限系统全面升级）待用户后续指示，不在本次实施。
- git 提交/推送默认由用户执行（本日改动未提交，`git status` 见下）。

## 实施: 中心形态 Web 功能裁剪 + 测试连接迁移桌面端（ADR-044，2026-08-14）

- 三问: 为什么做=center 形态下 Web 仍暴露采集侧功能（测试连接/串口/死信/转发/MQTT/熔断状态），中心到不了现场 PLC，页面恒空或调用失败误导使用者；且 `StatusController` 注入采集侧依赖在 center（`isCenter` 跳过了 Forwarder/MQTT/Collection 注册）DI 解析直接 500；验收=center 下 status 不 500、测试连接显式拒绝、前端按 mode 裁剪、桌面可测连接，build+全量测试全绿；不做=中心形态能力边界混乱、误用现场功能。
- G1 确认: 行为变更（center 模式 status/test-connection 行为变化、前端 UI 裁剪、桌面新增测试连接），动手前已与用户一句话确认做 A，不再扩范围。
- 后端:
  - `Program.cs` 注册 `AddSingleton(typeof(DeploymentMode), deploymentMode)`（enum 必须用 typeof 泛型重载，泛型值重载 CS0452）
  - `StatusController` 采集侧依赖改可空注入（`IForwardBuffer?/IMqttClient?/ForwardingThrottle?/ICircuitBreakerRegistry?`）；新增 `GET /api/status/info` 返回 `{ mode: Gateway|Center }`；`/api/status/system` 在 center 下返回 `Mode=Center, MqttState=Unavailable, BufferBacklog=0, Throttle=0, 空 breakers`（breaker 条件表达式两分支类型不同，用 `Enumerable.Empty<object>()` 或强转匿名类型）
  - `DevicesController` 注入 `DeploymentMode`：`test-connection / serial-ports / serial-port-status` 在 center 返回 400「中心形态无现场通路/串口，请到桌面端测试/查看」
  - 测试: `WebapiControllerTests` 10 处构造追加 `DeploymentMode.Gateway` + 新增 4 例（center 拒绝 / status 空依赖不 500 / info 返回 mode）
- 前端 Vue（`npm run build` 通过）:
  - 新增 `web/src/deployment.ts`：`export const mode = ref<DeploymentMode>('Gateway')`（**必须具名导出**，否则 TS2459）+ `initDeployment()` + `useDeployment()`；`main.ts` `bootstrap()` 先 `await initDeployment()` 再 `app.mount()`
  - `router/index.ts` 死信路由 `meta: { edgeOnly: true }`，守卫 center 重定向 `/dashboard`；`App.vue` 死信侧栏 `v-if="mode !== 'Center'"`、顶栏 MQTT/缓冲段隐藏、center 下跳过轮询与 SignalR；`SystemStatus.vue` MQTT/节流/熔断/串口卡片均 center 隐藏；`DeviceForm.vue` 测试连接按钮与串口可用端口 center 隐藏
- 桌面端 WPF（测试连接迁移）:
  - 新增 `Services/IDeviceConnectionTester.cs`（`ConnectionTestResult(bool Success, long LatencyMs, string? Error, string? Ping = null)` + 接口）与 `Services/DeviceConnectionTester.cs`：注入 `IProtocolDriverFactory`，`ConnectAsync` → `PingAsync`（ADR-023 防假阳性），`RetryCount/RetryIntervalMs` 置 0（`DeviceConnection` 是 record 可用 `with`），异常捕获转结果
  - `Views/DeviceEditorWindow.xaml` 底部改 Grid：左侧 TextBlock（`TestResultText`）展示结果，右侧按钮行加「测试连接」（`TestButton`，取消左侧）；`.xaml.cs` 构造加可选 `IDeviceConnectionTester? connectionTester = null`（冒烟测试兼容，null 时按钮禁用）+ `OnTestConnection` 异步执行
  - `Services/DeviceDialogService.cs` 注入 `IDeviceConnectionTester` 传入窗口；`DesktopServiceCollectionExtensions.cs` 注册 `AddSingleton<IDeviceConnectionTester, DeviceConnectionTester>()`
  - 测试: 新增 `DeviceConnectionTesterTests`（4 例：成功 / Connect 成功 Ping 失败 / Connect 失败 / 不支持的协议不抛异常），复用 WebapiControllerTests 的 Fake
- 语义一致性: 桌面测试连接与 Web `DevicesController.TestConnection` 完全一致（Connect + Ping，Retry 置 0），只是换到有现场物理通路的桌面端执行；`DeviceEditorWindow` 冒烟测试（`new DeviceEditorWindow(new DeviceEditor())`）因新参数可选默认 null 无需改
- 验证: `dotnet build NitroGateway.slnx` 0 错误（警告均为既有 NU1903/NU1701）；`dotnet test tests/NitroGateway.UnitTests` 586/586 通过；`dotnet test tests/NitroGateway.IntegrationTests` 43/43 通过；`cd web && npm run build` 通过（仅第三方 signalr 的 Rolldown PURE 注释警告）
- 为 B 阶段预留: `DeploymentMode` + `GET /api/status/info` 结构稳定，B（中心写=意图落库 + 触发下发 + 回执）复用同一 mode 语义；测试连接/串口永远只在边缘侧
- 未提交: git 提交由用户执行（改动文件见 `git status`：Webapi/Desktop/UnitTests 源码 + web/src 前端 + ADR-044 + deployment.ts + DeviceConnectionTester 等新文件；tools/ 验证截图按仓库惯例不入库）

## 验证: FACTORY-TEST 现场模拟方案落地（2026-08-14）

- 背景: 用户已有 Modbus Slave 模拟器（10 个从站共用 127.0.0.1:502，靠 Slave ID 区分），但缺"多点位 + 多数据类型"。确认主验证路径 = 该模拟器，不重造。
- 关键确认（读代码）：ModbusTCP 驱动靠 `Parameters["UnitId"]` 区分同端口从站（1~247）；点位地址 PLC 风格 1 基（4xxxx 保持 / 3xxxx 输入 / 0xxxx 线圈 / 1xxxx 离散）；`DataFormat` 默认 ABCD（标准大端），CDAB 为低字在前；前端 DeviceForm 已有"从站地址(UnitId)"字段（注释即描述本场景）。
- 新增 `tools/factory_test_points.py`：按每台从站 50 点位生成 10 份 CSV（`tools/factory-test/points-device-01~10.csv`），覆盖 Bool/Int16/UInt16/Int32/UInt32/Int64/UInt64/Float/Double 共 9 种 + 4 功能区（保持 42 点 / 输入 4 点 / 线圈 2 点 / 离散 2 点）；UTF-8 校验通过、地址零重叠（40001~40090 / 30001~30006 / 00001~00002 / 10001~10002）。
- `FACTORY-TEST.md` 追加第 14 节"现场模拟方案"：每从站 4 个 Function 块配置、设备注册参数（Endpoint=127.0.0.1:502, UnitId=1..10, DataFormat=ABCD）、CSV 导入路径（设备详情→点位列表→⬆导入 CSV / `POST /api/devices/{id}/points/import`）、字节序排障（乱码换 CDAB）、备用自动化 `tools/modbus_slaves.py`（独立端口 + 进程级故障注入，T2.4/T4.1 用）。
- T0 复跑：`dotnet build NitroGateway.slnx` 0 错误；单测 586/586；集成 43/43；前端 `npm run build` 通过（仅既有 signalr Rolldown PURE 警告）。未改动 .NET 代码，仅新增 tools 脚本 + 文档。
- 未提交：git 由用户执行。

## 实施: CSV 点位导入迁移（Web → 桌面端）（2026-08-14）

- 三问: 为什么做=用户拍板「批量点位导入是边缘现场能力」，Web（中心）不承担批量写配置，桌面端（边缘/现场离线工具）才需要它；验收=桌面 PointsWindow 出现「导入 CSV / 导出 CSV」且能落地、Web 点位页移除导入 UI、build+全量测试全绿、FACTORY-TEST §14.3 更新；不做=Web 保留导入按钮，与「边缘做配置、中心做管理」的角色划分矛盾。
- G1 确认: 行为变更（Web 删导入 UI、桌面新增导入/导出），动手前一句话说明并经用户确认；**保留 `POST /api/devices/{id}/points/import` API**（桌面本地导入不走 HTTP，此 API 留作集成测试与自动化，不破坏契约）；Web「批量生成」与「导出 CSV」不动。
- 桌面端（WPF）:
  - 新增 `Services/ICsvFileService.cs` + `Services/CsvFileService.cs`（Microsoft.Win32 打开/保存对话框，无额外包；`PickImportCsv` 返回全文/null=取消，`SaveCsv` 写文件返回是否成功）；`DesktopServiceCollectionExtensions.cs` 注册 `AddSingleton<ICsvFileService, CsvFileService>()`
  - `ViewModels/PointsViewModel.cs` 构造注入 `ICsvFileService` + `PointBatchService`（Singleton，复用 Web 后端同款 CSV 解析器，格式天然一致）；新增 `ImportCsvCommand`（选文件→ParseCsv→`IPointManager.ImportAsync`→逐条入 outbox 供配置同步上报 ADR-033 阶段 4→刷新）与 `ExportCsvCommand`（`GetByDeviceAsync`→`ExportCsv`→保存）
  - `Views/PointsWindow.xaml` 工具栏加「⬆ 导入 CSV」「⬇ 导出 CSV」；`Services/DeviceDialogService.ShowPoints` 从作用域解析新依赖传入
- Web（Vue）:
  - `views/Points/PointList.vue` 删除「⬆ 导入 CSV」按钮 + `handleImport`；`api/devices.ts` 删除 `importPoints`（全仓无残留引用）
- 测试: `PointsViewModelTests` +5 例（导入成功含 outbox、取消不导入、坏 CSV 报错、导出写文件、导出取消不报已导出）；新增 `StubCsvFileService` 替身（DeviceConfigTestHelpers）
- 文档: FACTORY-TEST.md §14.3 导入路径改为桌面端 + API 备用
- 验证: T0 复跑见下（build 0 错误 / 单测 / 集成 / 前端 build 全绿）
- 未提交: git 由用户执行。

## 文档: 手动现场模拟搭建指南 (docs/09-现场模拟测试搭建指南.md, 2026-08-14)
- 背景: 用户要求"自己动手搭"，不再由 AI 自动化操作 Modbus Slave；交付一份 GUI 手动搭建手册。
- 内容: 验收标准(10从站×4功能区×非零可辨识值+网关验证)、连接设置(TCP/IP 127.0.0.1:502)、40窗口布局表(F C03 90/FC04 6/FC01 2/FC02 2)、种子值公式表(每块30寄存器, 9种类型)、字节序坑(ABCD/CDAB)、默认窗口Mbslave1与FC03从站1重复导致EXC2的处理、验证两步走(网关桌面端导入CSV + hsl-probe一键SEED-OK)。
- 状态: 仅新增文档，未改代码/架构；git 由用户提交。

## 实施: ModbusSlaveSim 程序化从站模拟器（2026-08-14）

- 背景: 用户要替代 Witte GUI 手动搭 10 从站×4 功能区=40+1 窗口，批量化模拟多数据/多类型/多 IP；三要点=CSV 换点位、范围内随机值、多 IP 多站点。用户确认「直接改项目导入文件测试」。
- G1 确认: 新增独立工具项目（不影响主仓运行时/接口/依赖），属非破坏性新增，直接做。
- 新增 `D:\resource\Modbus.project\ModbusSlaveSim`（.NET 8 控制台，零第三方依赖）:
  - `PointDef.cs` 点位定义（Address/DataType/Min/Max/Enabled）；`PointSource.cs` 扫描 `points-device-NN.csv`（NN=UnitId）
  - `SlaveDataStore.cs` 每从站 4 区存储（FC01/02/03/04）+ 随机值（CSV 行级 > `--range` > 类型默认），客户端写入值不被随机覆盖
  - `ModbusTcpSlaveServer.cs` 多客户端 TCP + 功能码 01/02/03/04/05/06/0F/10，ABCD 大端，异常回填 EXC1/2/3
  - `Program.cs` 参数 `--csv-dir/--port/--ip/--tick/--range`，交互 `d/s/r/q`，stdin 关闭时后台保持运行
  - `README.md` 使用说明；已加入 `Modbus.Project.sln`
- 修复 `tools/factory-test/hsl-probe/Program.cs`（未跟踪文件，原编码损坏且 `.Result` 挂错位置无法编译）：重写后已编译，10 从站 `seedFail=0 readFail=0 SEED-OK`
- 文档: `docs/09-现场模拟测试搭建指南.md` 重写为「ModbusSlaveSim 程序化方案」（保留布局/种子公式/字节序/面试素材/网关验证，GUI 手动法降为附录 A）；`FACTORY-TEST.md` §14 改为首选 ModbusSlaveSim；`D:\resource\Modbus.project\docs\ModbusSlaveSim-设计说明.md` 写设计说明
- 验证: `dotnet build Modbus.Project.sln` 0 错误（仅旧项目 2 个 NU1701 预存警告）；模拟器指向 `D:\Code\NitroGateway\tools\factory-test` 启动 10 从站 500 点，hsl-probe 全通；502 端口测试后已释放
- 遗留: 用户提到的 `SimpleHybirdLock` 老写法——新代码已用 `lock`，旧库暂不动隔离风险；可选 v2：seed 固定公式模式
- 未提交: git 由用户执行（主仓 NitroGateway 与本仓 Modbus.project 均未提交）
