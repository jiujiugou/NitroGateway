# 2026-08-06 工作日志
- 本轮目标: ADR-001 P0-1 / P0-2 修复
- 完成:
  - P0-1① 启动恢复: SqliteForwardBuffer 构造时把遗留 InFlight 全部重置为 Pending（覆盖进程崩溃），恢复失败仅告警不阻断启动
  - P0-1② 出队反序列化恢复: 损坏/为 null 的行重置 Pending + retry_count+1 + last_error，超 _maxRetries 进死信（复用 MarkFailedAsync；恢复自身失败记日志，不影响其余行出队）
  - 顺带根因修复: DequeueAsync 两阶段事务提交后立即释放作用域，否则恢复路径在同一连接上开新事务会异常（此前事务存活至方法结束）
  - P0-2 EnqueueAsync 异常包装: try/catch 统一走 SqliteErrorClassifier("Buffer 入队失败")，与 Dequeue/Commit/MarkFailed 一致；DataDispatcher 优雅降级分支（bufResult.IsFailure）现可达
  - 新增 SqliteForwardBufferTests 6 个单测: 启动恢复 / 损坏负载恢复 / 超限死信 / null 负载 / 入队异常分类 / 正常往返
  - ADR-001 删除 P0-1/P0-2 条目，状态更新为 "P0 已修复，P1/P2/P3 待处理"
- 验证: build 0 错; dotnet test NitroGateway.slnx 全量 136 通过（单测 133 = 127+6，集成 3），基线 130 → 136
- 未完成/阻塞: P1-4~P1-6 等 P1/P2/P3 条目待后续处理; git 提交由用户执行
- 新问题: 无

## 追加: ADR-001 P1-3 修复（Forwarder 失败路径静默）
- Forwarder.cs: Dequeue 失败 → LogError + 返回失败结果（此前吞掉返回 Success）；Commit/MarkFailed 结果检查 IsSuccess，失败记 Error（MarkFailed 失败会卡 InFlight）；新增 MarkFailedOrLogErrorAsync 助手
- 新增 ForwarderFailureTests 6 个测试: Dequeue 失败返回失败结果/记日志、MarkFailed 失败记日志、Publish 失败 MarkFailed 记录原因、Commit 失败记日志、空队列无日志
- Fakes.cs: FakeForwardBuffer/FakeMqttClient 增加失败注入，新增 CapturingLogger
- ADR-001 删除 P1-3 条目，状态更新为 "P0 已修复，P1-3 已修复，其余 P1/P2/P3 待处理"
- 验证: build 0 错; UnitTests 133 通过 + IntegrationTests 9 通过（新增 6）
