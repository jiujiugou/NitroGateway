# 2026-08-29

## 提交与推送记录（AGENTS.md 规则 6，用户明确指示代执行）
- 分支 `master`；远程：`origin`（GitHub https://github.com/jiujiugou/NitroGateway.git）。gitea 无本仓库（ls-remote 认证失败），本次仅推 GitHub；如需 gitea 需先建仓库并加远程。
- 提交：`f50d498` feat(collection): 上行载荷携带点位权限 Access（云端自动注册识别可写点）
- 结果：`origin` `16fe5db..f50d498 master -> master` 推送成功；本地 HEAD 与远程 master 一致。

## 已完成：上行载荷携带点位权限 Access（ADR-016 云端配套）
### 三问
- 为什么做：云端自动注册点位 `Writable` 恒为 false，即使网关已配「读写」，大屏也无写值按钮。
- 验收标准：上行载荷 records[] 带 `access`（ReadOnly=0/WriteOnly=1/ReadWrite=2，JSON 数字）；云端据此回填 Writable 并展示。
- 不做会怎样：回写闭环在自动注册点位上悬空。
### 改动（只增不改；不动库/依赖）
- `Domain/Devices/PointSnapshot.cs`、`Domain/Measurements/MeasurementRecord.cs`：新增 `Access`（默认只读）。
- `Collection/Pipeline/PointValuePipeline.cs` 4 处快照构造 + `Collection/Dispatcher/DataDispatcher.cs` 组装透传 `point.Access`。
- `JsonMessageSerializer` 保持 camelCase 无枚举转换器 → 发出数字 access。
### 验证
- 新增 2 单测（快照透传 + 分发保留 Access，红绿对照）；全量单测 782/782 绿（先停掉锁 DLL 的本地 Webapi PID 49384）。
