# 2026-08-19

## 当前目标

- （当前，2026-08-19）MQTT 转发总开关（ADR-059）已实现（见下方小节），未提交。
- （当前，2026-08-19）CI/CD 流水线扩展（ADR-058）：`ci.yml` 由 CI 扩为 CI/CD —— 新增 `validate-compose`（4 种 compose 形态校验）+ `build-images`（仅 push master / v* tag 且测试全绿时，用 Buildx 构建 gateway/web 镜像推 GHCR，`latest` + `sha-<7>` / `vX.Y.Z`）；新增 `docker-compose.cd.yml`（部署机 `pull` 发布产物，`build: !reset`，不再现场构建）；README 补 CI/CD 章节与 badge；验证：compose 合并 `config -q` EXIT=0、YAML 合法。
- （上一轮，2026-08-19）Telemetry 追踪 File 导出器（ADR-057）：`Exporter=File` 把 span 落盘 `logs/traces/traces-yyyyMMdd.jsonl`，解决"没 collector 看不到 span、日志里也没有"的本地观察问题；构建 0 错误 + 全量测试 633 通过 + 冒烟 50 条 span。
- （上一轮，2026-08-19）Telemetry 追踪执行层启用（ADR-056）：OTLP/Console 导出 + `Telemetry:Tracing` 配置，Webapi/Ingest 已接线，构建 0 错误 + 全量测试 633 通过。
- （上一轮，2026-08-19）Desktop Services 目录重构完成：平铺 24 文件按功能归入 5 个子目录（见下方小节），构建编译通过 + 全量测试 622 通过。
- （下一轮，延续 2026-08-18）中心决策：是否需要多现场中心？是 → 独立建中心项目（中心 webapi + Ingest）；否 → 归档的 ingest/center 代码可删。

## MQTT 转发总开关（ADR-059，2026-08-19）

用户指示「读取 ADR-059: MQTT 转发总开关（运行期启停上云转发）开始实现代码」。已按 ADR 决策 B + 只控 MQTT 实现双宿主总开关。

### 改动
- 新接口 `Storage/Buffer/IForwardMqttToggle.cs`（IsEnabled / SetEnabledAsync / InitializeAsync；接口只增不删）。
- 热路径：`Collection/Dispatcher/DataDispatcher.cs` 在 mqtt 通道入转发缓冲前检查开关（`_forwardMqttToggle is { IsEnabled: false }` 则 `continue`，http 照常、落库照常）；未注册开关（独立测试/旧宿主）视为恒启用。`CollectionServiceCollectionExtensions` 用 `sp.GetService<IForwardMqttToggle>()` 注入（可空）。
- Webapi 宿主：`Persistence/Sqlite/IAppMetaStore.cs` + `SqliteAppMetaStore`（app_meta 键值读写，M006 已建表，不加迁移）+ `SqliteForwardMqttToggle`（key=`forwarder_mqtt_enabled`）；`Program.cs` 迁移后 `InitializeAsync`；新增 `Controllers/ForwarderController.cs`（GET/PUT `/api/forwarder/enabled`，GET 全员、PUT 仅 Admin/Operator）+ `Models/ForwardMqttEnabledDto.cs`。
- Desktop 宿主：`Services/Settings/DesktopSettings.cs` 加 `ForwarderMqttEnabled`（缺省 true）+ 新 `DesktopForwardMqttToggle`（desktop-settings.json 持久化，合并写保留 LogDirectory）；`GatewayHost.cs` 迁移后 `InitializeAsync`；`SettingsViewModel` 加开关属性/命令，`SettingsView.xaml` 加 ToggleButton 开关组。
- Web 前端：`web/src/api/forwarder.ts`（get/set）+ `SystemStatus.vue` 加 `el-switch`（首载读服务端值，失败回滚，3s 轮询不覆盖用户操作）。

### 验证
- `dotnet build NitroGateway.slnx` 0 错误。
- 全量测试 **663 通过 / 0 失败**（基线 645 + 新增 18：DataDispatcher 2、SqliteForwardMqttToggle 9、DesktopSettingsStore 4、SettingsViewModel 3）。关键红绿对照：开关关闭时 mqtt 跳过入队且 http/落库不受影响；`ToggleForwardMqtt_persists_and_shows_status` 曾因 VM 成功路径未显式更新 `ForwardMqttEnabled` 失败，已在成功分支加 `ForwardMqttEnabled = enabled` 修复。
- 备注：早期用 PowerShell 行替换改 `DataDispatcherTests.cs` 把整个文件 UTF-8 中文注释打成乱码并错位换行，已 `git restore` 该文件后重新 apply_patch 干净加回两个测试与 `FakeForwardMqttToggle`；其余文件无乱码（git diff 抽查无锛/鈥/锘）。

### 推送结果（2026-08-19，用户指示「远程推送」）
- 分支/远程：`master` → `origin`（https://github.com/jiujiugou/NitroGateway.git），1 笔提交 `afe873e`（`feat(forwarder): MQTT 转发总开关（ADR-059）运行期启停上云`），已推送（`c15abf4..afe873e`），本地与远端 0 ahead/0 behind。
- 提交内容：Storage 新接口 `IForwardMqttToggle`、DataDispatcher 开关检查、Webapi `app_meta` 持久化 + `ForwarderController`、Desktop 开关（desktop-settings.json + 设置页）、web `forwarder.ts` + `SystemStatus.vue` el-switch、测试 +18（全量 663 通过）。
- 未提交（保留待用户确认）：`docker-compose.center.yml` / `docker-compose.monitoring.yml` 删除（任务开始前已存在的工作区改动，非本次 ADR-059 改动，未纳入提交）。

## Telemetry 追踪 File 导出器（ADR-057，2026-08-19）

用户反馈：ADR-056 之后"追踪在哪里观察？日志里没看出来"。根因：默认 `Exporter=Otlp` 走 localhost:4317 collector，本机无 collector 时 span 被丢弃，且 Otlp 不写 Serilog .log 文件 → 需要"无需 collector、可直接落盘"的观察方式。

改动：
- 新增 `Tracing/FileActivityExporter.cs`（`BaseExporter<Activity>`）：span 以 JSON Lines 追加写 `{LogDirectory}/traces-yyyyMMdd.jsonl`（本地日期滚动）；字段 ts/duration_ms/name/kind/trace_id/span_id/parent_span_id/status/service/tags；线程安全 + 跨日 Flush 切换 + Dispose 冲刷。
- `TelemetryTracingOptions`：`TracingExporterKind` 加 `File=3`；新增 `LogDirectory`（默认 `logs/traces`，环境变量 `Telemetry__Tracing__LogDirectory` 可覆盖）；`Resolve` 同步解析。
- `AddNitroTelemetry(IConfiguration,...)`：`Exporter=File` 分支挂 `SimpleActivityExportProcessor(FileActivityExporter)`；XML 注释同步。
- File 导出器独立于 Serilog：span 写 `logs/traces/`，不混入 `logs/nitrogateway-.log`；`logs/` 已在 .gitignore。

验证：
- `dotnet build src/NitroGateway.Telemetry` 0 错误 0 警告；Telemetry 过滤器测试 14 通过（新增 File 写盘测试）；全量（重定向输出 + `--filter !~DesktopThemeTests`）**633 通过 / 0 失败**。
- 冒烟（`Telemetry__Tracing__Exporter=File` + 临时 LogDirectory + 带设备 DB + `ASPNETCORE_ENVIRONMENT=Development` 启动 Webapi，端口 5199 避开运行实例 5100）：20s 写出 `traces-20260819.jsonl`，**50 条 span 覆盖全部 8 个名字**（CollectRound/CollectDevice/ReadDevice/Pipeline/Dispatch/Forward/SqliteWrite/MqttPublish），含 trace_id/parent_span_id/service/tags。冒烟临时目录已清理；未停运行中的 Webapi 实例（PID 47884）。
- 复检注意：运行实例锁 `bin/Debug`，构建/测试一律重定向输出；临时测试输出目录的 appsettings.json 是 Ingest 的 copy-conflict 胜者，冒烟时用 Webapi 源 appsettings.json 覆盖。

## Desktop Services 目录按功能分组（2026-08-19）

用户指示：`src/NitroGateway.Desktop/Services` 按文件类型/功能多划分几个文件夹。将原平铺 24 文件归入 5 个子目录，命名空间同步为子目录名（与仓库「目录 ↔ 命名空间」约定一致，见 Messaging/Hosting）。

### 分组

- `Services/Dialogs/`：WPF 对话框/文件对话框（DeviceDialogService、AlarmRuleDialogService、CsvFileService 及其接口）→ `NitroGateway.Desktop.Services.Dialogs`
- `Services/Sync/`：中心/站点配置同步 ADR-033/036（CenterConfigClient、CenterConfigImporter、CenterSnapshotDtos、CenterSyncSettingsStore、ConfigSyncOutboxStore、SiteConfigSyncService、SiteIdProvider、SiteSettingsStore）→ `...Services.Sync`
- `Services/Settings/`：桌面本地设置存储 + DPAPI 保护（DesktopSettingsStore、DpapiProtector）→ `...Services.Settings`
- `Services/Infrastructure/`：UI 线程/定时器/VM 工厂/图表工厂（UiDispatcher、DispatcherUiTimer、PointsViewModelFactory、RealtimeChartFactory 及其接口）→ `...Services.Infrastructure`
- `Services/Connectivity/`：设备连接测试（DeviceConnectionTester）→ `...Services.Connectivity`

### 改动

- 移动 24 文件 + 更新各自 namespace；根 `NitroGateway.Desktop.Services` 不再有类型。
- 更新全部引用 using：源 15 个（DesktopServiceCollectionExtensions、Hosting×2、ViewModels×12，含 Services 内部跨目录引用 DeviceDialogService/PointsViewModelFactory/CenterSyncSettingsStore）+ 测试 22 个。
- 未动任何接口/行为/依赖版本；`Services` 根目录仅剩 5 个子目录。

### 验证

- 编译通过（无 CS 错误）。注意：正在运行的 `NitroGateway.Desktop` 进程（PID 42940）锁定 `bin\Debug` 的 exe/dll，正常 `dotnet build` 仅报 MSB3021 复制错误；未结束该进程，改用临时输出目录构建验证。
- `dotnet test`（临时输出 + vstest 从仓库根运行）全量 **622 通过 / 0 失败**，含 Desktop 各 ViewModel/服务测试。
- 复检：无残留 `using NitroGateway.Desktop.Services;` 与 `namespace NitroGateway.Desktop.Services;`。

## Telemetry 追踪执行层启用（ADR-056，2026-08-19）

用户指示：把 Telemetry 里"空转"的追踪真正用起来。此前 ADR-009 P2-4 决策"预留入口不接执行层"，各模块 `StartActivity` 埋点齐全但生产无监听者 → 返回 null（dormant）。

改动：
- 新增 3 包（锁 1.17.0，未升级现有包）：`OpenTelemetry.Extensions.Hosting` / `Exporter.Console` / `Exporter.OpenTelemetryProtocol`
- 新增 `Tracing/TelemetryTracingOptions.cs`：`Telemetry:Tracing` 段（Enabled / Exporter=Otlp|Console|None / Endpoint / Protocol=Grpc|HttpProtobuf / ServiceName），非法值回退默认
- `TelemetryServiceCollectionExtensions` 新增 `AddNitroTelemetry(IConfiguration, serviceName)`：指标注册 + `AddOpenTelemetry().WithTracing(...)`；`Enabled=false`/`Exporter=None` 保持 dormant（兼容 ADR-009）
- 入口：Webapi（service.name=nitrogateway-webapi）、Ingest（nitrogateway-ingest）；两处 appsettings 加 `Telemetry:Tracing` 段
- 测试新增 11 个（`TelemetryTracingOptionsTests` 7 + `TelemetryServiceCollectionExtensionsTests` 4）

验证：
- `dotnet build NitroGateway.slnx` 0 错误；`dotnet test tests/NitroGateway.UnitTests --no-build` **633 通过 / 0 失败**（基线 622 + 新增 11）
- 冒烟（`Telemetry__Tracing__Exporter=Console` 启动 Webapi）日志出现真实 span（Forward，scope Name: NitroGateway，Recorded/Ok）；冒烟产生的 `smoke.db*` 已删
- 文档同步：`docs/interview/telemetry/` answers/questions/README 更新为"追踪已启用"，ADR-009 各条目已修复；`notes/ADR/ADR-056-otel-tracing-execution.md` 记录方案

## Docker 部署验证（2026-08-19）
用户问「现在这个 docker 部署文件能使用吗」。真实验证（非仅 `docker compose config`）：build + up + 健康检查 + 登录 E2E。
发现并修复 3 处部署缺陷：
1. compose 只强制覆盖 admin 密码，operator/viewer 仍默认测试密码 → 生产校验拒启崩溃循环。修复：`docker-compose.yml` / `docker-compose.center.yml` / `.env.example` 新增必填 `OPERATOR_PASSWORD` / `VIEWER_PASSWORD`（`Security__Users__1/2__Password`）。
2. Kestrel 容器内监听 127.0.0.1 而非 0.0.0.0（`appsettings.json` `Urls=localhost:5100` 压过 Dockerfile 的 `ENV ASPNETCORE_URLS`）。修复：appsettings `Urls` 改 `http://0.0.0.0:5100`。
3. compose/.env 以明文覆盖密码，而代码只认 PasswordHasher 哈希 → 登录 500（`FormatException: not a valid Base-64 string`，`TokenGenerator.IssueToken`→`VerifyHashedPassword`）。修复：`AddNitroSecurity` 配置加载阶段明文归一化（`IsHashedPassword` 按版本字节判定，非哈希一律按明文；生产先拒绝默认测试密码再 `HashPassword` 写回 jwtConfig，TokenGenerator 不变）。
验证：`dotnet build` 0 错；全量单测 **645/645**（新增 3：生产明文默认密码拒启 / 生产明文强密码启动且哈希归一化 / 开发明文不拒）；容器 E2E：healthz/readyz 200、`nitro_mqtt_state=2`、admin/operator/viewer 明文登录 200、token 调 `GET /api/devices` 200、经 web 5170 代理登录 200。
注意：本机 1883 被开发 mosquitto 占用，验证用临时 override 改 1884（`docker-compose.verify.yml` 已删，勿提交）；干净机器直接 `docker compose up -d` 即可。本次改动未提交，git 提交默认留给用户。

## 推送结果（2026-08-19，用户指示「远程推送」）
分支/远程：`master` → `origin`（https://github.com/jiujiugou/NitroGateway.git）
四笔提交（`05670d0..12802b2`，已推送，本地与远端 0 ahead/behind，工作树干净）：
- `965d2d8` refactor(desktop): Services 目录按功能分组（Dialogs/Sync/Settings/Infrastructure/Connectivity）
- `c01156d` feat(telemetry): 追踪执行层启用 OTLP/Console/File 导出（ADR-056/057）
- `1e3d7cf` fix(deploy): docker 部署实机验证修复（明文密码归一化/监听端口/账号覆盖）
- `12802b2` chore(persistence): SqliteForwardBuffer 构造器上移整理（ADR-018 P3-5，无行为变更）
.env 未提交（已 gitignore）；.env.example 仅占位值，无敏感信息外泄。

## CI/CD 流水线扩展（ADR-058，2026-08-19）

用户问「如何构建 CI/CD 流水线」。ADR-010 已有 CI（build+test），本次扩展为 CI/CD：
- `ci.yml` 触发：`push: [master]` + `tags: ['v*']` + `pull_request`（PR 只跑 CI 不发布）。
- 新增 `validate-compose` job：CI 占位环境变量下 `docker compose config -q` 校验 `docker-compose.yml` / `+cd` / `center` / `monitoring` 4 种形态。
- 新增 `build-images` job（CD）：`needs: [validate-compose, build-server, build-windows]` 测试全绿才发布；Buildx + `type=gha` 缓存 + GHCR 登录用 `secrets.GITHUB_TOKEN`（默认可用）；镜像 tag：master → `latest` + `sha-<7>`，`vX.Y.Z` tag → `vX.Y.Z` + `sha-<7>`。
- 新增 `docker-compose.cd.yml`：部署机 `docker compose -f docker-compose.yml -f docker-compose.cd.yml pull && ... up -d`，`build: !reset` + `pull_policy: always` 覆盖镜像来源（Compose v5.1.1 支持，实测合并通过）；mqtt/端口/卷/环境变量仍由 base 定义；本地开发仍 `docker compose up -d`。
- README：补「CI/CD」章节 + badge + 测试计数 115→645 修正（实际 645，旧文案过时）。
验证：`docker compose -f docker-compose.yml -f docker-compose.cd.yml config -q` EXIT=0（合并后无 build、image 指向 GHCR）；4 个 YAML + workflow PyYAML 可解析（`!reset` 除外，属 Compose 自定义 tag）；workflow 推送后到 GitHub Actions 复核 `build-images` 只在 master push / v* tag 触发。

### 推送结果（2026-08-19，延续用户「远程推送」指示）
分支/远程：`master` → `origin`，1 笔提交 `b7ced7f`（`ci: 新增 CD 流水线（GHCR 镜像发布 + compose 校验，ADR-058）`），已推送（`0f4bdaa..b7ced7f`），本地与远端同步。文件：ci.yml / docker-compose.cd.yml / README.md / ADR-058 / 本 worklog。首次推送后到 GitHub Actions 观察 `build-images` 是否在 master push 触发并出镜像。
