# 2026-09-01

## Collection 模块完毕后复核（problem-complexity skill 调用）
- 方式：完毕后复核。仓库无启动前风险画像/开工约束沉淀，约束从既有 ADR 重构；本次为首次实况打分，无预测档位可比。
- 验证：Collection 相关单测 91 通过 / 0 失败；最近一次采集改动 f50d498（Access 上行）只增不改、有测试。

### 五因子实况评分（乘积 4×4×4×5×4 = 1280 → XL）
| 因子 | 分 | 证据 |
|---|---|---|
| 规模 | 4 | 21 文件 8 子域；跨模块核心；改动公共记录类型 PointSnapshot/MeasurementRecord |
| 约束 | 4 | 适用 ADR 016/030/031/053/062/059/012/011/035；MQTT payload 协议/数据格式兼容 |
| 故障影响 | 4 | 数据管道首环；DispatchAsync 恒 Success 掩盖缓冲失败、Channel 满丢弃、磁盘降级跳写 |
| 生命周期 | 5 | 平台级核心，影响后续所有协议/点位/数据流迭代（ADR-053/062 杠杆点） |
| 耦合度 | 4 | 依赖 Domain/Shared/Storage/Device/Host/Telemetry + 事件/消息链；Collection→Host 依赖倒置 |

### 约束兑现核对（已实施 ADR 决策全部落地）
- ✅ drain 协调(016)、Options 校验(016)、死区抑制(053)、点位级降频(062)、空点位真实探活(031)、磁盘降级(012)、MQTT 转发开关(059)、站点 ID 上行(035)
- ✅ AGENTS.md：Domain 未引用基础设施；Access 改动只增不改、无 DB 迁移、加测试
- ⚠️ 文档纪律：Dispatcher/DESIGN.md 与 DeviceReader/DESIGN.md 漂移未标注（ADR-016 只标了 root 与 Pipeline）

### 遗留偏差（ADR-030 待处理项，2026-08-31 已登记，仍全部在案）
- P2-1 高危：DataDispatcher.DispatchAsync 恒返回 Success，转发入队失败被掩盖（熔断/指标仍记 success）
- P2-3 熔断注册表/ChangeDetector/管道 _lastValues 无清理，设备增减场景内存增长
- P3-1 CircuitBreaker 用 DateTime.UtcNow 墙钟做冷却/探测超时
- P3-2 CollectionEngine _roundCts/_currentRound 跨线程字段无同步
- P3-3 依赖方向倒置 Collection→Host（CollectionEngine 依赖 GatewayLifecycle）
- P3-4 转发通道解析与 ForwarderOption 重复

### 下一步
- P2-1 优先（XL 档高风险偏差，数据丢失被掩盖；方案建议 review-agent 复核）
- P2-3 / P3-1~P3-4 择期集中处理，或立 ADR 跟踪

## OPC UA 四层生产化封装审查（problem-complexity skill 调用，审查产出）
- 产出：`docs/07-OPC-UA四层生产化封装审查与实施计划.md`（审查 + 封装方案，非实现）。
- 现状：层1 基础通信约 80% 完成（Browse/NodeClass/AccessLevel 缺）；层2 订阅全部缺（能力声明 true 但引擎轮询）；层3 仅整轮重建 Session 式重连；层4 AutoAccept 未关、凭据未接。
- 复用边界：SDK 1.5.378.156（包内 XML 核对 API）已提供 Browse/Subscription/MonitoredItem/KeepAlive/SessionReconnectHandler/TransferSubscriptions/UserIdentity/CertificateValidator——全部复用不重写。
- 优先级：P0（Browse+前端点选 / 策略+用户名密码可配置）→ P1（订阅接入 / 会话自愈）→ P2（证书白名单+信任流程）。
- 逐项打分：W1=72 M、W5=72 M；W2/W3/W4=324+ XL，均需拆子步骤 + ADR。
- 待办：用户拍板先做哪个阶段；订阅接入与会话自愈需先立 ADR（订阅/轮询并存、凭据不落明文、重连状态机对齐 HealthMonitor）。

## ADR-070 层次1 Browse 实施闭环（docs/07 P0-1）
- 范围：只做层1 P0-1（Browse 节点浏览 + 后端 API + 前端树点选），闭环 ADR + 实现 + 验收标准。
- ADR：`notes/ADR/protocol/ADR-070-opcua-layer1-browse.md`（重写——原文件中文被写坏成 `?`）。
  关键决策：D1 单层非递归、parent 缺省 Objects；D2 复用 SDK Browse/BrowseNext 分页；
  D3 变量补读 DataType+AccessLevel → TypeName(11 种)/Access；D4 失败不置 Faulted、_gate 串行；
  D5 Webapi 走驱动池（WriteService 同范式）；D6 前端 el-tree 懒加载点选回填；
  D7 nsu= 暂缓（P3）。
- 实现：`IBrowseableDriver`+`BrowseNode` 下沉到 `Domain/Protocols`（装饰器需转发）；
  `OpcUaDriver.BrowseAsync`（分页+批量补读属性）；`ReliableProtocolDriver` 转发；
  `DriverCapability.SupportsBrowse`；Webapi `OpcUaBrowseController`（404/400/建连）；
  前端 `BrowseNode`/`browseNodes`/PointList 浏览树。
- 测试：集成 3 条（根浏览/变量字段/非法父 Validation+不置 Faulted）全过；
  单测 6 条控制器 + 2 条装饰器转发 + 未连接 Unavailable + SupportsBrowse。
- 验收：`notes/AcceptanceCriteria/ADR-070-opcua-layer1-browse.md`。
- 验证：slnx build 0 错；`npm run build` 0 类型错误；单元/集成测试全绿。
- 未做（范围外）：P0-2 安全策略、P1 订阅、P2 证书、P3 nsu=。

## AC 规范重写（REMADE.md → Agent 执行规范）
- 用户定调：AC 文档是给执行 Agent 读的，不是给人看的。按大模型阅读/执行风格重写 `notes/AcceptanceCriteria/REMADE.md`。
- 结构：生成规则（写 AC：命名/范围/可判条件/怎么验/证据粒度/验证命令）＋执行规则（跑 AC：按序执行/取证/范围外核对/只读不改）＋禁止（自证、凑 PASS、源码像就算过、无证据判 PASS）＋完成定义。
- 把本轮讨论的失败模式写成硬约束：测试必须独立于验收、UI 类仅源码检查须标注、范围外须核对。

## AC-opcua-layer1-browse 按更新后 REMADE 规则补全并复核
- 复核 ADR-070 层次1 Browse 实现（驱动/装饰器/控制器/前端已存在、未提交）：逐条对照 AC 全数通过。
- 验证：slnx build 0 错；单测 791 通过（含 BrowseAsync_NotConnected_ReturnsUnavailable 等）；
  集成 54 通过（含 Browse_Root/Folder/InvalidParent 3 条）；npm run build（vue-tsc+vite）0 类型错误。
- AC 文档补全（REMADE.md 新规则）：W-1/F-1/F-2/F-3 缺检验方法 → 补 **PASS** 证据（源码检查+测试名+构建）；
  全部条件勾选 [x]。

## AI 工程产出率月度分析工具（tools/ai-metrics）
- 新增 `tools/ai-metrics/ai_productivity.py` + README：只读 Codex 会话库/会话 JSONL/Git 日志，统计月度 AI 工程投入产出。
- 8 月（NitroGateway）关键数据：185 个用户工程任务、完成 180（97.3%）、无中断一次通过 133（71.9%）、中断/返工 52（28.1%）；token 11.4 亿；真实构成=缓存命中输入 95.6% / 未命中 3.3% / 输出 1.1%；估算成本 ¥172（低谷）~¥344（高峰），以 DeepSeek 账单为准。
- Git：86 提交（feat 29/fix 9/docs 37）；新增代码 +30,298、测试 +19,494（测试:代码≈64%）、文档 +10,327；修复类提交 21。
- 优化抓手：中断线程占 39% token、>5 轮任务占 60% token（大头为缓存命中输入，主要耗时长）；成本极低因缓存命中率高。
- 未提交，留待用户确认。
