# 2026-08-08 工作日志

## 当前目标
- 异常处理与限流机制现状文档（docs/05-异常处理与限流.md），作为讨论基线

## 文档: 异常处理与限流机制现状梳理
- 应约梳理全项目异常处理 + 限流机制，产出 `docs/05-异常处理与限流.md`（带代码定位的讨论基线）
- 覆盖: 错误模型（OperationResult/OperationalError 双层通道）、异常处理链路（协议→采集→存储→事件→转发→API→生命周期）、6 个限流/节流点（登录限流 / 转发 AIMD / 采集并发 / 写变化率 / 告警去抖 / 协议串行化）、背压丢数据策略、8 个开放讨论点
- 纯文档改动，无代码变更；未跑测试（不涉及代码）
- 待聊的开放点: API 无全局限流、RateLimitValidator 命名歧义、业务失败 200 包裹、时序 Channel 丢数 vs 转发不丢数不对称、ForwardingThrottle 全局共享、熔断不持久化、单轮异常 5s 重试空转、登录限流单实例

## 当前目标
- ADR-014 appsettings 配置漂移修复（健康检查键名 / Collection 配置接线 / Serilog 去重）

## 修复: ADR-014 appsettings 配置漂移
- 问题（原 ADR-014，已删除）:
  1. `Program.cs` 读 `Persistence:DbPath`（不存在），sqlite 健康检查拿到空连接串
  2. `Collection` 段 4 键只有 `IntervalMs` 生效，熔断器冷却/并发上限硬编码
  3. `Logging` 与 `Serilog` 重复配置
- 改动:
  - `src/NitroGateway.Webapi/Program.cs`: 健康检查改读 `Persistence:ConnectionString`（fail-fast）；`builder.Logging.ClearProviders()` 让 Serilog 成为唯一日志输出；`AddNitroCollection` 改传 `builder.Configuration`
  - `src/NitroGateway.Collection/CollectionServiceCollectionExtensions.cs`: 签名改为 `(IConfiguration)`（与其他 AddNitro* 一致），全字段绑定 `IOptions<CollectionOption>`；`CircuitBreakerRegistry` 冷却时长取配置；`DeviceCollector` 并发上限取配置；删除过时注释代码
  - `src/NitroGateway.Webapi/appsettings.json`: 删除 `Logging` 段（Serilog 段为唯一日志级别来源）
  - 顺带对齐同一批未提交“Collection 配置化”改动的遗留断裂: `tests/NitroGateway.IntegrationTests/CollectionEngineTests.cs` 构造函数改为 `Options.Create(new CollectionOption{...})`（预存在编译错误）
- 测试: 新增 `CollectionOptionsWiringTests` 3 个（全字段绑定 / 熔断时长来自配置（红绿对照）/ 缺省回退默认值）
- 验证: build 0 错误；UnitTests 196 通过（含新增 3 + 恢复编译的 DeviceCollectorMaintenanceTests 3）；IntegrationTests 25 通过
- git 提交由用户执行
## 追加重做: CollectionServiceCollectionExtensions / Program.cs 被回退后重新套回
- 工作区中 `CollectionServiceCollectionExtensions.cs` 与 `Program.cs` 被回退到中间破损状态（签名 `IConfiguration` 但方法体引用不存在的 `options`、`AddNitroCollection()` 无参调用、`Persistence:DbPath` 回退），无法编译
- 重新应用 ADR-014 完整修复：扩展方法全字段绑定 + 熔断器/并发取配置；Program.cs 健康检查读 `Persistence:ConnectionString`、`ClearProviders`、`AddNitroCollection(builder.Configuration)`
- 验证: build 0 错误；UnitTests 196 通过；IntegrationTests 25 通过
