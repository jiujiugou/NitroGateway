# 2026-08-07 工作日志

## 当前目标（由 notes/backlog.md 迁移，2026-08-07 起待办统一由 ADR + 本段承载）
- [s] D-10 Device 模块优化（扫描发现项，优化，中）
- [s] D-02 补充 CI 工作流（dotnet build + test）（优化，高）
- [s] D-08 幽灵目录清理 + Mitsubishi/OpcUa 处置（优化，中）
- [ ] D-09 Collection 模块闭环扫描与验收补全（优化，高）
- [s] D-11 Persistence 模块优化（ADR-002 全部条目已修复，2026-08-07，优化，高）
- [ ] D-06 OT 网络隔离评估（缺陷，中，推迟-生产部署触发）

- 本轮目标: ADR-001 P1 全部修复（P1-4 / P1-5 / P1-6）
- 完成:
  - P1-4 共享 SqliteConnection 跨线程并发：SqliteForwardBuffer / SqliteAlarmRepository / SqliteAlarmRuleRepository 改为每操作独立连接（构造参数由 SqliteConnection 改为 connectionString，参考 SqliteMeasurementStore 模式）；DI 注册移除 Singleton 裸连接；启动恢复/Count 均用独立连接；连接打开失败也归入 SqliteErrorClassifier（Forwarder 依赖 OperationResult 结果路由）
  - P1-5 DataType 硬编码 Float：PointSnapshot 增加 DataType 冗余字段（构造时由 DevicePoint.DataType 填充）；PointValuePipeline 4 处快照构造透传；DataDispatcher.ToBatchMeasurements 改为 s.DataType，不再恒为 Float
  - P1-6 测试缺口：SqliteForwardBufferTests 改为临时文件库（适配每操作独立连接）+ 新增 MarkFailed 超限死信、死信查询/重试/丢弃、Commit 删除共 5 个测试（Enqueue 异常/反序列化恢复/Forwarder 失败路径测试已由 P0/P1-3 会话补齐）；新增 DataDispatcherTests（P1-5 类型透传）+ PointValuePipelineTests 类型透传断言
  - ADR-001 删除 P1-4/P1-5/P1-6 条目，状态更新为 "P0/P1 已全部修复，P2/P3 待处理"
- 验证: build 0 错; UnitTests 140 通过（上轮 133 + 新增 7）；IntegrationTests 9 通过；合计 149
- 未完成/阻塞: P2-7~P2-11、P3-12~P3-14 待后续处理；git 提交由用户执行
- 文档策略: 不做模块级详解文档（docs/modules/Domain.md 已删）；类/方法/属性详情写进代码 XML 注释并随代码维护，已写入 AGENTS.md 轻量规则 7
- 新问题(待确认): ① Domain/README.md 称不依赖任何其他项目但 csproj 实际引用 Shared; ② Alarm.Events.PointStoredEvent 与 Domain.Events.PointStoredEvent 结构相同(重复模型)

## 追加: ADR-001 P2-7~P2-10 修复
- P2-7 文档漂移: Forwarder/DESIGN.md 重写为实际实现（重试计数 ≤5 + 死信；流水线图/示例代码/演进表同步）；Forwarder/README.md 删除「指令下行」声称并标注未实现
- P2-8 积压告警限流: ForwarderEngine 首次超限立即告警 + 之后每 60s 一次，积压回落后重置；新增 ForwarderEngineTests 2 个（持续超限不刷屏 / 回落后重置立即再告警）
- P2-9 Activity 状态: Forwarder 失败路径（Dequeue/Publish/异常/Commit 失败）SetStatus(Error, 原因)，全成功才 Ok；新增 ForwarderActivityTests 4 个（成功/空队列 Ok，Publish/Dequeue 失败 Error+原因）
- P2-10 StatusController.SystemStatus 改 async Task + await GetByStatusAsync（移除 .Result 线程阻塞；Webapi 无测试工程，纯机械改动未补单测）
- ADR-001 删除 P2-7~P2-10 条目，状态更新为 "P0/P1 已全部修复，P2-7~P2-10 已修复，P2-11/P3 待处理"
- 验证: build 0 错; UnitTests 144 通过（上轮 140 + 新增 4）；IntegrationTests 11 通过（上轮 9 + 新增 2）；合计 155
- 未完成/阻塞: P2-11、P3-12~P3-14 待后续处理；git 提交由用户执行

## 追加: 提交推送
- 用户指示提交并推送远程；commit `ff91f01 fix(forwarder): ADR-001 数据可靠性修复（P0/P1 + P2-7~P2-10）`（23 文件, +1320/−103）
- 推送 `2ede7c9..ff91f01 master -> origin/master` 成功；含本 worklog 记录在内的后续小提交另行推送

## 追加: AGENTS.md 规则更新
- 规则 6 改为：git 提交/推送默认由用户执行；用户明确指示时 AI 可代执行，执行后须在 worklog 记录提交哈希、分支/远程与结果（用户要求 agent 也具备「完成推送写入 worklog」的能力）
- 规则首次生效：commit `a7c4565 docs(workflow): AGENTS.md 规则 6 允许 AI 代执行提交/推送并须记录 worklog`，推送 `366021c..a7c4565 master -> origin/master` 成功

## 追加: Persistence 模块扫描
- 全模块只读扫描（22 文件：Sqlite 实现 9 + EF 仓储 2 + 迁移 6 + 工具 5），发现 13 项优化点，已写入 `notes/ADR/ADR-002-persistence-optimization.md`，backlog 登记 [D-11]
- 重点: 告警仓储/测量查询无异常分类且 AlarmHostedService 无容错（P1-1）；measurements 无保留策略（P1-2）；point_name/data_type 写空串（P1-3）；无 WAL 配置（P2-1）；采集每轮 EF 全量加载且 README 的 DeviceCache 不存在（P2-2）；latest 全量查询后内存过滤（P2-4）
- 未处理: ADR-002 各条目待后续排期修复；未提交推送（用户未指示）

## 追加: 删除需求池（backlog）
- 用户拍板方案 B：删除 `notes/backlog.md`，待办统一由 ADR 承载，方向/优先级迁移到本文件头部「当前目标」段
- 未完成条目 D-02/D-06/D-08/D-09/D-10/D-11 已迁移（含状态）；已完成条目 D-01/D-03/D-04/D-05/D-07 不迁移（worklog 历史已覆盖）
- AGENTS.md 规则 4 同步：删除「待办索引写 backlog」改为「当前目标写 worklog 头部，问题写 ADR」

## 追加: 告警仓储 EF 化（ADR-002 部分修复）
- 用户指示：告警两个仓储换 EF Core，其他存储不动，同时修正 ADR-002 相关条目
- SqliteAlarmRepository / SqliteAlarmRuleRepository 由裸 SqliteCommand 重写为 EF Core（新增 AlarmEntity / AlarmRuleEntity + DbContext 映射，列名对齐 M005 迁移）；异常统一走 SqliteErrorClassifier（解包 DbUpdateException），P1-1 告警部分修复
- P2-3 修复：IAlarmRuleRepository 新增 GetByDeviceAsync（AlarmHostedService 改为按设备一次取规则、内存按点位过滤），消除每轮每点一次 DB 往返
- P1-1 配套：AlarmHostedService.ProcessEventAsync 整体 try/catch 单事件容错 + UpdateStateAsync 失败记 Error，SQLite 异常不再击穿后台服务
- DI：告警仓储注册由 Singleton 改 Scoped（适配 EF DbContext；AlarmHostedService 每事件建 scope 解析）
- 新增 SqliteAlarmRepositoryTests 9 个：告警保存/状态更新/活跃与历史查询、规则 upsert/按设备批量/删除、表缺失时返回 OperationResult 不抛异常
- ADR-002 删除 P2-3、P1-1 收窄为测量/缓冲部分；其余条目（P1-2/P1-3/P2-1/P2-2/P2-4/P3）未动
- 未处理: 其余 ADR-002 条目待后续排期；未提交推送（用户未指示）

## 追加: apply_patch 命令行修复
- 根因: PowerShell 5.1 经 cmd.exe 调用 apply_patch.bat 时多行补丁被换行截断；且 PS 5.1 原生传参会静默剥掉内嵌双引号，两种情况都会损坏补丁
- 修复: 新建 `C:\Users\jiuji\Documents\WindowsPowerShell\Microsoft.PowerShell_profile.ps1` 定义同名 apply_patch 函数（优先级高于 .bat），从当前 .bat 动态解析 codex.exe 路径，用 Process.Start + CommandLineToArgvW 转义直调 `--codex-run-as-apply-patch`，多行与内嵌引号均保留；已在新进程验证
- 注意: 补丁 hunk 内每行必须以空格/+/－开头（空行写 +），hunk 语法错误与命令行截断是两类不同报错

## 追加: ADR-002 P1 修复
- 用户指示: 读取 ADR-002 P1 部分并修复
- P1-1 剩余部分: SqliteMeasurementStore.QueryAsync/QueryByDeviceAsync/PurgeAsync 与 SqliteForwardBuffer.GetDeadLetters/Retry/Discard 异常统一走 SqliteErrorClassifier，返回 OperationResult 不再向调用方抛异常；死信三个方法同时补传 ct 给 Dapper（合并 P3-5）
- P1-2: 新增 MeasurementRetentionService（BackgroundService）周期调用 PurgeAsync，默认保留 30 天 / 每 24h 清理一次，失败只记日志不中断、下周期重试；AddNitroSqlite 注册 + appsettings 增加 MeasurementRetentionDays / MeasurementRetentionInterval
- P1-3: WriteAsync 写入 s.PointName / s.DataType.ToString()（修复前写空串，列存在但数据丢失）；QueryAsync/QueryByDeviceAsync SELECT 增加 point_name/data_type 并回填；修复前旧空串数据解析回退默认值不抛异常
- 测试: 新增 SqliteMeasurementStoreTests 6 个（读写回填/旧数据兼容/Purge/表缺失分类）、MeasurementRetentionServiceTests 2 个（周期阈值/失败不中断）、SqliteForwardBufferTests 表缺失分类 3 个
- ADR-002 删除 P1-1/P1-2/P1-3/P3-5，状态更新为 "P1 已全部修复，P2/P3 待处理"
- 验证: build 0 错; UnitTests 164 通过（新增 11）；IntegrationTests 11 通过；合计 175
- 未完成: P2-1/P2-2/P2-4、P3-1~P3-4 待后续处理；未提交推送（用户未指示）

## 追加: ADR-001/002 条目补代码位置
- 按 AGENTS.md 规则 5（代码位置粒度：路径 + 类.方法名，不写行号），给 ADR-001 剩余条目（P2-11/P3-12~P3-14）与 ADR-002 剩余条目（P2-1/P2-2/P2-4/P3-1~P3-4）各补一行「代码位置」，供后续 agent 直接定位
- 用户反馈部分行与标题重复后收敛：AGENTS.md 规则 5 增加「条目标题已含类名/唯一标识的不再重复写代码位置行，仅当标题无类名或涉及跨模块/多类时才写」；ADR-001 删除 P3-13/P3-14 代码位置行，ADR-002 删除 P2-2/P3-1/P3-4 代码位置行，保留 P2-11/P3-12（标题无类名）与 P2-1/P2-4/P3-2/P3-3（跨模块/多类或标题无类名）

## 追加: 新增 optimize 技能（/优化 触发）
- 新增个人技能 `optimize`（UI 显示「优化」），位于 `~/.codex/skills/optimize`，触发方式：`/优化 模块名或文件名`（如 `/优化 Collection`、`/优化 MeasurementsController`）
- 能力闭环：定位目标（scripts/locate_target.py：读 AGENTS.md 模块表 + git 跟踪文件模糊匹配）→ 读取 docs/notes 上下文 → 扫描登记 ADR → 执行优化（修复+测试+build）→ 收尾更新 worklog/docs
- 文档：SKILL.md + references/（scan.md 定位与上下文、optimize.md 执行纪律、notes-docs.md 读写规范）
- 验证：quick_validate 通过；locate_target.py 对 Collection/Persistence/MeasurementsController/SqliteForwardBuffer/web/前端/Domain 等用例测试通过

## 追加: /优化 Protocol（先查 Modbus）→ ADR-003
- 扫描 src/NitroGateway.Protocol/Modbus 全模块，登记 ADR-003（P1-1 批量读类型组错位、P1-2 写入类型回退全按 Float；P2-1 地址偏移回绕、P2-2 String 长度硬编码；P3-1~P3-5 死代码/注释/超时/部分失败）
- 当前仅扫描登记，未修复；待用户确认后按 P1 优先执行

## 追加: /优化 Security（边缘网关适配范围）→ ADR-004
- 扫描 src/NitroGateway.Security 全模块 + Webapi 接线，登记 ADR-004（P1-1 WriteGuard 未接线无写路径、P1-2 密码明文回退；P2-1 登录无限流、P2-2 JWT 配置缺校验、P2-3 Role 不校验、P2-4 审计无异常兜底；P3-1~P3-3 角色字面量/Trim/审计 body）
- 范围纪律：不做用户管理/OAuth/SSO/密码过期等超边缘网关适配范围的设计
- 当前仅扫描登记，未修复；待用户确认后按 P1 优先执行

## 追加: /优化 Storage（纯接口层）→ ADR-005
- 扫描 src/NitroGateway.Storage 全部接口（Buffer/Configuration/TimeSeries），登记 ADR-005（P2-1 IPointRepository 缺批量保存、P2-2 历史查询无分页；P3-1 GetById 文档与实现语义不一致、P3-2 死信展示缺设备名）
- 引用不重复：IMeasurementStore 缺 QueryLatestAsync（ADR-002 P2-4）、IForwardBuffer.Count 同步属性（ADR-001 P3-13）

## 追加: 修复 ADR-005（P2-1/P2-2/P3-1/P3-2）
- P2-1: IPointRepository 新增 SaveBatchAsync（接口只增不删），SqlitePointRepository 单事务 upsert；PointManager.ImportAsync 批量优先、失败回退逐条保留失败点名诊断
- P2-2: IMeasurementStore 新增 QueryPagedAsync（pointId 可空、LIMIT/OFFSET 夹紧 1..1000），SqliteMeasurementStore 实现 + 2 测试；控制器接线未做（历史接口默认全量，避免截断行为变更，待确认）
- P3-1: IDeviceRepository.GetByIdAsync 文档改为「不存在返回 Failure」；P3-2: DeadLetterEntry 注释说明最小字段设计意图
- 测试: 新增 3 个（ImportAsync 批量路径、分页切片、按设备分页）；补 4 处测试 fake 的 QueryPagedAsync 实现
- 验证: build 0 错；UnitTests 167 通过（上轮 164 + 新增 3）；ADR-005 状态更新（P2-2 接线待确认）

## 追加: /优化 Transport MQTT → ADR-006
- 扫描 src/NitroGateway.Transport/MQTT 全模块 + Webapi 接线，登记 ADR-006（P1-1 ClientId 截断恒为 "NitroGat"、P1-2 重连后订阅丢失、P1-3 Faulted 无恢复路径；P2-1 cmd 订阅无消费者；P3-1~P3-5 通道丢弃策略/CTS 泄漏/订阅结果忽略/关停状态/配置一致性）
- 当前仅扫描登记，未修复；待用户确认后按 P1 优先执行

## 追加: 修复 ADR-001 全部条目
- P2-11: SqliteForwardBuffer.MarkFailedAsync 合并为 1 UPDATE（CASE 重试计数+超限进死信）+ 1 SELECT 判断，3 往返 → 2
- P3-12: ForwarderEngine 首轮立即执行（do-while），不等首个 tick；新增 IntegrationTests 用例
- P3-13: IForwardBuffer 新增 GetCountAsync（只增不删），实现 ExecuteScalarAsync；ForwarderEngine/StatusController 改异步；Count 保留兼容并注释；新增 UnitTests 用例
- P3-14: 节流全局共享 v1 单 Broker 接受不修，ForwardingThrottle 注释记录决策
- 验证: build 0 错；UnitTests 168 通过（上轮 167 + 1）；IntegrationTests 12 通过（上轮 11 + 1）；ADR-001 条目全部清空，状态更新

## 追加: 修复 ADR-002 全部条目
- P2-1 WAL/PRAGMA：新增 `SqlitePragmas.Apply`（journal_mode=WAL + synchronous=NORMAL + busy_timeout=5000），MigrationRunner 启动时应用（WAL 库级持久），SqliteMeasurementStore/SqliteForwardBuffer 每连接打开后应用，覆盖 1s 写 + 查询 + Alarm 并发
- P2-2 采集热路径：新增 `IDeviceSnapshotCache`/`DeviceSnapshotCache`（Singleton，TTL 10s 兜底，SemaphoreSlim 防并发重复加载）；DeviceManager.GetAllAsync 改走缓存；注册/注销/状态变更/点位增删改（含批量导入部分成功）均 Invalidate；与 docs/02-架构理解.md「API 改配置 → DeviceCache 内存更新」设计一致
- P2-4 latest SQL 化：IMeasurementStore 新增 `QueryLatestAsync`（接口只增不删）；pointId 非空 ORDER BY timestamp DESC LIMIT 1，null 按 point_id 分组 MAX(timestamp) 取每点最新；MeasurementsController.Latest/LatestBatch 改调，不再拉 1 小时全量内存过滤；新增 2 个单测（单点最新/每点最新）
- P3-1 M003 命名风格：已执行迁移不改列名（破坏既有库），类注释记录历史命名决策
- P3-2 MigrationRunner 日志注入：DatabaseInitializationExtensions 从 DI 取 ILoggerFactory 传入 Run
- P3-3 备份一致性：BackupDatabase 先 `PRAGMA wal_checkpoint(TRUNCATE)` 再 File.Copy；首次运行库不存在不产生空备份
- P3-4 SqliteErrorClassifier：IOERR(10)/CORRUPT(11) 由误标 StorageFull 改归 Storage（消息保留 I/O/损坏上下文），仅 SQLITE_FULL(13) 为磁盘满
- 测试: 新增 3 个（QueryLatestAsync 单点/每点 + ImportAsync 部分成功失效缓存）；补 4 处测试 fake 的 QueryLatestAsync 实现；DeviceManager/PointManager 测试加 FakeDeviceSnapshotCache 与失效断言
- 验证: build 0 错；UnitTests 171 通过（上轮 168 + 3）；IntegrationTests 12 通过；ADR-002 条目全部清空，状态更新

## 追加: ADR-002 P2-2 方案 1（状态与配置解耦）
- 用户确认「数据实时性优先于缓存」：明确缓存只缓存「设备+点位配置」，测量值/最新值完全不缓存
- DeviceCollector 维护过滤改读 `IDeviceHealthMonitor` 实时快照（GetSnapshot 优先，无快照回退配置 Status）；DI 注入 IDeviceHealthMonitor
- StatusController.DeviceSummary 状态同样改读 HealthMonitor 实时快照（回退配置 Status）
- DeviceSnapshotCache/IDeviceSnapshotCache 注释明确「配置缓存，运行状态以 HealthMonitor 为准」
- 新增 DeviceCollectorMaintenanceTests 3 个（HealthMonitor Maintenance 跳过 / HealthMonitor Online 强制采集 / 无快照回退配置 Status）
- 验证: build 0 错；UnitTests 174 通过（上轮 171 + 3）；IntegrationTests 12 通过

## 追加: 修复 ADR-003 / ADR-004

- ADR-003（Protocol Modbus）全部条目处理：
  - P1-1 批读类型组错位：新增 `ModbusBatchPlanner.SplitContiguousSegments`，ReadRangeAsync 类型组内按寄存器连续段切分后再批读（同类型非连续点位不再从首点连读）；基于 ModbusTcpServer 的真实 TCP 回环测试验证非连续同类型点位不串位；顺带移除 ReadRange 死字段 StartOffset/TotalRegisters（P3-2）
  - P1-2 写入类型回退：ModbusRtuDriver/ModbusTcpDriver `WriteSingleValueAsync` 按 DataType 全量映射 HSL 写方法（Bool/Byte/Int16/UInt16/Int32/UInt32/Int64/UInt64/Float/Double/String），不再 Convert.ToSingle 兜底；回环测试覆盖 11 种类型写读
  - P2-1 地址回绕：`ModbusAddressParser.Parse` 校验 1..65536 超限抛 ArgumentException；P2-2 `DefaultStringLength=10` 常量 + 注释说明 v1 固定 10 字符约定
  - P3-1 GetDistance 死代码注释保留（IAddressParser 接口只增不删）；P3-3 MergeRanges 注释修正；P3-4 SerialPortSettings 增加 ReceiveTimeoutMs/ReadTimeoutMs/WriteTimeoutMs 并从 RequestTimeoutMs 透传；P3-5 ReadBatchAsync 部分失败 LogWarning 成功/总数
- ADR-004（Security + Webapi 接线）全部条目处理：
  - P1-1 写门控未接线：确认当前无需写操作 API，注册处标注「写门控为预留能力，未接线」，docs/03-功能清单.md F-28 同步（如需写端点另起任务）
  - P1-2 密码明文回退：TokenGenerator 移除明文 Equals 兜底（仅 VerifyHashedPassword）；appsettings.json 三个开发账号改 PasswordHasher 哈希（明文口令不变：admin/admin123、operator/oper123、viewer/view123）
  - P2-1 登录限流：新增 `LoginRateLimiter`（内存，按「用户名|IP」计数，默认 5 次/10 分钟 → 60s 锁定，429 + 剩余秒数）；P2-2 JWT 密钥 ≥32 字节/ExpireHours ≥1 fail-fast；P2-3 Role ∈ {Admin, Operator, Viewer} 白名单校验；P2-4 新增 `ExceptionHandlingMiddleware`（500 JSON，注册在 Audit 内层防审计丢失）
  - P3-1 Roles 新增 AdminOperator/AllRoles 常量，7 控制器 8 处 [Authorize] 改用常量；P3-2 Login 用户名 Trim；P3-3 审计不记 body（敏感）注释说明
- 测试: 新增 21 个（UnitTests +19：ModbusBatchPlanner 4、ModbusAddressParser 越界 3、LoginRateLimiter 5、TokenGenerator 3、SecurityConfigValidation 4；IntegrationTests +2：ModbusTcpDriver 真实 TCP 回环 P1-1/P1-2）
- 验证: build 0 错；UnitTests 193 通过（上轮 174 + 19）；IntegrationTests 14 通过（上轮 12 + 2）；ADR-003/ADR-004 状态更新为「全部条目已处理」
- 未完成/阻塞: 无；git 提交由用户执行

## 追加: 修复 ADR-005 / ADR-006
- 用户指示: 读取 ADR-005/006 文档并解决这些问题（P2-2 属行为变更，用户指示视为确认）
- ADR-005 P2-2: MeasurementsController.History 增加 limit（默认 1000）/offset 查询参数并改调 QueryPagedAsync；实现侧 LIMIT/OFFSET 夹紧 1..1000 已有单测，控制器为薄接线未另建 Webapi 测试工程
- ADR-006（Transport MQTT）全部条目：
  - P1-1 ClientId 唯一: 只截 GUID 后缀 8 位（修复前整串 [..8] 恒为 "NitroGat"，多实例互踢）
  - P1-2 重连重放订阅: wrapper 记录 topic→qos，ConnectAsync 成功（含重连）后重放
  - P1-3 恢复路径: ConnectAsync 失败确定性启动重连循环（HandleConnectFailure，不依赖事件时序）+ 循环单实例互斥；MqttHostedService 改监督循环（Faulted / Disconnected+已配置重连时周期兜底）
  - P2-1 移除 `nitrogateway/+/cmd` 订阅（全仓无消费者，云端指令走 HTTP，见 Transport/DESIGN.md）；Messages 通道保留给未来消费者
  - P3-1 通道满丢弃: 无下行订阅后无命令消息流入，OnMessageReceivedAsync 注释明确未来命令下行须 WriteAsync
  - P3-2 TryReconnectAsync 退出即释放 _reconnectCts（finally）；P3-3 监督循环常驻；P3-4 DisposeAsync 先置 Disconnected；P3-5 Host/Port 改 init + KeepAliveSeconds 夹紧 [5,3600]
- 工程: MQTT csproj 加 InternalsVisibleTo（NitroGateway.IntegrationTests，与 Modbus/Persistence 同款）；MqttClientWrapper 增加 internal 注入式构造函数（可注入 MQTTnet 内层替身，无需真实 broker）
- 测试: 新增 11 个（MqttClientWrapperTests 8：ClientId 唯一/KeepAlive 夹紧/Host 不可变/重放订阅/首连失败恢复/Faulted 恢复/Dispose 状态/CTS 释放；MqttHostedServiceTests 3：Faulted 监督恢复/Disconnected 周期重试/无订阅调用）
- 验证: build 0 错；UnitTests 193 通过；IntegrationTests 25 通过（上轮 14 + 新增 11）；ADR-005/ADR-006 条目全部清空，状态更新
- 未完成/阻塞: 无；git 提交由用户执行

## 追加: 提交推送（ADR-002~006 优化修复）
- 用户指示提交并推送远程；commit `dc631b1 fix: ADR-002~006 模块优化修复（Persistence/Protocol/Security/Storage/Transport MQTT）`（92 文件, +3720/−479），含 ADR-002~006 各轮优化修复与 ADR/文档/worklog 同步
- 推送 `5bcbc08..dc631b1 master -> origin/master`（https://github.com/jiujiugou/NitroGateway.git）成功；本地与远程已同步
