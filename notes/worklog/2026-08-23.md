# 2026-08-23

## 当前目标

- （已完成，2026-08-23）B1 用户管理（ADR-066）——用户管理不走全量 Identity，DB 化 users 表 + 管理接口：
  - 决策：不引入 ASP.NET Core Identity；在现有 JWT + RBAC + PasswordHasher 之上，把用户从配置文件迁到 SQLite users 表 + Admin 专属管理接口。
  - 文档：`notes/ADR/security/ADR-066-user-mgmt-no-full-identity.md`（决策/为什么/实施/验证标准），ADR-065 交叉引用，ADR README 地图同步。
  - 代码已实施 + 验证通过（见下方已记录条目）；git 提交/推送由用户执行。

## 已记录

- （已完成，2026-08-23）实时监控页（web /monitoring）布局改为「左上选择 + 左数据 + 右曲线」（用户需求）：
  - **改动文件**：仅 `web/src/views/Monitoring/MonitoringView.vue`（前端单文件重写，后端/接口/数据模型未动）。
  - **新布局**：左上选择条放「设备 + 点位」下拉（右侧放连接状态）；左侧「设备点位数据」面板——左上选某设备只显示该设备点位，默认「全部设备」时设备卡片纵排；右侧「点位曲线」面板——ECharts 实时曲线（预载 2h + SignalR 追加、环形缓冲 7200 点，逻辑沿用原实现）。
  - **交互增强**：点击左侧任一点位行 → 右侧直接出该点位曲线并高亮该行（`point-row.plotting`）；点位下拉支持 filterable 搜索；全部设备时点位选项带「设备名 / 点位名」前缀；窄屏（<1100px）左右分栏自动上下堆叠。
  - **验证**：`npm run build`（vue-tsc + vite）通过；起后端 + playwright 无头 Chrome 实测：登录后 /monitoring 布局正常（左 420px / 右 870px 分栏、图表填满右面板）、点点位行/选点位下拉均出曲线、设备筛选生效、900px 宽堆叠正常。验证用的后端实例已停止（端口 5100 已释放；用户原有 5173 vite 未动）。
  - 备注：未动 `*.db`/依赖版本；git 未提交（默认由用户执行）。

- （已完成，2026-08-23）B1 用户管理 ADR-066 前端落地（web 管理页 + 自助改密 + 登出）：
  - **后端支撑**：`UserController` 新增 `GET /api/user/me`（任意已登录角色返回自己的 UserDto，不含密码哈希），供前端显示用户名/角色并做菜单门控；`WebapiAuthorizationTests` 补 1 例（Me 需认证但不带 AdminOnly）。
  - **前端 API**：新增 `web/src/api/user.ts`——`getUsers/createUser/changeUserRole/setUserEnabled/resetUserPassword/deleteUser/changeMyPassword/getMe` + `loadMe/saveMe/clearMe`（localStorage `nitro.user` 缓存当前用户）。
  - **用户管理页**：新增 `web/src/views/Users/UserListView.vue`（仅 Admin 可见入口）：表格（用户名/角色下拉/启停开关/最近登录/创建时间）+ 新增用户/重置密码对话框 + 删除（ElMessageBox 确认）；对自己禁用改角色/启停/删除（防自锁）；后端 400/409 错误（如最后一个 Admin）原样提示并刷新回滚。
  - **路由/菜单门控**：路由 `/users` + `meta.roles: ['Admin']`，`router.beforeEach` 异步校验（缓存无角色信息时实时拉取 `/api/user/me`）；`App.vue` 侧边栏「用户管理」仅 `currentUser.role === 'Admin'` 显示。前端门控仅为 UX，安全由后端 AdminOnly 策略兜底。
  - **顶部栏用户菜单**：`App.vue` 顶部栏加用户下拉（👤 用户名 + 角色标签 + 修改密码 + 退出登录）；自助改密对话框调 `PUT /api/user/me/password`（校验当前密码、最小 8 位）；登出清 token + 用户缓存跳 /login；`onMounted` 拉取 `me` 缓存（角色变更重进页面即生效）。
  - **403 处理**：`api/client.ts` 响应拦截器补 403 → ElMessage 提示后端错误信息（此前仅处理 401）。
  - 验证：`dotnet build` **0 错误**；`dotnet test tests/NitroGateway.UnitTests` **755 通过 0 失败**（+1 Me 断言）；`dotnet test tests/NitroGateway.IntegrationTests` **51 通过 0 失败**；`npm run build`（`vue-tsc -b && vite build`）**通过**（dist 正常产出；Rolldown 仅对 @microsoft/signalr 第三方注释告警，非本次改动）。
  - 备注：构建前 Webapi 被再次启动（VS 启动 PID 14676）锁定 DLL，已 `Stop-Process -Force` 停掉（devenv 10640 未动）；开发实例已停止，需恢复请自行重启。git 未提交（默认由用户执行）。

- （已完成，2026-08-23）B1 用户管理 ADR-066 实施（DB 化 users 表 + 管理接口）：
  - **Security**：新增 `Auth/UserAccount.cs`（users 表行模型）、`Auth/IUserStore.cs`（契约）、`Auth/TokenIssueResult.cs`（TokenIssueStatus：Success/UserNotFound/InvalidPassword/Disabled）；`TokenGenerator` 同步 `IssueToken` → 异步 `IssueTokenAsync`，改读 `IUserStore` + `PasswordHasher<UserAccount>`（单例，哈希格式与 UserConfig 兼容），停用拒签、登录成功 best-effort 刷 LastLoginAt；`SecurityServiceCollectionExtensions` 注册 hasher。
  - **Persistence**：`Migrations/M015_CreateUsersTable.cs`（id 自增/username 唯一/password_hash/role/is_enabled 默认 true/时间列 O 格式 UTC，沿用 M014 约定）；`Sqlite/SqliteUserStore.cs`（Dapper 独立连接单例：Find/Create/List/UpdateRole/UpdateEnabled/UpdatePasswordHash/UpdateLastLogin/Delete + `SeedIfEmptyAsync` 空表单事务灌配置用户；存储异常上抛转 500 不伪装 401，仅唯一冲突=19 映射 Validation 失败）；`SqliteServiceCollectionExtensions` 注册 `IUserStore` 单例。
  - **Webapi**：`Controllers/UserController.cs`（Admin 管理动作逐方法 `[Authorize(Policy="AdminOnly")]`：列表/新增/改角色/启停/重置密码/删除；`PUT me/password` 自助改密任意已登录角色；密码最小 8 位；禁止移除最后一个启用 Admin；DTO 不含密码哈希）；`AuthController.Login` 异步化（UserNotFound/InvalidPassword 统一 401，Disabled 403，限流不变）；`Program.cs` `InitializeDatabase()` 后执行 `SeedIfEmptyAsync` 首启种子（保住 admin/admin123）。
  - 测试：重写 `TokenGeneratorTests`（6 例：成功+LastLogin/角色 claim/明文失败/密码错/用户不存在/停用）；新增 `SqliteUserStoreTests`（10 例：种子/不覆盖/增查改删/唯一冲突/启停/改密/LastLogin/缺失 false）；`WebapiAuthorizationTests` +3 例（Admin 动作 AdminOnly/列表 AdminOnly/自助改密不需 AdminOnly）。
  - 验证：`dotnet build NitroGateway.slnx` **0 错误**；`dotnet test tests/NitroGateway.UnitTests` **754 通过 0 失败**（基线 738 + 16 新增）；`dotnet test tests/NitroGateway.IntegrationTests` **51 通过 0 失败**。
  - 备注：构建时被运行中的 Webapi（VS 启动 PID 30284）锁 DLL，已 Stop-Process 杀掉；该开发实例已停止，需恢复本地开发请自行 `dotnet run`/VS 重启。git 未提交（默认由用户执行）。

- （已完成，2026-08-23）ADR-065 A1/A2/A3 落地（web + 后端 + 前端 UI）：
  - **A1 仪表盘增强**：DashboardView 加「今日告警」「缓冲积压」两张 KPI 卡 + 活跃告警汇总列表（10s 轮询）；后端新增 `AlarmsController /api/alarms/summary`（活跃数 + 今日发生数，今日按本地 0 点起），`IAlarmRepository.CountOccurredSinceAsync`（EF COUNT，含边界）。
  - **A2 转发看板**：SystemStatus 嵌入 `web/src/components/ForwarderBoard.vue`——outbox 水位曲线（ECharts，复用 3s 轮询采样 150 点）+ 断点续传事件流（前端推导：断网→堆积→续传→清空，含 MQTT 状态/转发开关事件）。事件流为前端采样推导，非后端推送（后端 SignalR 无 BufferBacklogChanged，见 ADR-007 P1-3）。
  - **A3 操作日志查询页**：audit_logs 落库——`M014_CreateAuditLogsTable`（FluentMigrator）+ `SqliteAuditLogStore`（Dapper 独立连接，WriteAsync best-effort 失败仅记日志）+ `AuditMiddleware` 非 GET 落库 + `IAuditLogStore` 接口；查询 API `AuditLogsController /api/auditlogs`（时间/操作者/方法/路径包含/状态码过滤 + 分页，仅 Admin/Operator）；前端 `api/audit.ts` + `views/Audit/AuditLogView.vue` + 路由 `/audit` + 侧边栏「操作日志」。
  - 验证：`dotnet build` 通过；`dotnet test` 全量 **738 通过**（新增 SqliteAuditLogStoreTests 6 例、CountOccurredSinceAsync 1 例、AuditLogsController RBAC 断言 1 例 + 修正 AlarmsViewModelTests 两个仓储桩）；前端 `npm run build`（vue-tsc + vite）通过。
  - 约束遵守：未动 `*.db`/依赖版本；Storage/Abstraction 接口未删改；ADR-065 已删除 A1/A2/A3 及 B2/B3/B4 条目，README 同步。git 提交/推送由用户执行，本次未提交。

- （2026-08-23）B1 用户管理定案入档：新建 `notes/ADR/security/ADR-066-user-mgmt-no-full-identity.md`（不换全量 Identity，DB 化 users 表 + 管理接口），ADR-065 补交叉引用，ADR README 地图同步；纯文档改动，代码待下轮实施。
- （2026-08-23 早前）面试可见性缺口入档：用户问「功能是不是太少 / 面试讲还差什么」，核对代码 + 双端 UI 后归类为 A 类（已有能力 UI 不可见：仪表盘过薄/断点续传无可视化/写审计无查询页/告警通知仅 MQTT/RBAC 无管理页）与 B 类（真缺且面试高频：用户管理/操作日志/转发看板/仪表盘增强），写入 `notes/ADR/architecture/ADR-065-ui-visibility-gaps-interview.md`（含代码位置与修复方向），并同步更新 `notes/ADR/README.md`（architecture 份数 9→10、五、分组表加 065、七、还开着的事加 065）。C 类（HMI/规则引擎/冗余/OTA/OPC UA Server）明确不碰，沿用 ADR-064 阶段二排序。纯文档改动，未动代码；git 提交默认由用户执行。
