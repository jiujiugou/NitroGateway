# 2026-08-13

## 当前目标

- **（当前状态，2026-08-13）项目冻结：只修 bug，不再做新功能/优化。** 用户明确「项目可以完全冻结，除非有 bug」。据此：
  - ADR-042 的 5 条极限优化优先级、L 验证（T1/T2 + T3 8h 长稳）、F 决策（NU1903 / NU1701 / docs/interview 移库）全部**挂起**，除非被判为 bug 范畴。
  - 此后所有改动先过三问：是否属于 bug？不属于则不做。
  - 冻结前工作区改动已于 **6d35f79** 提交并推送 origin/master（桌面告警规则管理 + ADR-042/043 + worklog，见文末「提交/推送」）；`tools/` 验证截图/脚本按仓库惯例未入库。
- （已完成）用户澄清「我要的是可以为设备设置、到达什么条件后就触发报警那种」→ ADR-043 已重写并**已实施**为桌面「告警规则」管理页（见下「实现: 桌面『告警规则』管理页」），build 0 错误 + 578 单测全绿。此前误实现的「异常报警」（设备离线/通信异常事件记录，M013 + AbnormalAlarmRecorder）已回退（见下）。
- （本轮追加）用户问「注册多少设备/点位测出项目极限」→ 全链路复核极限根因，新写 ADR-042（极限模型=写入吞吐<每秒批数；纠正 ADR-032 关于转发缓冲异步化的结论；告警通道 DropOldest 有漏报风险；给出 5 条优化优先级），待用户确认现场规模/丢数容忍/硬件后再定实施项（见下）。
- （承接 2026-08-12）ADR-037 桌面 UI 美化 S1-S12 已全部实施；今日追加完成 ADR-038 视觉设计系统重写（靛蓝主色/深色侧栏/卡片化/圆角投影，build + 558 单测全绿，见下）。
- （本轮追加）用户二轮反馈后完成 ADR-039：侧栏文字对比度根因修复 + 设备统计卡 + 行美化 + 弹窗美化，build + 559 单测全绿（见下）。
- （本轮追加）用户三轮反馈「行内文字显示不对称、很突兀」→ ADR-040：DataGridTextColumn 单元格文本垂直居中，消除与模板列（设备名/徽章）的错位，build + 559 全绿（见下）。
- （本轮追加）用户四轮反馈「悬浮解决了但和上面没对齐」→ ADR-041：DataGridCell 自定义模板使 Padding=14 真正生效，数据行文字与表头左缘精确对齐（UIA 逐列 0px 差），build + 559 全绿（见下）。
- 待用户拍板：L 验证（T1/T2 + T3 8h 长稳）与 F 决策（NU1903 / NU1701 / docs/interview 移库）；git 提交/推送默认由用户执行。

## 实现: 桌面「告警规则」管理页（ADR-043，2026-08-13）

- 三问: 为什么做=用户澄清「我要的是可以为设备设置、到达什么条件后就触发报警那种」，即现有 AlarmRule/AlarmEvaluator 系统的桌面配置入口（此前误把「异常报警」实现为设备离线/通信异常事件记录，已回退）；验收=桌面新增「告警规则」导航页：列表含禁用 + 新增/编辑/删除模态对话框、设备→点位级联、保存即生效；不做=现场无法离线配置报警条件。
- G1 确认: `IAlarmRuleRepository` 新增只读 `GetAllIncludingDisabledAsync`（只增不删，Web 侧零改动），ADR-043 已记录。
- 范围: 复用现有 AlarmRule/AlarmEvaluator/AlarmHostedService——桌面与告警评估同进程，`CachedAlarmRuleRepository.Save/Delete` 成功即失效缓存，新规则立即可评估。
- 仓储: `SqliteAlarmRuleRepository.GetAllIncludingDisabledAsync` 不过滤 Enabled；`CachedAlarmRuleRepository` 绕过缓存直读内层（缓存只放启用规则）；`InMemoryAlarmRuleRepository` 返回原始全量。
- ViewModel: `AlarmRulesViewModel`（单例，`IServiceScopeFactory` 每操作新建 scope 解析 Scoped 仓储，与 AlarmsViewModel 同模式）——`GetAllIncludingDisabledAsync` 加载全量 + `IDeviceSnapshotCache` 映射设备/点位名（缺失回退短 ID `ToString("N")[..8]`）；新增/编辑/删除命令，操作后自动刷新。
- 表单: `AlarmRuleEditor` 实现 `INotifyDataErrorInfo`（设备→点位级联、运算符/阈值/时长/等级/消息模板/启用，Between 显上限），字段与 Web AlarmRulesView.vue + AlarmRulesController 对齐；`AlarmRuleEditorWindow` 4 列表单走 `IAlarmRuleDialogService` 抽象（仿 IDeviceDialogService 便于单测）。
- 视图/接线: `AlarmRulesView`（工具栏 + DataGrid，禁用行灰色 + 空态）；`MainViewModel` 在「告警」后加导航项「告警规则」（Segoe MDL2 \uE8FD）；`MainWindow` DataTemplate；`DesktopServiceCollectionExtensions` 注册 `AlarmRulesViewModel` 与 `IAlarmRuleDialogService`。
- 测试: 新增 `AlarmRuleEditorTests`（8）+ `AlarmRulesViewModelTests`（12，含 scoped 每操作新建/全量含禁用/短 ID 回退/增删改确认与取消/失败提示）+ `CachedAlarmRuleRepositoryTests` 缓存旁路用例；`DesktopViewSmokeTests` 加入 `AlarmRulesView` 与 `AlarmRuleEditorWindow`；`AlarmRuleEditorTests` 曾有一行 `Assert.All(points, ...)` 占位空断言已改为真实断言。基线 559 → 578 单测全绿，build 0 错误。
- 未提交: git 提交由用户执行

## 回退: 桌面「异常报警」误实现（ADR-043 范围纠正，2026-08-13）

- 用户反馈「异常报警没在桌面设置」后，曾误把「异常报警」实现为设备离线/通信异常/恢复事件记录：M013 迁移建 device_abnormal_events 表 + AbnormalAlarmRecorder（IDeviceHealthListener 归类 离线/通信异常/恢复）+ SqliteAbnormalAlarmStore（Dapper）+ 桌面「异常报警」页（5s 增量刷新），并新增 23 个单测（基线 559 → 582 全绿）。
- 用户澄清「我要的是可以为设备设置、到达什么条件后就触发报警那种」→ 该实现并非所需：用户要的是现有 AlarmRule/AlarmEvaluator 系统的桌面配置入口，而非设备状态事件日志。
- 已完全回退：`git checkout --` 恢复 8 个被改文件（DesktopServiceCollectionExtensions / Styles.xaml / MainViewModel / MainWindow.xaml / SqliteErrorClassifier / DesktopShellRegistrationTests / DesktopThemeTests / DesktopViewSmokeTests），删除 12 个新增文件（M013 迁移、AbnormalAlarmRecorder/Store/Event/Type、IAbnormalAlarmStore、SqliteAbnormalAlarmStore、AbnormalAlarmsViewModel、AbnormalAlarmsView.xaml/.cs、3 个测试文件）；build 0 错误，单测回到基线 559 全绿。
- 教训: 需求不清时先按「配置规则→条件触发报警」澄清，不把「设备状态事件记录」当作「异常报警」实现。
- 未提交: git 提交由用户执行

## 桌面 UI 四轮打磨：表头与数据行水平对齐（ADR-041，2026-08-13）

- 三问: 为什么做=用户反馈「这个悬浮问题解决了，但是和上面没有对齐」；验收=从站/上次采集/点位数/错误及模板列文字左缘与表头一致，build+单测全绿；不做=多列表格行内文字与表头横向错位 17px
- G1 确认: 纯 XAML 样式/视觉变更，无接口/数据/行为变更，直接实施
- 根因: WPF 默认 DataGridCell 模板不应用 Padding——CellStyle 的 Padding="14,0" 从未生效，所有单元格内容贴左缘 0px；表头 ColumnHeaderStyle Padding=14 生效于 14 DIP，水平差 17px（UIA 实测 header 1040/1115/1235/1305 vs 行 1023/1098/1218/1288）
- 修复: DataGridStyle.CellStyle 自定义 ControlTemplate（Border Padding=TemplateBinding Padding 包裹 ContentPresenter，保留 Background/BorderThickness 绑定与 IsSelected 触发器），所有列内容统一按 14 DIP 内边距排布
- 验证: dotnet build 0 错误；单测 559 全绿；UIA 重启后逐列比对全对齐（从站/上次采集/点位数/错误 0px 差，图标/徽章左缘=表头 525/930）；像素扫描 tools/screen-aligned.png 二次确认
- 未提交: git 提交由用户执行

## 桌面 UI 二轮打磨（ADR-039，2026-08-13 续）

- 三问: 为什么做=用户反馈「左侧栏文字看不清、设备页要统计数、加设备后那行很丑、弹窗要美化」；验收=导航文字亮色可读 + 4 张统计卡 + 行徽章/双层名 + 分组弹窗，build+单测全绿；不做=主操作面可读性与信息密度不足
- G1 确认: 纯视觉/样式变更 + DevicesViewModel 新增 4 个只读统计属性（无接口/数据模型变更），直接实施
- 侧栏对比度: NavForeground #CBD5E1→#E2E8F0、NavMutedBrush #64748B→#94A3B8、NavHoverBrush #1F2937→#293548
- 根因修复: MainWindow 导航两个 TextBlock 显式绑定 ListBoxItem.Foreground（隐式 TextBlock 样式 #1E293B 覆盖了继承色，悬停/选中也不变白）→ 默认 #E2E8F0、悬停/选中 White；像素采样确认修复生效
- 统计卡: DevicesViewModel 增 TotalCount/OnlineCount/OfflineCount/TotalPoints（RefreshAsync diff 后重算）+ 单测 Refresh_computes_total_online_offline_and_point_counts；DevicesView 顶部 4 卡（蓝总/绿在线/红离线/紫点位）+ 工具栏下移一行
- 行美化: RowHeight=48；设备名列=靛蓝图标块 + 名称/协议双层；状态列=圆角徽章（圆点 + 语义色软底，DataTrigger 对齐 web DeviceStatusTag）
- 弹窗美化: DeviceEditorWindow 页头图标 + 标题/副标题、4 个 GroupBox 分区、WrapPanel 两列表单、高 680
- 关键坑: DesktopThemeTests 扫描 Views/*.xaml 任何 `#`（含 XML 实体 `&#xE787;`）→ 5 个 Segoe MDL2 图标字形收拢到 Styles.xaml sys:String 资源，Views 零 `#`
- 验证: dotnet build NitroGateway.slnx 0 错误；单测 559 全绿（558+1）；capture-window-dpi.ps1 像素采样确认导航亮色/4 卡/在线徽章；UI 自动化加设备验证行渲染后已删除测试数据（tools/ui-final2.png 为干净态截图）
- 未提交: git 提交由用户执行

## 桌面 UI 三轮打磨：表格文字垂直对齐（ADR-040，2026-08-13）

- 三问: 为什么做=用户反馈设备表格「这里文字显示不对称、很突兀」；验收=DataGridTextColumn 单元格文本与模板列（图标+名称、状态徽章）垂直中线对齐，build+单测全绿；不做=多列表格内文字上飘、视觉重心错位
- G1 确认: 纯 XAML 样式/视觉变更，无接口/数据/行为变更，直接实施
- 根因: DataGridTextColumn 生成的 TextBlock 默认 VerticalAlignment=Stretch，字形渲染在文本块顶部，虽 CellStyle 已设 VerticalContentAlignment=Center 仍整体「上飘」；而 DataGridTemplateColumn 的根元素（设备名 StackPanel / 状态 Border）显式 VerticalAlignment=Center，导致同一行里文本列与模板列垂直错位约 16px
- 像素定位（tools/ui-asym.png，1550×950 物理，125% DPI）：行 y378-440 中线 409；模板列 c1=412/c2=410.5（居中），文本列 c3/c4/c5=393.5、c6=395（上飘 -14~-16px）→ 确认
- 修复: Styles.xaml 新增共享样式 DataGridCellTextStyle（TextBlock VerticalAlignment=Center + Foreground=TextPrimary + TextFormattingMode=Display，与隐式 TextBlock 样式等价避免覆盖前景色）；5 个视图（Alarms/Devices/History/PointsWindow/Realtime）全部 29 个 DataGridTextColumn 加 ElementStyle="{StaticResource DataGridCellTextStyle}"
- 验证: build 0 错误；单测 559 全绿；重启应用重抓 verify-1.png 逐列测量——c3/c4/c5=410.5、c6=412，全部落回中线 ±3px 内，与模板列一致（修复前 -14~-16px）
- 未提交: git 提交由用户执行

## 实现: ADR-037 桌面 UI 美化与优化全量落地（2026-08-13）

- 三问: 为什么做=ADR-037 已规划（2026-08-12），桌面端是现场主操作面，需补齐状态色/空态/校验/启动反馈；验收=12 步逐条按 ADR 验收标准落地并附测试，build+全量测试全绿；不做=体验粗糙与散落硬编码色值、全量重建列表等维护成本持续累积
- G1 确认: S4 表单校验、S5 Token DPAPI、S6 窗口缩放、S7 增量刷新为行为/安全变更，动手前已一句话说明（S1-S3/S8-S12 按 ADR 直接实施）
- S1 令牌化: Styles.xaml 增 AlternatingRow/BadRow/Severity×3/StatusBar×2/MutedText/CurveCardBorder/ButtonHover/ButtonPressed/Status×5/Error/BrandGradient 令牌 + BoolToVis；各视图零 # 色值字面量（DesktopThemeTests 扫描兜底），DataGrid 交替行色收敛到全局样式
- S2 状态色: DevicesView 状态列 ElementStyle DataTrigger 前景色，与 web/src/components/DeviceStatusTag.vue 语义一致（在线绿/离线红/异常橙/维护紫/未知灰），测试解析 vue 色值比对 Styles.xaml
- S3 空态/加载: Devices/Alarms/History/Realtime/PointsWindow 空列表叠「暂无数据」（Items/Rows/Points.Count=0 触发）；Devices/Alarms/History 刷新/查询中按钮禁用（IsIdle 计算属性）+ 不确定进度条
- S4 表单校验: DeviceEditor/PointEditor 实现 INotifyDataErrorInfo（Validate 全量重算 + 字段变更即重算）；规则: Name/Endpoint/Address 非空、UnitId 1-247（仅 Modbus）、超时/重试/间隔>0、RTU 波特率/数据位枚举化、点位数>=0/有限数值；Styles.xaml TextBox 全局 ErrorTemplate 红框+悬浮错误；窗口 OnSave 校验不过不关窗
- S5 Token 安全: SettingsView Token 改 PasswordBox + 「显示 Token」切换（code-behind 同步 VM 内存明文）；CenterSyncSettingsStore 落盘 DPAPI 加密（P/Invoke CryptProtectData CurrentUser，零新依赖 DpapiProtector），CenterToken 标 JsonIgnore 不落盘，旧明文文件读取兼容并即时改写迁移；密文损坏回退空 Token 不阻断设置页
- S6 窗口缩放: DeviceEditorWindow 720x620 Min560x400 / PointEditorWindow 680x500 Min520x360，ResizeMode CanResize + 内容 ScrollViewer（禁横向滚动）
- S7 增量刷新: Devices/Alarms 刷新由 Clear+全量重建改为按 Id diff——行模型改 ObservableObject 原位更新（Not 属性变更），新增插入/消失移除/选中对象复用；DeviceCountChanged 事件供状态栏
- S8 启动反馈: 新增 StartupWindow（splash：进度条+文案，失败切错误态+关闭按钮）；App.OnStartup 先显示启动窗再 StartAsync，失败窗口内提示不再只弹 MessageBox，成功后切 MainWindow
- S9 曲线窗口: RealtimeViewModel MaxChartPoints 600→7200（1s≈2h 与预载一致）
- S10 导航图标: NavItem 增 Glyph，MainWindow 导航 Segoe MDL2 Assets 图标（设备 E772/实时 E9D9/告警 E7BA/历史 E81C/设置 E713）；品牌块 BrandGradientBrush 轻渐变
- S11 设备数合并: MainViewModel 删 5s 独立目录查询与缓存依赖，订阅 DevicesViewModel.DeviceCountChanged（同一刷新节奏），状态栏设备数不再重复查询
- S12 环形缓冲: 新增 RingObservableCollection.TrimFront（底层 List 批量移除+单次 Reset 通知），替代逐项 RemoveAt(0) 的 O(n) 搬移与 N 次 CollectionChanged
- 测试: 新增 DesktopThemeTests(3) + CenterSyncSettingsStoreTests(5) + DesktopViewSmokeTests.ListViews_initialize_on_sta(1，含 StartupWindow) + DeviceEditorTests 校验 6 + PointEditorTests 校验 4 + DevicesViewModelTests S7/S11 4 + AlarmsViewModelTests S7 3 + RealtimeViewModelTests S9/S12 1，共 27 例；单测 558 全绿（基线 531 + 27）、IntegrationTests 43 全绿、dotnet build NitroGateway.slnx 0 错误
- 修复过程: 首次补丁在 Styles.xaml 重复定义 TextBox 隐式样式致 XamlParseException（duplicate key），已合并进既有样式；观察 ListViews 冒烟暴露 SettingsView 缺 Styles.xaml 合并字典，已补
- 未提交: git 提交由用户执行

## 视觉美化: 桌面设计系统重写（ADR-038，2026-08-13）

- 三问: 为什么做=用户反馈桌面 UI 丑陋，要求优化成符合人类审美的美观界面；验收=统一设计系统（主色/圆角/投影/深色侧栏），像素级验证关键区配色，build+全量单测全绿；不做=主操作面观感平庸
- G1 确认: 纯视觉/样式变更（含 RealtimeViewModel 曲线色），无行为/接口/数据变更，按 ADR-038 直接实施
- 设计系统: Styles.xaml 重写——靛蓝主色 #2563EB 令牌（PrimaryBrush/NavSelectedBrush 等）、深色侧栏 NavGradientBrush（#0F172A 系）+ 品牌渐变、卡片样式 CardBorderStyle/CardElevatedStyle（圆角 8 + 轻投影）、按钮/TextBox/ComboBox/DataGrid/GroupBox/滚动条/ProgressBar/ToolTip 模板统一重绘；ADR-037 RequiredTokens 与状态语义色 Hex 全部保留
- 视图重排: MainWindow 深色侧栏（Segoe MDL2 图标 + 选中靛蓝胶囊 + 品牌块 + 页脚）+ 底部深色状态栏；各列表/配置视图统一页头（大标题+副标题）+ 卡片工具栏 + 卡片表格；弹窗表单化
- 关键坑修复: (1) WPF 隐式 Window 样式对 Window 子类不生效 → MainWindow/StartupWindow/DeviceEditorWindow/PointEditorWindow/PointsWindow 显式声明 Background/Foreground/FontFamily/FontSize；(2) 窗口自身属性 StaticResource 在 Window.Resources 就绪前解析，无 Application 冒烟测试抛 XamlParseException（找不到 PageBackground）→ 元素属性级改 DynamicResource，内容区保持 StaticResource
- 验证: dotnet build NitroGateway.slnx 0 错误；单测 558 全绿（修复了 2 例 DesktopViewSmokeTests）；新增 tools/capture-window-dpi.ps1（SetProcessDPIAware + PrintWindow 抓物理分辨率）像素采样确认侧栏深色/导航选中靛蓝 #2563EB/画布 #F3F5F9/卡片白/状态栏 #0F172A；前后对比图 tools/baseline-main.png → tools/ui-final.png
- 未提交: git 提交由用户执行

## 提交/推送（2026-08-13，用户指示「远程一下」）
- 分支: master -> origin/master（https://github.com/jiujiugou/NitroGateway.git）
- 提交: 0c1bd7f feat: 桌面 UI 美化（ADR-037 S1-S12）+ 配置同步/站点管理收尾（69 files changed, +3219/-288）
- 结果: 首次推送后修正了提交信息中文被管道编码成 ? 的问题（55f4ee5 -> 0c1bd7f，无 BOM UTF-8），force-with-lease 覆盖远程成功；本地与远程同步（0/0）

## 提交/推送（2026-08-13，用户指示「远程推送一下」）
- 分支: master -> origin/master（https://github.com/jiujiugou/NitroGateway.git）
- 提交: 68ab664 feat: 桌面 UI 视觉重绘与多轮打磨（ADR-038~041），19 files changed, +1802/-845（含 ADR-038/039/040/041 + worklog + 桌面源码/测试）
- 结果: 一次推送成功 a8df7a8..68ab664 master -> master；tools/ 下验证截图与 UIA 脚本为 AI 验证产物，按仓库惯例未入库；本地与远程同步（0/0）

## 提交/推送（2026-08-13，用户指示「远程推送」）
- 分支: master -> origin/master（https://github.com/jiujiugou/NitroGateway.git）
- 提交: 6d35f79 feat: 桌面告警规则管理页 + 极限根因 ADR（ADR-042~043），23 files changed, +1503（ADR-042/043 + 告警规则仓储只增 GetAllIncludingDisabledAsync + 桌面告警规则页 + 测试 + worklog 冻结状态）
- 结果: 构建 0 错误、单测 578 全绿后一次推送；tools/ 验证产物按仓库惯例未入库；本地与远程同步
