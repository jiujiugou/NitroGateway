# 2026-08-24

## 当前目标

- （已完成，2026-08-24）web 收敛为纯边缘网关三件事：① 设备/点位写入自动注入本站点 ID（SiteIdProvider，app_meta 持久化 + 启动写回 Site:Id）；② 新增「边缘→中心配置同步 push」通道（outbox + CenterConfigClient + WebConfigSyncService 周期拉中心快照双向合并并上报）；③ 完全清理 web 作为中心库的接口/方法（删 SitesController/ConfigSyncController/ConfigSyncService/SqliteSiteCatalog 及对应测试），Storage 纯接口 ISiteCatalog/SiteInfo 保留。构建 0 错误 / 单测 750 通过 / 集成 51 通过。git 提交/推送由用户执行。
- （已完成，2026-08-24）ADR-067 桌面 MQTT Broker 地址/端口可编辑 + 测试连接：设置页新增「MQTT 连接设置」可编辑区（Host/端口/TLS/用户名/密码）+「测试连接」「保存」按钮，密码 DPAPI 落盘，保存后重启生效（环境变量 ＞ 持久化设置 ＞ appsettings 默认）。配套单测 +21，构建 0 错误 / 单测 776 通过 / 集成 51 通过。git 提交/推送由用户执行。

## 已记录

- （已完成，2026-08-24）ADR-067 桌面 MQTT 连接设置实施（用户需求「桌面 UI 可填地址/端口并测试连接」）：
  - **三问**：为什么做——现场改 broker 地址/端口/凭证只能动 `appsettings.json` 重启，且无连通性预检，配错转发静默失败；验收标准——设置页可填 Host/端口/TLS/用户名/密码并「测试连接」成功/失败有明确提示、保存后重启生效、密码加密落盘；不做会怎样——现场配置 MQTT 成本高、排障靠猜。
  - **G1**：行为变更（新增 UI 配置入口 + 启动覆盖规则）；不改接口/数据模型/依赖版本，Storage/Protocol 纯接口未动。
  - **持久化**：`DesktopSettingsStore` 扩展 `MqttHost/MqttPort(1883)/MqttUseTls/MqttUsername/MqttPasswordEncrypted/MqttPasswordConfigured`，密码 DPAPI（CurrentUser）落盘（复用 ADR-037 S5 `DpapiProtector`），明文不写盘；`MqttPasswordConfigured` 区分「未配置」与「明确空密码（匿名）」。
  - **测试连接**：新增 `Services/Connectivity/IMqttConnectionTester` + `MqttConnectionTester`——独立临时 `MqttClientWrapper`（无状态监听者、无转发开关、`MaxReconnectAttempts=0`，不碰 DI 单例），Connect 成功 + 发布测试消息（topic `nitrogateway/{siteId}/connection-test`，QoS1，ADR-020 P3-6 无订阅者按成功），8s 超时兜底，防「TCP 通但写不进」假阳性。
  - **生效机制**：`Hosting/MqttDesktopConfig.cs`（internal）在 `GatewayHost.Create`、`AddNitroMqtt` 前把持久化 MQTT 配置写回 config，**仅当环境变量未设置时**（优先级 环境变量 ＞ 持久化设置 ＞ appsettings 默认）；UI 明确提示「保存后重启生效」（与 LogDirectory/SiteId 一致）。
  - **UI**：`SettingsView.xaml` 新增「MQTT 连接设置」GroupBox（Host/端口/TLS CheckBox/用户名/密码 PasswordBox 可显示隐藏）+ 测试连接/保存按钮 + 状态文本；端口用字符串属性 `MqttPortText` 绑定 + `TryValidateMqttInput`（Host 非空、Port 1-65535），避免 int 双向绑定非法输入静默问题。
  - **验证**：`dotnet build NitroGateway.slnx` **0 错误**；`dotnet test tests/NitroGateway.UnitTests` **776 通过 0 失败**（新增 tester 5 / MqttDesktopConfig 4 / SettingsViewModel 9 / DesktopSettingsStore 3，共 +21；`DesktopViewSmokeTests` 通过）；`dotnet test tests/NitroGateway.IntegrationTests` **51 通过 0 失败**。
  - 备注：未动 `*.db*`/依赖版本/appsettings 测试账号；git 未提交（默认由用户执行）。

- （已完成，2026-08-24）web 纯边缘改造（用户需求：1 补全 web 的站点 ID 注入；2 补「边缘→中心配置同步 push」通道；3 完全清理 web 作为中心库的接口和方法）：
  - **三问**：为什么做——web 建设备时前端不传 siteId（落库为空），作为纯边缘网关应自动带上本站点身份；同时要把边缘离线改动上报中心（桌面已有 SiteConfigSyncService，web 缺失）；web 上残留的中心侧接口（SitesController/ConfigSyncController）属中心库职责，应清理使 web 完全边缘化。验收标准——建设备不传 siteId 落库后 SiteId=本站点；配置 CenterUrl 后设备/点位增删改经 outbox 周期上报中心、中心快照可双向 UpdatedAt 合并下发；Sites/ConfigSync 相关接口、实现、测试全部删除且构建/测试绿。不做会怎样——边缘身份缺失、改动无法同步到中心、web 职责混淆。
  - **G1**：行为变更（siteId 注入、新增同步通道、删除中心接口/实现）；接口层面仅删中心实现，`Storage/Protocol.Abstraction` 纯接口只增不删（`ISiteCatalog`/`SiteInfo` 保留）；未动依赖版本/`*.db`。
  - **站点 ID 注入**：新增 `Webapi/Services/SiteIdProvider.cs`（`ISiteIdProvider`）——解析顺序 配置 `Site:Id` → app_meta（key=`site_id`，`SqliteAppMetaStore` 已注册）→ 自动生成并持久化，`default` 视为未初始化；Program.cs 在 `InitializeDatabase()` 后首次 resolve 并写回 `Site:Id`，使采集/转发/告警/StatusController 构造期读取一致。`DevicesController` Create/Update 的 `SiteId` 空白时取 `_siteId.Current`（前端 devices.ts 不传 siteId）。
  - **同步 push 通道**：新增 `Services/CenterConfigClient.cs`（`ICenterConfigClient`，HTTP，契约同桌面）、`Services/ConfigSyncOutboxStore.cs`（`IConfigSyncOutboxStore`，表 `config_sync_outbox` 由 M010 建好，`d|{deviceId}`/`p|{deviceId}|{pointId}` 固定主键，删除后重建原地替换行类型）、`Services/WebConfigSyncService.cs`（BackgroundService，每周期 FetchSyncSnapshot→ApplySnapshot→PushPending，未配置 `ConfigSync:CenterUrl` 静默跳过，失败不阻塞采集下周期补做）。`DevicesController`（Create/Update/Delete/AddPoint/UpdatePoint/DeletePoint）与 `PointImportController`（ImportCsv/Generate）成功落库后写 outbox；outbox 写入失败仅记 Debug 日志不阻断主操作（与桌面 `RecordOutboxAsync` 语义一致）。Program.cs 注册 `HttpClient`(Singleton 15s)/`IConfigSyncOutboxStore`/`ICenterConfigClient`/`WebConfigSyncService`；appsettings 新增 `ConfigSync:{PollIntervalSeconds,CenterUrl,CenterToken}`。
  - **中心库清理**：删除 `Controllers/ConfigSyncController.cs`、`Controllers/SitesController.cs`、`Services/ConfigSyncService.cs`、`Models/ConfigSyncDtos.cs`、`Models/RenameSiteRequest.cs`、`Persistence/Sqlite/SqliteSiteCatalog.cs` 及其 DI 注册；测试删 `ConfigSyncServiceTests.cs`、`SqliteSiteCatalogTests.cs`；`WebapiControllerTests` 删 Sites_* 与 `FakeSiteCatalog`，Devices 构造加 siteId/outbox/logger 参数，新增「缺省 siteId 注入本站点」与「Create 成功写 outbox」两个用例。保留 `Storage/TimeSeries/ISiteCatalog.cs`+`SiteInfo.cs`（纯接口只增不删）、`DevicesController.Export`（只读快照端点，供中心/导入场景）与前端（web/src 无 sites/configsync 消费方）。
  - **验证**：`dotnet build NitroGateway.slnx` 0 错误；`dotnet test tests/NitroGateway.UnitTests` 750 通过 0 失败；`dotnet test tests/NitroGateway.IntegrationTests` 51 通过 0 失败。
  - 备注：工作区存在他人未提交改动（桌面 ADR-067 MQTT 设置 + Ingest 删除），本次未混入未触碰；git 未提交（默认由用户执行）。
