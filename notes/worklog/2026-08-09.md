# 2026-08-09

## 当前目标
S7 前后端完整性已修复（ADR-024，2026-08-09，见下方记录）：默认 CpuType 连接不再必失败、地址类型校验、连接取消防泄漏、前端 S7 参数表单、批量生成支持 S7 DB 区地址；ADR-020/021 的 P3 待处理；git 提交由用户执行

## 修复: S7 前后端完整性（ADR-024，2026-08-09）
- 问题: S7 实现不完整——后端默认 CpuType "S71200" 不在 switch 分支且无 default，默认配置连接必抛 SwitchExpressionException；M/I/Q 区地址自带类型被忽略（MW10+Float 静默读 MD10）；连接取消时已建连 client 未 Dispose；前端无 Rack/Slot/CpuType/PingAddress 表单（S7-300/400 slot=2 无法配置必连不上）；批量生成只能产 Modbus 数字地址
- 后端: S7Driver.ParseCpuType（默认 S-1200 + 未知显式报错）、连接取消/异常统一 Dispose、Rack/Slot Convert 解析、Ping 位地址按 Bool 读；S7AddressParser.FormatForHsl 地址自带类型优先 + 冲突/位后缀违规显式报错
- 批量生成: GenerateRequest.StartAddress int→string（兼容数字）、PointBatchService.Generate 增 Protocol 参数，S7 支持 DB{n}.DBD/DBW/DBB{offset} 按 ByteSize 递增（Bool 拒绝）
- 前端: DeviceForm 增加 S7 参数区与固定 TCP 传输方式、按协议端口占位；PointList 按设备协议切换默认地址并传 protocol
- 测试: S7AddressParserTests/S7DriverTests/PointBatchServiceTests 新增 49 用例（红绿对照：ParseCpuType 默认值、地址类型冲突、S7 生成步长）；UnitTests 346 通过，build 0 错误，web npm run build 通过
- 未提交: git 提交由用户执行
## 修复: 连接测试假阳性（ADR-023，2026-08-09）
- 问题: 从站地址 UnitId 填 11、实际无该从站时连接测试仍显示"连接成功"；ConnectAsync 成功即返回 success=true，PingAsync 失败仅标记 ping=unreachable，前端忽略 ping 字段
- 改动: DevicesController.TestConnection —— Connect 成功 + Ping 失败改为 success=false 并带原因（链路通 ≠ 从站存在）；latencyMs 覆盖 Connect+Ping
- 测试: 新增 3 个 TestConnection 用例（红绿对照）；UnitTests 295 通过；build 0 错误
- 限制: Modbus TCP 从站若忽略 UnitId 仍会误判成功（协议现实，不修）；RTU 从站号不匹配必然无响应，测试可靠
- 未提交: git 提交由用户执行
## 修复: 连接测试失败文案可读化（ADR-023 修复 2，2026-08-09）
- 问题: 修复 1 后从站无响应时前端显示 HSL 原始文本（PipeTcpNet[ip:port] : Socket Exception -> 接收数据超时：5000），不可读
- 改动: ModbusDriverBase.PingAsync 新增 ClassifyPingError：超时/断连 → "从站无响应（接收数据超时）：请检查从站地址/UnitId、网络与防火墙"；其余 → "从站无响应：请检查从站地址/UnitId 与设备状态"
- 测试: 新增 ModbusDriverBaseTests 2 用例（红绿对照）；UnitTests 297 通过
- 未提交: git 提交由用户执行
## 修复: 前端 Modbus TCP 支持配置从站地址（2026-08-09）
- 问题: Modbus 从站软件单端口多窗口（各窗口只能改从站 ID）；前端 TCP 表单无「从站地址」且保存时 delete UnitId，导致 UI 建出的 TCP 设备永远 UnitId=1
- 现状: 后端已支持按设备区分从站（ModbusTcpDriver 读 connection.Parameters.UnitId，默认 1；驱动池按设备 ID 独立建连），仅前端缺字段
- 改动: web/src/views/Devices/DeviceForm.vue — TCP 分支新增「从站地址」输入（1..247）；syncParams 对 Modbus 一律写 p.UnitId（TCP/RTU 均保留），S7 不受影响
- 用法: 同 IP:端口 建 N 台设备，UnitId 分别填 1/2/3... 对应各从站窗口；测试连接按钮同样按该 UnitId 校验
- 验证: npm run build 通过；dotnet build 0 错误；UnitTests 292 + IntegrationTests 40 全绿
- 未提交: git 提交由用户执行

## 修复: ADR-018 Persistence + ADR-019 Protocol 全部条目（2026-08-09）
- ADR-018：PurgeAsync 分批删除 + M007 timestamp 索引 + MeasurementWriteHost 检查 WriteAsync 失败（新增指标 nitro_store_write_failures_total）；Device/Point 仓储异常归类 + DeviceManager 不再忽略返回值；forward_buffer 入队上限 + IForwardBuffer.PurgeDeadLettersAsync + DeadLetterRetentionService；Commit 状态守卫；QueryLatest ROW_NUMBER 去重；SqlitePragmas WAL 缓存；枚举解析容错；启动恢复移出构造器；ExtractDataSource 改 SqliteConnectionStringBuilder
- ADR-019：S7 失败读不产出伪值（ReadCheckedAsync）+ 读写闸门 + DataType 全量映射 + M/I/Q 地址支持 + 全失败返回 Failure/Faulted + PingAddress 可配置 + 异步连接响应取消；ReliableProtocolDriver 超时注入（取 RequestTimeoutMs）+ 重试日志降 Debug；DeviceReader 每轮降 Debug；S7DriverCapability 下调批量声明；EndpointParser 支持 IPv6；DESIGN.md 标注历史快照
- 测试：新增 58 个单测（S7Driver/S7AddressParser/ReliableProtocolDriver/EndpointParser/DeadLetterRetentionService/SqliteDeviceRepository/MigrationRunner + 既有测试补充）；UnitTests 292 通过（234+58）；IntegrationTests 40 通过；build 0 错误
- 未提交: git 提交由用户执行

## 提交与推送（2026-08-09，用户指示 AI 代执行）
- 提交 fe8750e: fix: code review 全模块二轮修复（ADR-009/016~022）——src/tests/docs 功能清单
- 提交 30f02d1: docs(notes): 记录 ADR-008~022 与 worklog、访谈文档——notes/ 与 docs 新文档
- 推送: origin/master（https://github.com/jiujiugou/NitroGateway.git），master -> master 快进，807b157..30f02d1
- 结果: 推送成功，本地 HEAD = origin/master = 30f02d1185e4b40a50618cedbfb81cd815a9ac94，工作区干净

## 修复: ADR-009 Telemetry 观测契约（2026-08-09）
- P1-1 补 `collection_duration_ms`：`DeviceCollector.CollectOnceAsync` Stopwatch 整轮计时，finally 中 Observe（不含设备列表获取）
- P1-2 补 `devices_online`：`CollectOnceAsync` 与 `DevicesAvailable` 同处刷新（HealthMonitor 快照 Online 数，与"待采集数"语义区分）
- P2-1 补 `forward_total{status=deadletter}`：`SqliteForwardBuffer.MarkFailedAsync` 超限进死信处上报（转换点感知）
- P2-2 `mqtt_state` help 对齐枚举序（0=Disconnected 1=Connecting 2=Connected 3=Reconnecting 4=Faulted）
- P2-3 文档 8→9：`docs/03-功能清单.md` F-23、`docs/01-盘点.md`、`docs/interview/telemetry/` questions/answers 同步
- P2-4 Telemetry csproj 去重 OpenTelemetry 引用；决策：OTel 保留为预留入口不接执行层（生产无 Listener，追踪 dormant），注释说明
- 新增测试 2 个：`MarkFailed_OverMaxRetries_ReportsDeadletterMetric`（存在性断言）、`CollectOnceAsync_ReportsOnlineAndDurationMetrics`；死信精确值断言在并行下会因损坏负载恢复路径累加而失败，改存在性断言
- 验证: build 0 错误；UnitTests 199 通过（197+2）；IntegrationTests 25 通过
- 未提交: git 提交由用户执行

## Code Review: Collection 模块
- 范围: src/NitroGateway.Collection 全部 26 个源文件 + 依赖类型（DeviceHealthMonitor/GatewayLifecycle/ForwarderEngine/ProtocolDriverPool）+ 相关单测
- 产出: notes/ADR/ADR-016-collection-module-code-review.md
  - P1-1 关闭 drain 协调未实现（GatewayLifecycle 死代码 + 注册顺序导致 Forwarder 先停、与注释意图相反）
  - P2-1 热路径日志刷屏（每设备每轮 6+ 条 LogInformation）
  - P2-2 CollectionOption 无校验（IntervalMs<=0 / MaxConcurrency<=0 无防线）
  - P2-3 MeasurementWriteHost/SinkDispatcher 停机不排空 Channel（注释与实现不符）
  - P3×6: StopAsync CTS 竞态、探测名额闭环脆弱、Report 失败明细丢失、Scan 时间失真、HalfOpen 超时并发放行、DESIGN.md 漂移
- 复核撤回: 熔断器指数退避被 Trip 重置不成立——Offline 已触发后 HealthMonitor 不再重复通知 Trip（DeviceHealthMonitor.cs 状态变化才通知），退避 5s→5min 实际可达
- 亮点: 死区只影响缓存不丢数据（语义正确）；DataType 透传（ADR-001 P1-5）；选项全字段接线（ADR-014）；维护过滤走 HealthMonitor 实时状态（ADR-002 P2-2）；熔断器锁内状态机 + CQS 拆分（ADR-015）
- 验证: dotnet build 0 错误；单测 197 通过；集成测试 25 通过

## 修复: CircuitBreaker CQS（原 ADR-015，已删除）
- 接口: `ICircuitBreaker.IsOpen` → `TryEnterProbe()`（命令，返回是否放行，注释写明 Closed/Open/HalfOpen 各状态语义与调用约定）；`State` 改为纯查询（不再触发 Open→HalfOpen 迁移）
- 实现: `CircuitBreaker.State` getter 直接返回 `_state`；迁移/占探测名额只发生在 `TryEnterProbe`（锁内）
- 调用点: `DeviceCollector` 改调 `!TryEnterProbe()` 跳过；`StatusController` 诊断改读 `State`，`IsOpen` 由 `State == Open` 推导，不再误触探测
- 测试: `CircuitBreakerTests` 全部改 `TryEnterProbe` 语义，新增 `State_Getter_IsPureQuery_DoesNotTransitionOrConsumeProbe`（CQS 回归）；`CollectionOptionsWiringTests` 同步
- 文档: `docs/interview/collection/answers.md` / `questions.md` 的 IsOpen 描述更新为 TryEnterProbe + CQS
- 存量修复: `ReliableProtocolDriver.cs` 命名空间被上次会话误改为 `CircuitBreakerRegistry`，导致 slnx 构建失败，已改回 `NitroGateway.Protocol.Abstractions`
- 验证: `dotnet build` 0 错误；单测 197 通过；集成测试 25 通过
- 遗留: `CircuitBreakerListenerRegistrar.cs` 已从工作树删除（冗余类，无注册无使用点），未提交
## 复盘: 两轮 AI review 对比（ADR-001 vs ADR-016）
- 结论: 问题不是"变多"，是"变深"——首轮是模块内数据可靠性 bug（P0/P1: 崩溃恢复、共享连接并发、DataType 硬编码、失败静默），本轮是跨模块设计缝（P1-1 关闭协调未实现需跨 Host+Collection+Forwarder+注册顺序才可见；P2 日志/校验/停机排空；P3 竞态与契约）
- 原因: ① 洋葱效应——表层 bug 清完 review 自然进入结构/契约层；② Collection 是编排中枢，问题天生在接缝上；③ 上一轮按模块扫描时 D-09（Collection）挂起，本轮是补最后一块拼图
- 复发类别（机制缺口，不是随机 bug）:
  - 文档漂移 ×4: ADR-001 P2-7 / ADR-008 / ADR-009 P2-3 / ADR-016 P3-6
  - 死代码/未接线 ×3: GatewayLifecycle 无人读 / WriteGuard 注册未接线 / Telemetry 哑火指标
  - 并发卫生 ×2: ADR-001 P1-4 共享连接 / ADR-016 P3-1 CTS 竞态
- 处置建议: ① 用"新 P1/P2 速率 + 同类是否复发"衡量收敛，不用问题数；② 对复发类别做一次性机制（DESIGN.md 标注历史快照、review 清单加"找消费方"、热路径日志约定、Options 统一校验）；③ ADR-016 仅 P1-1 需拍板（删 GatewayLifecycle 还是真接协调），P2×3 本迭代修，P3×6 顺路修


## 修复: ADR-016 Collection review 全部条目（2026-08-09）
- P1-1 关闭 drain 协调真接实现：`GatewayLifecycle.RequestStop()` 语义修正（置 draining，原实现置 false 与命名相反）、新增 `MarkStopped()`；`CollectionEngine.StopAsync` 起始 `RequestStop()`、末轮结束后 `MarkStopped()`；`ForwarderEngine` 停机后 `DrainOnShutdownAsync()`（等采集侧 ≤15s，MQTT 连接时限 10s 排空缓冲，MQTT 不可用留待下次启动续传）
- P2-1 热路径日志：`DeviceCollector` 每设备每轮明细 Info→Debug，失败保留 Warning
- P2-2 Options 校验：`AddNitroCollection` 走 Bind+Validate+ValidateOnStart，IntervalMs/MaxConcurrency/熔断冷却非法值启动即报错并指明字段
- P2-3 Channel 停机排空：`MeasurementWriteHost`/`SinkDispatcher` catch OCE 后排空剩余批次/事件（各限时 5s），注释与实现一致
- P3-1 `CollectionEngine.StopAsync` CTS 竞态：局部捕获 + catch ObjectDisposedException
- P3-2 `DeviceCollector` 探测名额 try/catch 闭环：异常路径 RecordFailure，熔断器自身异常不阻断采集
- P3-3 `Report` 失败明细透传首个非 Good 快照 ErrorMessage，LastError 不再恒为占位
- P3-4 批次扫描窗口取快照 Timestamp min/max，不再恒为分发时刻
- P3-5 `CircuitBreaker` 探测超时并发放行：注释明确为有意放宽（防慢读永久阻塞恢复）
- P3-6 `Collection/DESIGN.md` 标注历史快照 + 已知漂移清单（Process 带 deviceId / 死区不丢数据 / Reader 复用长连接）
- 测试：新增 GatewayLifecycle 3、ChannelDrain 2、DeviceCollectorProbe 2、CollectionOptions 校验 Theory 3、DataDispatcher 1；集成测试 `StopAsync_WithConnectedMqtt_DrainsRemainingBuffer` 重写为确定性（原偶发失败：.NET 10 BackgroundService 直接调用 ExecuteAsync，启动即 Stop 存在调度窗口；改为空缓冲+断连启动、等 ExecuteTask 挂起后注入积压再 Stop，连跑 5 轮稳定）
- 验证: build 0 错误；UnitTests 211 通过（199+12）；IntegrationTests 26 通过
- 未提交: git 提交由用户执行
## Code Review: Forwarder 模块（二轮）
- 范围: src/NitroGateway.Forwarder 全部文件 + 依赖（SqliteForwardBuffer/MqttClientWrapper/MqttHostedService/GatewayLifecycle/StatusController/Program.cs）+ 相关测试
- 产出: notes/ADR/ADR-017-forwarder-module-code-review-2.md
  - P1-1 RunRoundAsync 积压查询无防护 + GetCountAsync 无异常分类 → SQLite 瞬时错误整机退出（StopHost）且跳过停机排空
  - P2-1 空轮早退导致 BufferBacklog 指标恒显旧值（gauge 不归零）
  - P2-2 停机中断在途轮时 OCE 被泛化 catch 吞掉 → 批量误导性 Error 日志 + failure 计数虚增 + 节流误收紧
  - P3-1 Forwarder 仍用同步 Count（违反 ADR-001 P3-13）；P3-2 intervalMs 无校验；P3-3 StatusController 用 RequestAborted
- 亮点: 停机排空链路正确（注册序 + GatewayLifecycle + MQTT Singleton）；失败路径可观测性完整；节流边界有测试
- 本轮为纯 review，无代码改动，未跑测试


## 修复: ADR-017 Forwarder 二轮 review（2026-08-09）
- P1-1 积压查询无防护：SqliteForwardBuffer.GetCountAsync 与其余方法一致 catch+分类（取消仍抛，DB 故障 Warning 按 0）；ForwarderEngine.RunRoundAsync 接口级兜底跳过本轮——SQLite 瞬时故障不再触发 StopHost 整机退出
- P2-1 空轮指标：空轮分支补 BufferBacklog.Set(0)+ThrottleBatchSize，非空轮提交后统一刷新——积压归零后仪表盘不再恒显旧值
- P2-2 取消误报：Forwarder.cs 单独 catch OCE（不 MarkFailed/不计数/不收紧节流，置 Error rethrow）；Commit 前加 !ct.IsCancellationRequested 守卫
- P3-1 指标改 GetCountAsync 异步（弃同步 Count，P3-13 约定）；P3-2 AddNitroForwarder 校验 intervalMs>0；P3-3 StatusController 捕获 OCE 按 0 收尾
- 测试基建：ForwarderCollection 串行集合（BufferBacklog 全局 Gauge，防并行精确值断言互踩）
- 红绿对照：P1-1×2 / P2-1 / P2-2 共 4 个测试先红后绿（临时回退验证）；新增测试 9 个（单测 4 = Sqlite 1 + 注册 3；集成 5 = 指标 3 + 取消 1 + 引擎韧性 1）
- 验证: build 0 错误；UnitTests 215 通过（211+4）；IntegrationTests 31 通过（26+5）
- 未提交: git 提交由用户执行

## Code Review: Persistence + Protocol 模块（2026-08-09）
- 范围: src/NitroGateway.Persistence 全部 + 消费方 DeviceManager/PointManager；src/NitroGateway.Protocol（Abstraction/Modbus/S7）+ 消费方 DeviceReader
- 产出: notes/ADR/ADR-018-persistence-module-code-review.md（P2×3 + P3×6）、notes/ADR/ADR-019-protocol-module-code-review.md（P1×1 + P2×5 + P3×6）
- Persistence 重点: P2-1 保留清理大 DELETE 锁库 + MeasurementWriteHost 忽略 WriteAsync 失败导致窗口内时序写入静默丢弃（2026-08-09 修正）；P2-2 Device/Point 仓储异常不归类、manager IsFailure 死代码；P2-3 forward_buffer 无上限 + 死信无保留
- Protocol 重点: P1-1 S7 失败读返回 0（静默坏数据上云）；P2-1 S7 无读写闸门；P2-2 S7 DataType 映射不全/写恒 float；P2-3 S7 地址仅 DB 区与文档不符；P2-4 重试管线 3s 超时 < 设备默认 5s 错配；P2-5 重试日志刷屏
- 亮点: Modbus 批量读/写类型映射完备（ADR-003 已修）；串口共享闸门 + 租约引用计数设计正确；Dapper 存储异常归类契约统一；WAL/busy_timeout/检查点备份齐全
- 验证: 纯 review 无代码改动，未跑测试（基线 215/31）

## 修正: ADR-018 P2 段（2026-08-09）
- P2-1 影响修正: 写失败发生在 MeasurementWriteHost 异步落库（MeasurementWriteHost.cs:65），其丢弃 WriteAsync 的 OperationResult → 清理锁库窗口内本地时序批次被静默丢弃（无日志无重试），非"写入短暂中断"；云端数据不受影响（forward_buffer 独立入队）
- P2-2 措辞细化: RegisterAsync 的 IsFailure 分支永不触发，UnregisterAsync/UpdateStatusAsync 直接忽略返回值；补 PointManager 四个方法同问题
- P2-3 行号修正: EnqueueAsync 为 SqliteForwardBuffer.cs:110-128（INSERT :121）

## Code Review: Transport / Storage 模块二轮
- 范围: Transport 全模块（MQTT + HTTP，首轮 ADR-006 仅覆盖 MQTT，HTTP 本轮首次）+ Storage 纯接口层（Buffer/Configuration/TimeSeries）
- 产出: `notes/ADR/ADR-020-transport-module-code-review-2.md`（P1×2 + P2×3 + P3×7）、`notes/ADR/ADR-021-storage-module-code-review-2.md`（P2×2 + P3×6）
- Transport 重点: P1-1 IMqttStateListener 死接线（SignalR MQTT 状态推送永不触发）；P1-2 ConnectAsync 吞 OCE 仍启动重连（取消语义破坏）；P2 HTTP 异常全归类 Timeout、POST 重试非幂等、HTTP 零测试；P3 Options 无校验、fire-and-forget 无兜底、DESIGN.md 漂移 ×3
- Storage 重点: P2 查询接口无上限/死接口面（QueryAsync/QueryByDeviceAsync 生产无调用方）；P3 WriteAsync 原子性契约缺失、QueryLatestAsync 唯一性被消费方补丁掩盖、DeleteAsync 级联未定义、GetByStatusAsync 与 HealthMonitor 口径不一
- 亮点: ADR-006 修复有测试锁定（8+3）；接口只增不删纪律执行严格（Count/QueryByDeviceAsync 生产消费方已切换）
- 验证: 纯 review 无代码改动，未跑测试（基线 215/31）

## 修复: ADR-020/021 P1+P2（2026-08-09）
- Transport ADR-020 P1×2 + P2×3 全部修复：
  - P1-1 IMqttStateListener 接线：MqttClientWrapper 注入监听者，SetState → NotifyStateListeners（fire-and-forget + 异常隔离）；前端 App.vue 的 MqttStateChanged 推送恢复
  - P1-2 ConnectAsync 取消语义：OCE 回落到 Disconnected 后 rethrow 不触发重连；TryReconnectAsync/MqttHostedService 捕 OCE 正常退出
  - P2-1 HTTP 异常分类（TaskCanceledException→Timeout / HttpRequestException→CommunicationError / 取消上抛不计失败）
  - P2-2 HTTP 幂等重试（仅幂等方法重试，POST 直通）；顺带修复潜伏 bug：重试复用同一 HttpRequestMessage 抛 "request message was already sent"
  - P2-3 新增 HttpClientWrapperTests 7 个（HttpMessageHandler 替身）；HTTP csproj 清理死依赖（Microsoft.Extensions.Http/Resilience → Polly.Core 8.7.0，消除 NU1605）
- Storage ADR-021 P2×2 修复：接口注释标注全量查询/遗留接口契约（只增不删，勿新增消费方），无代码行为改动
- 测试: 新增 IntegrationTests 9 个（MQTT 2 + HTTP 7）；验证 build 0 错误；UnitTests 215 通过；IntegrationTests 40 通过（31+9）
- 遗留: ADR-020 P3×7、ADR-021 P3×6 待处理；git 提交由用户执行

## Code Review: Webapi 模块
- 范围: src/NitroGateway.Webapi 全部源文件 + Program.cs 接线 + 消费的 Security/Storage/Alarm 接口 + 部署配置（appsettings.json / docker-compose.yml / launchSettings.json）
- 产出: notes/ADR/ADR-022-webapi-module-code-review.md
  - P1×5: LiveDataHub 未鉴权可匿名订阅实时数据；DevicesController Viewer 可写（RBAC 越权）；DeadLettersController maxCount 未夹紧（SQLite LIMIT 负值全表返回）；appsettings Urls=localhost 致 Docker 容器内 API 不可达；docker-compose JWT 回退公开占位符可伪造 Admin
  - P2×4: 非法枚举/空嵌套输入抛 500 而非 400；AlarmsController.History 无分页上限；OutboxConsumer 热路径日志刷屏；POST 客户端可控 ID + upsert 可覆盖既有设备/点位
  - P3×9: 未用依赖、服务定位、LoginRateLimiter 条目不淘汰、异常响应 JSON 契约不一致、审计记录轮询 GET、Swagger/Metrics 生产暴露、Forwarder 间隔硬编码、launchSettings 端口与文档不一致、健康变更日志无设备信息
- 亮点: ADR-014 配置接线（健康检查/Serilog/Collection）、ADR-015 CQS 诊断只读 State、ADR-017 P3-3 取消保护、SignalR 统一出口（Channel + Outbox）设计清晰；幂等 DELETE、批量导入单事务、分页夹紧（ADR-005）均有测试锁定
- 已有覆盖: 业务失败 200 包裹 / API 无全局限流 → docs/05 开放讨论点 #1/#3，未重复登记
- 验证: 纯 review 无代码改动，未跑测试

## 修复: ADR-022 Webapi 模块 review（2026-08-09）
- P1×5 全部修复：
  - LiveDataHub [Authorize] + MapHub RequireAuthorization + 订阅 Guid 校验（前端已传 access_token，兼容）
  - DevicesController 写动作收窄 AdminOperator（GET 保持 AllRoles），授权反射测试锁定
  - DeadLettersController maxCount 夹紧 1..1000（SQLite LIMIT 负值=无上限）
  - Dockerfile 注入 ASPNETCORE_URLS=http://0.0.0.0:5100（appsettings localhost 绑定致容器内不可达）
  - docker-compose JWT_SECRET 必填 + AddNitroSecurity 拒绝含 ChangeMe 占位符（防伪造 Admin）
- P2×4 全部修复：非法枚举/Guid 改 TryParse+400；AlarmRepository.QueryAsync 加 limit（接口只增）；OutboxConsumer 日志降 Debug；创建路径忽略客户端 ID（防 POST 覆盖）
- P3×8 修复 + 1 撤回：未用依赖、构造注入、限流条目淘汰、审计 GET 降 Debug、Swagger 仅 Development、Forwarder:IntervalMs 配置化、launchSettings 5100、健康变更日志带设备信息；P3-4 复核撤回（两侧 JSON 均 camelCase，契约实际一致）
- 决策保留: /metrics 维持无鉴权（Prometheus 抓取契约）
- 测试: 新增 WebapiAuthorizationTests + WebapiControllerTests（11）+ LoginRateLimiter 淘汰 + SqliteAlarmRepository limit；UnitTests 234 通过（215+19）；IntegrationTests 40 通过；build 0 错误
- 未提交: git 提交由用户执行

## 可用性验证基线（2026-08-09 实跑）
- 环境: .NET SDK 10.0.301 / Docker 29.4.0 / Node 22.12.0
- 构建: `dotnet build NitroGateway.slnx` 0 错误（警告 16：NU1903 SQLitePCLRaw.lib.e_sqlite3 2.1.11 高危漏洞 + 若干 CS 警告）
- 测试: UnitTests 292 通过 + IntegrationTests 40 通过 = 332 全绿
- 启动冒烟: mqtt 容器(docker compose) + Webapi(Debug dll) 3s 内 /healthz 200；/readyz Healthy；/metrics 显示 nitro_mqtt_state=2(Connected)、forward_total 增长
- API 冒烟: 登录 admin/admin123 成功；/api/devices 返回既有测试设备；前端 `npm run build`（vue-tsc + vite）成功（仅 chunk 体积/第三方 PURE 注释警告）
- 遗留: 未接入真实 Modbus 从站，设备处于 Offline；长稳 8h soak 未跑，待按 FACTORY-TEST.md 第六节执行

## 更新: FACTORY-TEST.md 重构为测试标准（2026-08-09）
- 目的: 将工厂模拟检测指南升级为"可量化验收标准"，突出多设备/多采集/长时间运行核心场景
- 变更: 引入 P0/P1/P2 分级与放行规则；新增观测点与健康判定口径表（backlog 常态 0~5、内存 8h≤2 倍、SQLite 8h 增量≤50MB 等阈值）；T0 构建启动基线 / T1 单设备端到端 / T2 多设备并发 / T3 8 小时长稳 定为 P0；T4 故障恢复 / T5 安全 为 P1；T6 前端为 P2；保留 3 个演示场景
- 未提交: git 提交由用户执行

## 生成: FACTORY-TEST.html（2026-08-09）
- 将 FACTORY-TEST.md 转换为自包含 HTML（FACTORY-TEST.html，仓库根目录）
- 样式沿用 docs/00-项目导读.html；含目录锚点、P0/P1/P2 徽标、表格横向滚动、打印样式
- 转换工具: marked（临时目录安装，未改动仓库依赖）；DOM 校验通过（Edge headless）
- 未提交: git 提交由用户执行


