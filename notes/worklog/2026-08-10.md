# 2026-08-10

## 当前目标
职业方向已定：社招上位机/工业软件岗位（门槛低、与 .NET + 工业协议背景匹配）。NitroGateway 演进路线：B 方案（多现场 → 一中心 MQTT 上报）为主。当前进度：桌面端 P0/P1 已实施（ADR-026，2026-08-10）；中心 Ingest P0 已实施（ADR-025，2026-08-10：订阅 → INSERT OR IGNORE → 指标 + compose 服务）；告警上行契约已对齐（ADR-028 P2-1）；CI 双 job 拆分 + 停机排空 flaky 修复完成（ADR-028 P1-1，2026-08-10，并行 slnx 12 轮全绿）。桌面端本地设备/点位配置界面已实施（ADR-029 阶段 1，2026-08-10：设备 CRUD + 点位管理 + Modbus TCP/RTU/S7 表单）。下一步：E 验证（T1/T2 模拟从站端到端 + T3 8h 长稳）、F 决策拍板（NU1903 依赖升级 / NU1701 图表库 / docs/interview 移库）、B3 桌面剩余（串口 Modbus RTU 现场实测、阶段 2 中心配置导入）。git 提交由用户执行

## 文档: 设计方法（docs/06-设计方法.md，2026-08-10）
- 背景: 与用户讨论职业方向（上位机 vs 系统软件）、项目天花板、AI 时代"写代码不值钱、设计值钱"；用户要求把设计方法论落盘备用
- 内容: 设计七问（目标/边界/数据流/状态与失败/契约/规模与演进/权衡）+ B 方案 MQTT 契约演示 + 训练方法（设计先行/逆向工程/只设计切片）

## 设计: B 方案（ADR-025，2026-08-10）
- 产出: notes/ADR/ADR-025-b-scheme-desktop-cloud-design.md——桌面采集端 + MQTT + 云端中心（Ingest）完整设计
- 要点: 契约复用现有 topic `nitrogateway/{deviceId}/measurements` + BatchMeasurements JSON（Forwarder.cs:105），幂等靠 records 主键冲突；Ingest 独立项目/容器；中心库复用现有迁移；失败模式全部落到已有能力（forward_buffer/MqttClientWrapper 重连）
- 未提交: git 提交由用户执行

## 设计: WPF 现场采集端（ADR-026，2026-08-10，后改为 B 方案）
- 产出: notes/ADR/ADR-026-wpf-desktop-shell-design.md——B 方案现场侧桌面完整设计（初版为 A/B 共用壳，已按要求改为 B 方案）
- 要点: 宿主复用现有模块注册扩展（AddNitroCollection/AddNitroForwarder/AddNitroMqtt 等）保证采集行为三端一致；EventBridge 200ms 节流做 UI 数据通道；优雅关闭走 GatewayLifecycle drain；无内嵌 Web（远程查看走中心 Webapi/Vue，ADR-025）；Mqtt__Broker 指向中心 + topic 按 ADR-025 契约；SQLite/日志路径改 %LocalAppData%
- 未提交: git 提交由用户执行

## 实施: WPF 现场采集端（ADR-026 P0/P1，2026-08-10）
- 产出: src/NitroGateway.Desktop（net10.0-windows + WPF）——App 单实例/全局异常（D6/D7）、Hosting/GatewayHost（模块注册对齐 Webapi + 迁移 + drain）、Messaging/EventBridge（200ms 帧节流 + 2s 水位轮询，D2）、Services/UiDispatcher、五页 ViewModel/View（设备/实时/告警/历史/设置）+ LiveCharts2 实时曲线（P1）；appsettings 缺省路径落 %LocalAppData%\NitroGateway（Hosting/DesktopPathConfig，D4）；无内嵌 Web/不启用 AddNitroSecurity（D5）
- 对接: ADR-025 契约零代码改动（topic nitrogateway/{deviceId}/measurements + BatchMeasurements 现成），中心 broker 由 MQTT__Host 环境变量指向
- 测试: 新增 EventBridgeTests(6) + DesktopPathConfigTests(4) + DesktopShellRegistrationTests(1)；测试工程 TFM 调整 net10.0-windows + UseWPF 并补 System.IO/System.Net.Http 隐式 using；全量 357 通过；桌面端冒烟启动通过（建库 + 日志重定向 + 窗口正常）
- 未提交: git 提交由用户执行

## Review: Desktop 模块（ADR-027，2026-08-10，只扫描未修复）
- 产出: notes/ADR/ADR-027-desktop-code-review.md——1 个 P1 + 3 个 P2 + 6 个 P3
- 重点: P1 RealtimeViewModel 异步加载竞态（快速切换设备/点位旧结果晚到覆盖）；P2 HistoryViewModel 同型竞态、AlarmsViewModel 单例捕获 Scoped 仓储（EF change tracker 累积）、历史查询无分页 UI
- 未提交: git 提交由用户执行

## 提交与推送（2026-08-10，用户指示 AI 代执行）
- 提交 26d58df: fix: S7 前后端完整性 + 连接测试修复（ADR-023/024）——src/tests/web/docs/notes 共 22 文件
- 提交 ae2985b: docs: 设计方法七问框架（docs/06）——docs/06 与 worklog
- 分支/远程: master → origin（https://github.com/jiujiugou/NitroGateway.git）
- 结果: 推送成功（5370eb2..ae2985b），工作区已清空
## 修复: Desktop 模块 review 问题（ADR-027，2026-08-10）
- 产出: notes/ADR/ADR-027-desktop-code-review.md（条目已清，状态已修复）
- 修复: P1-1/P2-1 竞态版本守卫（Realtime/History VM）；P2-2 AlarmsViewModel 每刷新建 scope（IServiceScopeFactory）；P2-3 历史分页（上一页/下一页 + offset）；P3-1 EventBridge MqttState 注释对齐；P3-2 UiDispatcher 关闭安全；P3-3 Serilog File sink 按 Name 定位；P3-4 MainWindow 关闭级联 Dispose；P3-5 移除 Console sink；P3-6 日期改 DateTime?
- 测试: 新增 RealtimeViewModelTests(2)/HistoryViewModelTests(5)/AlarmsViewModelTests(1)/UiDispatcherTests(1)，DesktopPathConfigTests 扩展按名解析用例；全量 369 通过
- 未提交: git 提交由用户执行
- 测试稳定性（ADR-027 收尾）: DesktopViewSmokeTests 不再创建 Application（全局单例污染 UiDispatcher.Post 内联路径，导致并行测试挂起），已验证无 Application 时视图实例化/布局正常


## Review: Desktop 模块联动（ADR-028，只扫描未修复）
- 产出: notes/ADR/ADR-028-desktop-module-linkage-review.md——桌面与各模块联动核查
- 结论: 事件三通道/注册顺序/停机 drain 均正确，369 测试通过；发现 P1 CI(Linux) 与 Windows 桌面工程断裂、P2 告警上行契约外流量、P3 EventBridge 帧循环无自愈
- 未提交: git 提交由用户执行


## 清理: 删除非必要文档（2026-08-10，未提交）
- 删除: .trae/documents/nitrogateway-frontend-plan.md（IDE 工具产物）、docs/00-项目导读.html（盘点 HTML 冗余导出）；git rm 已暂存
- 保留: AGENTS.md / README.md / docs/03-功能清单.md / notes/ / 模块 README+DESIGN / FACTORY-TEST / docs/01-06（其中 03 为 AGENTS.md 引用文档）
- 待确认: docs/interview/**（个人面试题库，28 文件）是否移出仓库

## 生成: 项目闭环文档（docs/07-项目闭环.md，2026-08-10）
- 用户要求说明「还差什么项目完成闭环」；产出 docs/07-项目闭环.md——缺口清单（A 阻塞: CI 红/工作区未提交；B 路线: Ingest 未实施/告警契约外/桌面 P2；C P1: HTTP 通道/磁盘保护/EventBridge 自愈；D P2/P3 收尾；E 验证: 长稳与真实从站未跑；F 决策: SQLite 漏洞/图表包兼容/interview 目录）
- 验证: 当日实测 build 0 错误（20 警告）、UnitTests 369 + IntegrationTests 40 全绿
- 未提交: git 提交由用户执行

## 实施: 中心 Ingest（ADR-025 P0）+ 告警上行契约（ADR-028 P2-1）（2026-08-10）
- B1 Ingest 中心: 新增 `src/NitroGateway.Ingest`（net10.0 Web 宿主，端口 5200）——IngestService 订阅 `nitrogateway/+/measurements`（QoS1）→ 反序列化 → SqliteIngestStore 批量 INSERT OR IGNORE（D2 记录级幂等，dedup=记录数-新增数）；写失败重试 3 次指数退避后丢弃 + 计数（D5）；指标 ingest_received_total / ingest_dedup_total / ingest_failures_total（kind=measurements|alarms）；/healthz /readyz /metrics
- B2 告警契约: 中心订阅 `nitrogateway/+/alarms`，按 AlarmId UPSERT 中心 alarms（状态迁移 Active→Resolved 覆盖，非 INSERT OR IGNORE）；现场 MqttAlarmNotifier 契约不变；ADR-028 P2-1 修复
- 工程接线: slnx 新增 Ingest 项目；UnitTests 引用 + FakeMqttClient + IngestServiceTests 6 个（红绿对照：桩阶段 5 红 1 绿 → 实现后 6 绿）；Dockerfile 参数化（APP_PROJECT/APP_DLL/APP_PORT build args，compose 传参切 Ingest）；compose 新增 ingest 服务 + center-data/center-logs 卷；SqlitePragmas 改 public 供中心库复用
- 验证: build 0 错误（28 警告）；UnitTests 375（+6）+ IntegrationTests 40 全绿；本机冒烟——ingest 启动迁移建库（app_version 1.0.0）、/metrics 暴露 ingest_*、无 broker 时 mqtt_state=1 重连中（预期）、/healthz 503（mqtt Unhealthy，预期）
- 遗留: docker daemon 未启动，compose 镜像构建 + 端到端（桌面→broker→Ingest→中心库）待 docker 就绪补跑；ADR-025 P1（payload v 字段）/P2（元数据同步）待办
- 未提交: git 提交由用户执行

## 实施: CI 拆分（ADR-028 P1-1）+ 修复停机排空测试 flaky（2026-08-10）
- CI 拆分: `.github/workflows/ci.yml` 双 job——build-server(ubuntu) 构建 Webapi/Ingest/IntegrationTests 并跑集成测试；build-windows(windows-latest) 全量构建 slnx + 顺序跑 UnitTests→IntegrationTests（避免跨进程并行触发 ForwarderEngineTests flaky）
- 根因: .NET 10 BackgroundService.StartAsync 改用 Task.Run 调度 ExecuteAsync（不再同步执行）；StopAsync 取消若在委托真正启动前到达，Task.Run 直接返回 Canceled 任务、引擎体从未运行，停机排空不发生。本地并行 slnx 下 ~50% 复现（DIAG2 实证: ExecuteTask=[Canceled] logs=[]），单项目/顺序执行稳定
- 修复: ForwarderEngine.ExecuteAsync 入口加 "ForwarderEngine Started." 日志（确定性启动信号，与末尾 Stopped. 配对，ForwarderEngine.cs:106）；ForwarderEngineTests 改等该日志再注入停机现场（原等待 ExecuteTask 非空未完成无法防住 Task.Run 排队窗口，ForwarderEngineTests.cs:175）
- 验证: 并行 slnx 12 轮全绿（修复前首轮即失败）；顺序 CI 等价命令 UnitTests 375 + IntegrationTests 40 全绿；build 0 错误
- 未提交: git 提交由用户执行

## 提交与推送（2026-08-10，用户指示 AI 代执行）
- 提交 abd2365: feat: 桌面现场采集端 + 中心 Ingest + 告警契约 + CI 双 job（ADR-025/026/027/028）——src/tests/docs/notes/CI 共 67 文件（+4357/-593）
- 分支/远程: master → origin（https://github.com/jiujiugou/NitroGateway.git）
- 结果: 首次推送失败（本机 Clash 代理 127.0.0.1:7890 对 github.com 连接被重置，11+ 次重试失败）；代理恢复后重推成功 ac9d987..a8060e4（含 abd2365 + a8060e4 两个提交），工作区已清空

## 修复: 闭环 P1/P2/P3 收尾（2026-08-10 二轮，ADR-011/012/020/021/025/028）
- ADR-011 HTTP 上报通道全部落地（P1~P5）: M008 迁移 channel 列；IForwardBuffer channel 重载（只增不删）；HttpForwarderEngine（5s 周期/批量 POST/Commit/MarkFailed/死信/停机排空）；AddNitroForwarder 改配置驱动（Forwarder:Channels mqtt/http/both）；DataDispatcher 多通道入队（每通道独立 batchId）；/healthz 增 http 检查；appsettings 增 Forwarder:Http
- ADR-012 磁盘保护落地: Storage.Disk 新接口 + DiskGuardService（60s 周期、双目录取最小、滞后防抖）+ 采集/转发降级联动 + DiskHealthCheck + DiskFreeBytes 指标
- ADR-020 P3×7 / ADR-021 P3×6 全部修复（Transport 日志限流/Options 校验/并发安全/DESIGN.md 对齐；Storage 接口契约注释补齐）
- ADR-028 P3-1 EventBridge 自愈（while + 重建 PeriodicTimer 重试，异常后不再永久停摆）
- ADR-025 P1 payload v 版本字段（BatchMeasurements.V=1 输出 v:1；Ingest 旧载荷兼容读取）
- 测试: 新增 HttpForwarderEngineTests×3 + SqliteForwardBuffer channel×2 + DataDispatcher both×1 + Ingest v×1；全量 Unit 387 + Integration 43 全绿，build 0 错误
- 未提交: git 提交由用户执行

## 修复: 中心形态部署补齐（2026-08-10 三轮，回应 Ingest 理解疑问）
- 背景: 用户对 Ingest 的定位反复疑问（现场库 vs 中心库、为何再写一次）；排查发现 compose 只部署了「单现场一体机」，ingest 写 center.db 但无人读，中心展示链路未连通
- 产出: 新增 docker-compose.center.yml（mqtt + ingest 写 center.db + 中心 Webapi 读 center.db + web，JWT_SECRET 必填；与单现场栈同端口，二选一运行）；docker-compose.yml 头注释澄清角色；README 新增「部署形态」三形态说明；FACTORY-TEST 新增 T7 中心形态端到端（T7.1~T7.8：栈就绪/采集上行/中心入库/中心 API/中心 Web/幂等去重/断网续传/中心重启）
- 验证: docker compose -f docker-compose.center.yml config 校验通过；build + 全量测试不涉及代码改动（本轮纯部署/文档）
- 未提交: git 提交由用户执行

## 实施: 桌面端本地设备/点位配置界面（ADR-029 阶段 1，2026-08-10）
- 产出: 设备页增删改 + 点位管理闭环——DevicesViewModel 增 Add/Edit/Delete/ManagePoints 命令（复用 IDeviceManager，RegisterAsync upsert/UnregisterAsync 级联删点位）；PointsWindow + PointsViewModel（复用 IPointManager 增删改查）；DeviceEditor/PointEditor 表单模型（协议联动：Modbus TCP UnitId/DataFormat、RTU +Transport/BaudRate/Parity、S7 Rack/Slot/CpuType/PingAddress，与 Web DeviceForm.vue 对齐）；IDeviceDialogService 对话框抽象 + WPF 模态实现（DeviceEditorWindow/PointEditorWindow/PointsWindow）；DI 注册 Singleton
- 关键决策: 零后端改动/不改契约（复用桌面端已注册 AddNitroDevice）；IDeviceManager/IPointManager 为 Scoped，VM 命令内 IServiceScopeFactory 建作用域（与 AlarmsViewModel 同法）；阶段 2 中心导入仅记录决策（中心为准、全量覆盖、Token 存 %LocalAppData%、手动触发）
- 测试: 新增 DevicesViewModelTests(8)/PointsViewModelTests(7)/DeviceEditorTests(5)/PointEditorTests(3) + STA 窗口冒烟(1)；全量 Unit 414（+24）+ Integration 43 全绿，build 0 错误
- 未提交: git 提交由用户执行
